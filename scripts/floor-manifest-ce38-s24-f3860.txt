# scripts/floor-manifest-ce38-s24-f3860.txt
#
# CE-38 S2/4 · F-38.60 — THE REGISTRY'S PROSE WAS PARSED AS CODE.
#
# [F-19.16] A delivery tree is dirty by definition and R-33.7 forbids the
# executor the commit that would clean it. Every dirty path must be named here;
# one byte outside and the runner refuses. The manifest's files are SHA-256'd
# before the run and re-hashed after, so the SET is tolerated and the CONTENTS
# are not.
#
# FOUR PATHS, AND NO PRODUCT BYTE AMONG THEM. The tree was correct: all six
# links are declared with their source lines and the source-side twin (b40 C31)
# was green on the same tree the audit called red. Two readers, this manifest,
# its handover.
docs/TDW_09_M_FINISH_S24_F3860_HANDOVER.md
scripts/b40_worklist_shell_bench.js
scripts/floor-manifest-ce38-s24-f3860.txt
tools/wl_audit.mjs
