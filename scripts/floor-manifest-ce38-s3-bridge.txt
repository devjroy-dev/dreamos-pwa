# scripts/floor-manifest-ce38-s3-bridge.txt
#
# CE-38 S3 · F-38.52 · THE INTERIM /vendor LANE BRIDGE — THE DECLARED DIRT.
#
# [F-19.16] A delivery tree is dirty by definition and R-33.7 forbids the
# executor the commit that would clean it. Every dirty path must be named here;
# one byte outside and the runner refuses. The manifest's files are SHA-256'd
# before the run and re-hashed after, so the SET is tolerated and the CONTENTS
# are not.
#
# FIVE PATHS. The bridge is one write inside the existing one writer; the other
# four are its proof, that proof's compile config and wrapper, and this manifest.
lib/worklist/mode.ts
scripts/modeBridge.proof.ts
scripts/modeBridge.proof.tsconfig.json
scripts/run-mode-bridge-proof.sh
scripts/floor-manifest-ce38-s3-bridge.txt
