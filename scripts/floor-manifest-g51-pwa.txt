# scripts/floor-manifest-g51-pwa.txt
scripts/tdw_stripper_census.out.txt
# TDW_19 · BLOCK 19 · G5.1 — THE pwa HALF. The record's one control, the forward
# sheet with its refusal state, the two stamps on the record, the room, the live
# hub row, C31 by label, and the R5-hub re-shoot.
#
# F-14.16: a dirty file outside this list is a STOP until accounted for. Every
# file below was authored or amended by THIS seat in THIS sitting. The tree was
# clean at 02b1b75 before a byte moved and carried to cb724b6 mid-build (five
# files, G1.2's tap arc, disjoint — proven by `git diff --name-only` against this
# list before the carry, and all six held paths byte-identical after).
#
# THE MOCK RE-SHOOT IS NOT THIS SITTING'S CHANGE. G2's a48fa9b made the Google
# reviews hub row live while this seat was in the dream-os half, so the ratified
# R5-hub frame drew a hub that no longer exists. Four captures move; the other
# twenty-four are byte-identical, and the html's other eight frames are untouched.

app/vendor/(shell)/events/body.tsx
app/vendor/(shell)/expenses/body.tsx
app/vendor/(shell)/leads/body.tsx
app/vendor/(shell)/referrals/page.tsx
app/vendor/(shell)/support/page.tsx
components/vendor/slices/DetailSheet.tsx
components/vendor/slices/ForwardSheet.tsx
components/vendor/slices/SliceRow.tsx
components/vendor/slices/SliceShell.tsx
docs/mocks/referrals-mock.html
docs/mocks/referrals-mock__R5-hub__dark__374.png
docs/mocks/referrals-mock__R5-hub__dark__390.png
docs/mocks/referrals-mock__R5-hub__light__374.png
docs/mocks/referrals-mock__R5-hub__light__390.png
lib/solutions/routes.ts
lib/solutions/types.ts
lib/vendor/types/vendor.ts
lib/worklist/referrals.ts
scripts/b40_worklist_shell_bench.js
scripts/floor-manifest-g51-pwa.txt
scripts/tdw_stripper_census.out.txt

# ── MICRO 1 (2026-09-06, base 6d014ea) ─────────────────────────────────────
# R-G51.13 / F-40.119: `cap()` title-cased the vendor's own sentence. The row now
# declares `verbatim` and DetailSheet honours it — the helper's default untouched,
# every other slice unchanged BY CONSTRUCTION rather than by inspection.
# R-G51.16 / R-40.52: the REFERRAL chip, peer's copy only, gated on `referralIn`.
# Both found by the founder's walk. No bench asserted what the glass does to a
# value after it reaches the wire, which is the gap the walk law exists to cover.
#
# ⚠ THE FIRST CUT OF THIS MICRO WAS DESTROYED UNAPPLIED. It was built on cb724b6
# while origin stood at 6d014ea, because the pwa half was pushed from the
# founder's tree and this workspace never carried the commit. Four of the five
# files would have been shipped from a tree two commits stale. Rebuilt on the
# true base after proving G3.2's 6d014ea touched only docs/mocks.
#
# ── THE CENSUS CAPTURE, REGENERATED — F-40.69 FIRING ON THIS SEAT ──────────
# `tdw_f3942_census_guard` reddened at 6d014ea. NOT another seat's: the capture
# was stamped at tip 02b1b75 scanning 359 files, the live tree scans 362, and the
# gap is exactly the three .ts/.tsx files MY OWN pwa half committed at 1fe47b0 —
# `app/vendor/(shell)/referrals/page.tsx`, `components/vendor/slices/
# ForwardSheet.tsx`, `lib/worklist/referrals.ts`. Arithmetic, not plausibility.
#
# THIS IS F-40.69'S MECHANISM, EXACTLY AS FILED, NOW ON ITS THIRD SEAT. The
# census walks `git ls-files`, so in `--delivery` mode a delivery's NEW files are
# UNTRACKED and invisible to it: the capture reads correct AT THE CUT and becomes
# false the instant the delivery is committed. Both my floor and the founder's
# printed `FLOOR = NAMED BASE` truthfully over a tree the instrument could not
# yet enumerate. The finding predicted this; the prediction held again.
#
# Regenerated by the census's OWN documented step (`node scripts/
# tdw_stripper_census.mjs --write`), never hand-edited — the file is
# generator-managed and the bench prints its own cure.
#
# ── F-40.119's CURE LANDS ESTATE-WIDE (chair-ruled 2026-09-06) ─────────────
# `cap()` title-cased every detail value, which is right for a token and wrong
# for a person's words. FOUR rows across three slices carried free text the
# vendor typed into AddSheet's textarea and rendered as headlines: leads `Notes`,
# events `Notes`, expenses `Description`, and G5.1's two referral rows. Three of
# them are OUTSIDE this sitting's radius and were lifted BY LINE on the chair's
# ruling — one `verbatim` flag each, no other byte in those two files.
#
# C101 WALKS THE ROWS, IT DOES NOT COUNT THEM. A cell asserting "four rows carry
# the flag" would pass on the wrong four. Four mutations red it: stripping the
# flag from a row outside G5.1's radius, declaring the flag while DetailSheet
# ignores it, retiring cap() so the opt-out becomes an opt-in, and — the one that
# matters beyond today — ADDING A FIFTH PROSE ROW UNFLAGGED.
#
# CARRY: 6d014ea -> 3d13d65 (G1.3's mock cut, docs/mocks only, disjoint — proven
# by `git diff --name-only` against this list before the carry; all 22 declared
# paths byte-identical after). The floor above was measured at 6d014ea and the
# carry moved no code byte; b40 re-run GREEN at 3d13d65.

