# scripts/floor-manifest-ce38-s24-f3855.txt
#
# CE-38 S2/4 · F-38.55 — THE ONE-HUNK RESTORE AND ITS CELL.
#
# [F-19.16] A delivery tree is dirty by definition and R-33.7 forbids the
# executor the commit that would clean it. Every dirty path must be named here;
# one byte outside and the runner refuses. The manifest's files are SHA-256'd
# before the run and re-hashed after, so the SET is tolerated and the CONTENTS
# are not.
#
# FOUR PATHS, and the smallness is the point: a whole-file copy over a skipped
# guard reverted one region of one file, so the repair touches that region, the
# cell that now guards it, this manifest and its handover. Nothing else.
docs/TDW_09_M_FINISH_S24_F3855_HANDOVER.md
lib/worklist/copy.ts
scripts/b40_worklist_shell_bench.js
scripts/floor-manifest-ce38-s24-f3855.txt
