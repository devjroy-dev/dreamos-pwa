# scripts/floor-manifest-ce38-s3-seam0.txt
#
# CE-38 S3 · SEAM 0 · F-19.16 + F-19.18 (dreamos-pwa) — THE DECLARED DIRT.
#
# [F-19.16] A delivery tree is dirty by definition and R-33.7 forbids the
# executor the commit that would clean it. Every dirty path must be named here;
# one byte outside and the runner refuses. The manifest's files are SHA-256'd
# before the run and re-hashed after, so the SET is tolerated and the CONTENTS
# are not.
#
# THIS FILE IS ON ITS OWN LIST, and the naming follows dream-os's twenty-two
# existing floor-manifests rather than inventing a second home at repo root
# beside MANIFEST.txt, which is a ZIP file table and a different artefact.
#
# FIVE PATHS.
scripts/run-floor.sh
scripts/tdw_f0774_vacuity_probe.mjs
scripts/tdw07_f0772_circle.proof.mjs
scripts/lib/mutateCopy.mjs
scripts/floor-manifest-ce38-s3-seam0.txt
