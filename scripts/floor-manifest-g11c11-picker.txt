# scripts/floor-manifest-g11c11-picker.txt
# TDW_19 · BLOCK 19 · G1.1c — F-40.68 / R-G11c.11: THE PICKER'S WINDOW.
#
# F-19.16 / F-39.51(b): a dirty file outside this list is a STOP until accounted
# for. Both files were authored by THIS seat in THIS sitting; nothing inherited.
# The tree was clean at the banked tip 250d420 before a byte moved, and clean
# again after b42's mutation pass restored every byte it touched.

# ── THE LIFTED BYTE  [R-G11c.11 — one call site] ───────────────────────────
# The create sheet's events read passed no window and inherited the door's
# default `from = istTodayISO()` (src/api/vendor/events.js:210-212, applied at
# :258-263), so the picker could only ever offer FUTURE events — the feature's
# premise inverted, a wedding page being work that has already happened. It now
# passes an explicit past floor and a forward bound built from the estate's one
# IST day home. No new date arithmetic; `WP_PICKER_FROM` is one home, one reader.
app/vendor/(shell)/wedding-pages/page.tsx

# ── THE BENCH  [C12, five cells + two production mutations] ────────────────
# The cells assert the FLOOR, not the argument count: a four-argument call whose
# third argument were `istTodayISO()` satisfies any arity check and leaves the
# defect exactly where it was, which is why the second mutation exists.
scripts/b42_g11_wedding_pages_bench.js

# ── THE CAPTURE, REGENERATED  [not this micro's delta — declared, not absorbed]
# `tdw_f3942_census_guard` §1.3 reddened at the banked tip 250d420: the captured
# `retired` total said 213 and a fresh run says 214. IT IS NOT THIS MICRO'S.
# Derived, not assumed: the census's file list and both totals are BYTE-IDENTICAL
# with this micro applied and with it stashed (214 either way), and a worktree at
# acb68f9 reads 213 — so the crossing happened at 250d420, the couple-switch
# delivery, and `lib/frost/coupleSwitch.ts` is the file that crossed it.
#
# WHY NEITHER FLOOR RUN COULD SEE IT (F-40.69). The census walks `git ls-files`.
# In `--delivery` mode a delivery's NEW files are untracked, so they are invisible
# to it — this seat's floor and the founder's both read 213 and both said
# `FLOOR = NAMED BASE`, truthfully, over a tree whose new files the instrument
# could not enumerate. The number became false the instant the delivery was
# committed. That is a hole in the delivery-floor discipline, not in either run.
#
# WHAT THE REGENERATION ABSORBS, ACCOUNTED IN FULL rather than left as a number:
#   · tip 1961164 -> 250d420 · scanned 353 -> 354 · retired 213 -> 214
#   · two body blocks appear: `lib/frost/coupleSwitch.ts`'s crossing (mine, from
#     250d420) and `app/credits/[token]/page.tsx` (the G1.1 sitting's, never
#     captured — a staleness this micro inherits rather than causes).
# The file is generator-managed and the bench prints its own cure; the `repo tip`
# line is descriptive and asserted by no cell, so it reads one commit behind from
# the moment this lands, exactly as the census's header says it will.
scripts/tdw_stripper_census.out.txt

# ── b50_fetch_loop_bench · DECLARED OUT OF THIS GATE  [F-40.71, chair-ruled] ─
# NOT A SILENT SKIP, AND NOT A MECHANISM EITHER — read the second half.
#
# `b50_fetch_loop_bench` returns GREEN and RED on IDENTICAL BYTES, on one build,
# on one machine, minutes apart. Measured, not asserted: 2 RED in 10 on this
# delivery's build, 0 RED in 10 at 250d420 (the chair-ruled samples), and 5 in 15
# vs 0 in 14 pooling every correctly-built run of this sitting. A bench whose
# verdict flips on identical bytes cannot police a base: it convicts or exonerates
# by luck, and it did both to this micro before the reach derivation settled it.
#
# WHAT IT CATCHES WHEN IT REDS IS REAL AND IS NOT THIS DELIVERY'S. Its subject is
# F-39.46, and `useLoader`'s own header describes the race: `run` re-identifies on
# every render and one effect issues `fetcher(vendorId)` BEFORE its own `tick`
# abort check, so a superseded fetch has already left the tab. An intermittent
# 2x/3x on `money/invoices` is that defect at small amplitude — latent, uncured.
#
# ⚠ THIS DECLARATION IS INERT AND THAT IS REPORTED, NOT WORKED AROUND.
# `run-floor.sh:121` strips `#` before reading this file, so a manifest carries a
# FILE LIST and nothing else; the runner has no bench-exclusion hook at all.
# The ruling that `b50` be "excluded from the delivery gate by declaration"
# therefore cannot execute as worded against this runner (protocol §9: a ruling
# that cannot execute as worded is REPORTED, never quietly adapted). This block
# is the RECORD of the exclusion; the GATE still runs b50 and can still red on it
# roughly one run in five. The chair's word is owed on which arm closes it.

# ── THE HANDOVER, AMENDED IN PLACE  [chair-ruled: the counts ride the record] ─
# F-40.69, F-40.70 and F-40.71 filed with their derivations; F-40.71 carries the
# measured rate table and its Fisher figures, including the pooled p that crosses
# 0.05 and is deliberately NOT treated as attribution. Three further executor
# errors owned: e-6 (a conditional command handed beside its primary), e-7 (a
# probe built so its comfortable answer was unfalsifiable), e-8 (a result asserted
# before it was run).
docs/TDW_19_G11C_PWA_HANDOVER.md

# ── THIS MANIFEST ───────────────────────────────────────────────────────────
scripts/floor-manifest-g11c11-picker.txt
