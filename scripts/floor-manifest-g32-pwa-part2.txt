# scripts/floor-manifest-g32-pwa-part2.txt
# TDW_19 · BLOCK 19 · G3.2 — THE pwa HALF, PART 2. The `New contract` fork, the
# client picker, the record, and the cell the chair asked for.
#
# F-14.16: a dirty file outside this list is a STOP until accounted for. The tree
# was clean at 364bd0a before a byte moved — `git status --porcelain | wc -l`
# returned 0 after `git checkout -q origin/main`.
#
# ⚠ G1.3's pwa half did NOT land under this seat. The tip was 364bd0a at the read
# and 364bd0a at the cut. Had it landed it would have been outside this radius.

app/vendor/(shell)/contracts/screen.tsx
lib/vendor/api/vendor.ts
scripts/b57_contracts_wiring_bench.js
scripts/floor-manifest-g32-pwa-part2.txt
docs/TDW_19_G32_PWA_HANDOVER.md

# ── THE FLOOR ───────────────────────────────────────────────────────────────
#   tsc --noEmit                    CLEAN, exit 0
#   b57_contracts_wiring_bench      26/26 — NEW
#   b40_worklist_shell_bench        GREEN
#   b42_g11_wedding_pages_bench     GREEN
#   b05_f0589_pwa_name_wire_bench   GREEN
#   tools/bs_audit.mjs              RED · 2 cells — IDENTICAL at 364bd0a
#   tools/wl_audit.mjs              RED · exit only — IDENTICAL at 364bd0a
#   DELTA                           ZERO
#
# ⚠ `next build` OWED ON THE FOUNDER'S MACHINE, same shape as part 1: this
# container cannot reach fonts.googleapis.com and four `next/font` fetches fail.
# NOT ONE application error is reported. The gate is not claimed green here.
#
# ── b57, BOTH WAYS ──────────────────────────────────────────────────────────
# FIVE production mutations driven and reverted:
#   §1  delete the composeContract call        → 1 red (orphans: composeContract)
#   §2  point the picker at the binder plane   → 2 red
#   §4  drop include_cancelled from the client → 1 red
#   §5  Mark Signed on a composed contract     → 1 red
#   §6  a tenth byte in SIGN_COPY              → 1 red
#
# ⚠ ONE MUTATION WAS BADLY CHOSEN AND IS RECORDED RATHER THAN REPLACED QUIETLY.
# §4's first attempt renamed ONE `fetchAllContracts` call site and reddened
# nothing, because the room has TWO and the survivor satisfied the cell. The
# mutation that removes the BEHAVIOUR is the query param.
#
# ⚠ AND ONE CELL REDDENED ON ITS OWN REGEX. §5 matched `Mark Signed` within 400
# characters of its gate; the gate sits 600 away. Widening the window would have
# been tuning a cell to a tree, so the cell was rewritten to assert the BRANCH —
# the last `isComposed(selected)` before the label is negated. A window is a guess
# about formatting; that is a statement about the code.
#
# ── WHAT §1 FOUND ON ITS FIRST RUN ──────────────────────────────────────────
# THREE ORPHANS, all real, all this arc's own:
#   fetchContracts        — the room reads `fetchAllContracts` since part 1, so
#                           this had ZERO callers. RETIRED WITH ITS READER. The
#                           DOOR is not deleted and its default is not widened.
#   fetchContractProfile  — NOT SHIPPED. See below.
#   saveContractProfile   — NOT SHIPPED. See below.
# The cell the chair asked for closed F-40.109's class by finding three instances
# of it inside this seat's own delivery, on its first run.

# ── MICRO 1 (2026-09-06) · derived at dreamos-pwa 35b9ec4 ───────────────────
# R-G32.17/.18 built, R1-R8 vetoed. The picker's three states, the union with
# the Cabinet, the origin marks, and the promotion line.
#
#   app/vendor/(shell)/contracts/screen.tsx
#   lib/vendor/api/vendor.ts
#   scripts/b57_contracts_wiring_bench.js
#
#   tsc --noEmit                    CLEAN
#   b57                             39/39   (26 -> 39), five mutations both ways:
#       pickState back to a length key            -> 1 red
#       R7 keyed on the row's origin              -> 1 red
#       drop the Cabinet read                     -> 1 red
#       drop the phone dedup                      -> 1 red
#       drop the origin mark                      -> 1 red
#   b40 · b05_f0589                 GREEN
#   b42_g11_wedding_pages           RED · ONE cell, IDENTICAL at base — the same
#                                   inherited red the chair recorded at G1.3's
#                                   cfe4525 (`The cards couldn't be made.` /
#                                   `Make the reel`). It was GREEN when this seat
#                                   ran it at 364bd0a because G1.3 had not landed.
#                                   NOT this delivery's, proven by stash.
#   bs_audit · wl_audit             RED, IDENTICAL at base
#   DELTA                           ZERO
#
# ⚠ `next build` OWED on the founder's machine, same shape as parts 1 and 2.

# ── MICRO 2 (2026-09-06) · derived at dreamos-pwa b3e7083 ──────────────────
# P1-P4 and Q1-Q10 vetoed; the walk's three findings cured.
#
#   app/vendor/(shell)/contracts/screen.tsx
#   lib/vendor/api/vendor.ts
#   scripts/b57_contracts_wiring_bench.js
#
#   tsc --noEmit    CLEAN
#   b57             57/57  (39 -> 57), five mutations both ways:
#       open the door address directly (F-40.152's shape) -> 2 red
#       Send greyed instead of absent                     -> 1 red
#       a second required mark                            -> 1 red
#       phone written into terms                          -> 2 red
#       back to a vh literal                              -> 2 red
#   b40 · b05_f0589                 GREEN
#   bs_audit · wl_audit             RED, IDENTICAL at base
#   DELTA                           ZERO
#
# ⚠ A SECOND WRONG-PATH GUESS WAS CAUGHT BEFORE IT SHIPPED. The first cut of
# `updateClientPhone` addressed `/clients/:vendorId/:clientId` by symmetry with
# the LIST door on that same router. The patch door is `/clients/:clientId` —
# resolveVendor mode C `via: 'clients'`, where the list is mode B `:vendorId`.
# Two shapes on one router, and the wrong one 404s. Derived by reading
# `src/api/vendor/clients.js:174`, not by pattern-matching the neighbour. It also
# returns **409 PHONE_COLLISION**, which the record now surfaces with the door's
# own sentence rather than a generic failure.
#
# ⚠ A JSX COMMENT CANNOT SIT INSIDE `{cond && (`. The F-40.154 note was first
# written as `{/* … */}` in an EXPRESSION position and broke the parse. Folded
# into the block comment above the guard. Recorded because it is the third time
# this sitting a comment has had to move to a legal or non-punishing position.

# ── MICRO 3 (2026-09-06) · derived at dreamos-pwa 491c327 ──────────────────
# F-40.161 — the phone field's Save path, which the door proved was not its fault
# (`HTTP 200 {"ok":true,...,"phone":"9327715877"}` on a hand-made PATCH).
#
#   app/vendor/(shell)/contracts/screen.tsx
#   scripts/b57_contracts_wiring_bench.js     57 -> 61
#
# TWO DEFECTS, ONE OF WHICH THIS SEAT HAD NOT NAMED BEFORE DERIVING IT:
#
#   (a) THE SEED read `clients`, the PICKER's rows, which are filled only by
#       `openPicker`. A record opened FROM THE LIST — every record after the first
#       — found an empty array, so the field showed `Not filled` whatever the row
#       held, and `known` was '' so every save re-PATCHed.
#
#   (b) **THE GATE AND THE DOOR CONSULTED DIFFERENT SOURCES.** Send was gated on
#       `phone`, the INPUT BOX. Typing a number summoned the button instantly; the
#       door reads the ROW and answered `No number to send to.` That is what the
#       founder hit, and it is a control that lies about its own condition. The
#       gate is now `savedPhone`, which advances ONLY after the door says yes.
#
#   Mutations (harness now reads EXIT CODES, not just FAIL lines):
#       gate Send on the input again              -> exit 1, 3 red
#       seed from the picker rows again           -> exit 1, 2 red
#       advance savedPhone before the door answers-> exit 1, 2 red
#
#   tsc CLEAN · b57 61/61 · b40 · b05_f0589 GREEN
#   bs_audit · wl_audit RED, IDENTICAL at base · DELTA ZERO
#   `next build` OWED on the founder's machine (fonts.googleapis.com unreachable here)
#
# ⚠ THE GUARD REFUSED THIS PACKET ONCE, AND IT WAS RIGHT TO.
# `base_guard.sh` answered `REFUSED — HEAD is 491c327, base is 3258407 — the local
# checkout is not on the base. Do not apply. Nothing has been copied.` G3.1 pwa
# part 1 had landed between the cut and the apply. **NOTHING WAS COPIED**, which is
# the whole value of the guard: a packet derived against a stale tip never touched
# the tree.
#
# Re-derived at 491c327 by command. G3.1 part 1 moved `app/v/[code]/**`,
# `lib/public/copy.ts` and `tools/bs_audit.mjs` — the public lane and its audit,
# NOTHING in G3.2's radius. The three files were reapplied on the new tip and every
# floor re-run there:
#
#   tsc --noEmit                 CLEAN
#   b57                          61/61
#   b40 · b05_f0589              GREEN
#   b42_g11_wedding_pages        RED — IDENTICAL at 491c327, proven by stash;
#                                inherited since G1.3's cfe4525, not this delivery's
#   bs_audit · wl_audit          RED 2 / 0 FAIL lines, IDENTICAL at base
#   DELTA                        ZERO
#
# R-40.69: a base line means derived-at, so a moved queue is answered by deriving
# again rather than editing the line. Second time this sitting.
