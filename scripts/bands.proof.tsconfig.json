{
  "//": "TDW_04.5 P2 — standalone compile config for scripts/bands.proof.ts. The proof",
  "//2": "imports the REAL lib/vendor/derive, which resolves CabinetBinder through the",
  "//3": "repo's `@/*` alias; a bare `tsc file.ts` cannot see that alias and fails TS2307.",
  "//4": "This config supplies exactly the alias and nothing else — no error is suppressed.",
  "compilerOptions": {
    "baseUrl": "..",
    "paths": { "@/*": ["./*"] },
    "module": "commonjs",
    "target": "es2020",
    "moduleResolution": "node",
    "esModuleInterop": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmitOnError": true
  },
  "files": ["bands.proof.ts"]
}
