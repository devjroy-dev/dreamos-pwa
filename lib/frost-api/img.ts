// lib/frost-api/img.ts
// TDW_07 P3 · Fork 5(b) as RATIFIED — the spec names this path and the vendor
// twin; the chair ruled ONE module re-exported at both, never two tables.
// The variant widths, the LQIP chain and the `^v\d+$` pass-through rule all live
// in lib/img.ts and are written down exactly once. This file is an address.
export * from '../img';
