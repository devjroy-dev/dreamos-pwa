// lib/vendor/img.ts
// TDW_07 P3 · Fork 5(b) — the vendor-side address of the ONE img module.
// See lib/frost-api/img.ts for the same note. Nothing is defined here on purpose:
// a variant table copied twice is a drift generator, and this estate already paid
// that bill at the photo floor (P2 folded three copies of the number into one).
export * from '../img';
