# TDW · BLOCK 19 · G3.1 sitting 2 — YOUR WEBSITE & SEO · PWA PACKET (handover)

**Base** dreamos-pwa `2baa86cde88cbec5452fff67850e721ad707714d` · dream-os `c4145c8ab3cace992f08f2034588b6a92422eb09` (built on `bfa3197`, re-pinned through `6967978`, `2f420c6`, `2baa86c`; no file in this radius moved at any step — census by `git diff --name-only`). dream-os packet 1 sealed at `a4fdc92`.
**Rulings** R-40.122 (three surfaces) · R-40.123 (prototype yes; `the 28 before`; W3-report-empty) · FORK 2 as drawn · FORK 3 computed · F-40.253 retired · R-40.101 (the mock lands with the build).

## What this packet lands

| file | what | bench |
|---|---|---|
| `app/vendor/(shell)/storefront/screen.tsx` | the room replaced to the ratified prototype: live `/v/<handle>` in the shell (iframe, 374 scaled .914; 1:1 on tap) · What to fix (FORK 2: cover, about, city, price + a wedding page / a venue on one; open first, done collapse past three; never a score) · Your address (copy / share / open / QR off `solutions/storefront/qr.png` / `Your own name` behind the `website` gate with its honest line) · Weddings on your page + the date-check switch (mechanism carried: C103/C104/C109 green) · SEO — found on Google (title/description → `PATCH /me` seo_* → revalidate; Connect Google → `GET /solutions/google/connect`; `last_28` / `the 28 before`; five searches; three things COMPUTED from her gaps + her queries — FORK 3) | b40 C97 (amended by label), C98, C103, C104, C109 |
| `lib/worklist/copy.ts` · `lib/worklist/rooms.ts` | R-40.122: masthead `Your website` (`storefrontTitle`), tile `Your website & SEO` (matches the hub row at `solutions/copy.ts:109`; F-40.143 closes). `storefrontBioCta` / `storefrontPublicLabel` retired with their surface | b40 C97 |
| `lib/solutions/copy.ts` | F-40.253: `checkLive`, `checkPending`, `websiteAddressPending`, `websiteAddressNote` retired (zero readers, census by grep) | — |
| `app/v/[code]/page.tsx` | `generateMetadata` title/description and the JSON-LD description read `card.meta` (the door's one derivation, a4fdc92); the pre-a4fdc92 lines stand as `\|\|` arms only; canonical unchanged | — |
| `app/robots.ts` | one robots file: `/`, `/v/` allowed; every signed-in and token surface disallowed (derived from `ls app` at the cut); sitemap named | — |
| `app/sitemap.ts` | statics + every storefront and published wedding page from **`GET /api/v2/public/sitemap`** — a dream-os door that does not exist at a4fdc92 (**owed: dream-os packet 2**); until it lands the file serves the statics and logs nothing noisy | — |
| `lib/vendor/types/vendor.ts` | `seo_title` / `seo_description` on `/me`, `PATCH /me` request and echo | tsc |
| `scripts/b40_worklist_shell_bench.js` | C97 amended by label (R-40.123): one `Primary()` home, rooms opened by their homes, the two retired bytes stay retired | 105 GREEN; C50 and C102 RED **at the tip** (C50: contracts screen, G3.2 s3; C102: wedding-pages/page.tsx:840–841, F-40.239) — neither in this packet |
| `docs/mocks/your-website-s2-proto.html` + 32 shots + `G31_S2_VETO_LIST.md` | the ratified mock, in the tree with the build (R-40.101) | — |

`tsc --noEmit` clean in seat. **`next build` is the founder's gate at apply (R-40.66).** b42 is 170/172 at the tip (two G1.x cells) — unchanged by this packet.

## Walk (kickoff §5, as MAKEUPBYSWATIROY — her row still unverified by this seat)
Rooms → `Your website & SEO` → masthead `Your website`; her page in the window; fix two items (about, city) from the sheets and watch the window update after revalidate; Address → Copy; SEO — found on Google → Connect Google (needs the four Railway keys; until then the room says `Google connection is being set up.`); read three things.

## Owed
- dream-os packet 2: `GET /api/v2/public/sitemap` (handles + published slugs + updated_at, no phone), the nightly `pull()` runner, `PUBLIC_SCHEMA.md` for 0147 (chair).
- P2: the search/price/Get/status screens as drawn, when `RESELLERCLUB_*` exist (behind `website.live`).
- The iframe preview relies on the leaf being frameable by its own origin (no frame-ancestors header in the tree today); if a CSP lands later, add `frame-ancestors 'self'`.
