# TDW_09 · PACKAGE 2 · PHASE C — KICKOFF (rosters derived at 2ef6e4a; zero C bytes written)

**Banked by the A/B/R1/R2 executor at the clean seam, 2026-08-07.** Successor: checkout-first at origin tip (`git fetch` — the TDW_10 billing sitting is landing on dream-os in parallel; this phase is **pwa-only by charter**, any dream-os want = STOP-and-fork). All floors green at this seam: doors 86/86 · p2b 29/29 · r1 13/13 · arbiter 16/16 (founder-witnessed at his desk with sibling `bb72d46`) · canon 24/24 · p4b_body 133/133 · home 67/67 · tsc 0 · standing reds F-10.62/F-10.49 fenced.

## 1 · THE PRESSED ROSTER (re-derived 2026-08-07 at 2ef6e4a; charter's 29 moves by labelled amendment only)
`grep WebkitTapHighlightColor` ∧ file lacks `pressedStyle|onPointerDown` → **29 sites / 7 files**:
| sites | file | lane | disposition |
|---|---|---|---|
| 20 | `app/(frost)/frost/canvas/sanctuary/page.tsx` | couple (in-package: Phase B touched it) | ADOPT — chip grids; per-file `PressBtn` local wrapper (keyed state per map-rendered button; no new shared API) |
| 4+1+1 | `app/demo/vendor/[handle]/page.tsx` · `.../collab/page.tsx` · `components/demo/ModePill.tsx` | demo | **EXCLUDED under F-09.89's standing hold** — the twin's organs are frozen until its rider; adopt there WITH the five-door wholesale cure. Named in the amendment. |
| 1 | `components/vendor/VictorModeChip.tsx` | vendor | ADOPT (the one mode control gains the acknowledgment) |
| 1 | `components/vendor/CalendarBlockSheet.tsx` | vendor | ADOPT |
| 1 | `app/vendor/collab/page.tsx` | vendor | ADOPT |
**Amendment pre-draft:** 29 chartered → 23 adopted + 6 demo-held (warrant F-09.89), instrument headline `tap_suppress=30` reconciled in the same line (its per-file counting vs per-site — derive, don't assume). files_with_pressed at seam = 12.

## 2 · touchBox44 → THE CHAT CHIPS
`components/vendor/ChatThread.tsx:139` — clarify chips `height: 32` (the F-09.22 worst-verified class) and the M-3 report chip mirroring below (~:156). Cure: spread `touchBox44(32)` + pressed state on both; visual height unmoved by construction (the primitive's own no-op law). Both-ways: revert ChatThread alone.

## 3 · rowBaseline → F-09.16's MIRRORS (R-X24 numbers = acceptance)
Mirror roster NOT yet re-derived at tip — first successor act: pull F-09.16's mechanism A/B site list from the Blueprint read-first §3 + its fork paper (`docs/` dream-os, read-only), then re-shoot at the RULED dimensions (the measured 5–6px and 1px specimens are the before-numbers). Adopt `rowBaseline()`/`rowGlyphSlot()`; acceptance is the re-shot landing at the ruled dimensions, not near them.

## 4 · F-09.83 — THE INK-ON-BRASS ROLE MINT (canon-side)
P2A-ground counts: `#1a120e`×59 · `#111`×22 · `#111111`×5 · `#0c0a09`×10 — **re-derive at your tip before any byte** (R2 and B moved files). Zero live in `theme.ts`/`globals.css` (verified at seam) — all inline tsx. Cure shape: ONE named role minted in the canon homes (donor-cited, CITATION-NEEDS-A-CELL on every value byte), then **per-site adoption under P-2 verbatim** — theme-context classification each site, no bulk sweep; a dark literal onto a themed token is the pinned-ground species in reverse. Sites inside frost/demo: same lane dispositions as §1. `#1A120E` in LIGHT_VARS + landing pins = ruled invariants, lawful exceptions, fenced in cells.

## 5 · CENSUS CLOSE + CORPSE DUTY
Re-run instrument at the delivery tip; closing amendment attributes every A/B/R1/R2/C kill; lawful exceptions stand (pin trio · Splash · `#25D366` 「 green it 」 · IG bytes). F-09.84 brass-alpha: COUNTED, UNTOUCHED (not this phase). Any primitive left consumer-zero after C retires by NAMED LINE (P-1(a) corpse duty) — at this seam all three primitives have live consumers, so expect zero corpses; assert it, don't assume it.

## 6 · BENCH + LEDGER
`tdw09_p2c.proof.mjs`: both-ways per adoption CLASS (one revert per class representative + the roster count cells) · six attributed reds fenced · floor byte-stable at every stated count. **The C handover doubles as Package 2's closing ledger**: phases A/B/C + R1/R2, findings .89–.92 + C's mints, the founder's open words (F-09.90 V3/V4 'Business' given · V5 peek-tip · his combined thumb walk slots incl. C④ pressed-FEEL on three screens — the 「 insensitive 」 funeral).

## 7 · SESSION LAWS THAT BIT THIS PACKAGE (inherit them)
Bare proof lines, never `| tail` before a gate (F-09.92) · `test -z "$(git status --porcelain --untracked-files=no)"` verify form, fail-closed single exit · never `git checkout` to restore your own cures — bank cured copies before both-ways · stripper trap: no `/*`-glob inside line comments · absent-subject ⇒ named-refusal reds (shim every bench read).
