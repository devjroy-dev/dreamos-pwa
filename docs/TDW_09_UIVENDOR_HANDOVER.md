# TDW_09 · UI VENDOR — HANDOVER

**Built at `dreamos-pwa` `503b254`** (twenty-fifth chair, UI relay #7).
Head-guard `"name": "web"` witnessed. `tsc --noEmit` exit 0.
This is a **rebuild**: a predecessor verified an identical-in-intent ZIP at `0be7370` and lost it with its container unpresented. Nothing below is carried from that session's narrative — every number was re-derived by command at `503b254`.

---

## 0 · WHY THE BASE MOVED

The charter named `0be7370`. `git fetch` put origin at **`503b254`** — one commit ahead, the founder's own: *TDW_10 F-10.91: the picker filtered an entitlement, not a purchase*. It lands **+25 lines on `app/vendor/settings/page.tsx`**, which is one of this delivery's eight files. Building at `0be7370` would have shipped a settings file that **reverted F-10.91**.

Surfaced before a byte was written (§0.2, report-never-adapt). **Founder ruled B-1 → (a): rebase the whole delivery onto `503b254`.** F-10.91's block sits at `TierPicker`; this delivery's two settings cures sit above it. No collision, and `tdw10_tier` reads 98/98 with F-10.91 in the tree.

This is F-09.125's lesson arriving a second time in one week, and the fetch-first law catching it: **an approval is against a tree, and the tree moves.**

---

## 1 · THE NINE FILES

| # | path | what landed |
|---|---|---|
| 1 | `components/vendor/Header.tsx` | **C2** — the popover Settings row. Label `Settings` · subtitle `Profile, billing, preferences` · glyph ⚙ borrowed from the More row · seated in **Atelier beneath Discover Profile** (Fork 2(a)). |
| 2 | `components/vendor/TipsCarousel.tsx` | Tips **4 and 5 retired** on in-file warrants; the **nine approved bodies** landed byte-exact; **tip 17 re-sectioned** `Studio` → `Settings` (Fork B(a)), killing the Studio pill. 25 → **23 tips**. |
| 3 | `app/vendor/tds/page.tsx` | **F-09.119** — FAB `F.display 25` in `54×54` → **`F.body 20` in `46×46`**, the census-derived house idiom, whole. |
| 4 | `app/vendor/more/page.tsx` | **F-09.120 arm (a)** — mode pill retired; **one merged list in Paper A's sequence**; four section labels removed-by-ruling; file-scoped `SectionLabel` dead. **F-09.122 cures by this.** |
| 5 | `app/vendor/settings/page.tsx` | **F-09.105** two live sub-floor sites → **16**; **F-09.121** stepper `−`/`+` pair whole → **`F.body 20`** (Fork 4(a)). |
| 6 | `scripts/tdw09_uivendor.proof.mjs` | **NEW** — 61 cells, 10 mutations. F-09.124's cure. |
| 7 | `scripts/tdw10_tier.proof.mjs` | **AMENDMENT, LABELLED** — the null-link cell re-aimed off the retired hand-mint sentence onto the picker. 97/98 → **98/98**. |
| 8 | `scripts/tdw09_p2_doors.proof.mjs` | **AMENDMENT, LABELLED** — §8.4 **inverted**: the Advisor chip is now asserted **absent** from More. 86/86 held. |
| 9 | `docs/TDW_09_UIVENDOR_HANDOVER.md` | this file. |

---

## 2 · THE TIP INVENTORY, AS BUILT

25 tips at base → **23**. Every tip not named below is byte-untouched.

- **RETIRED (2):** tip 4 `Just Do It mode.` (warrant **F-07.31**, cited in-tree at `app/vendor/page.tsx` — `justDoIt` retired with the CommandBar) · tip 5 `Quick action pills.` (warrant **F-09.54**; derived at this tip, the four pill bytes exist nowhere in `app/` or `components/` except inside the tip's own body — the tip was the only surviving witness to its own subject).
- **REWORDED (9):** 6, 7, 8, 10, 14, 17, 18, 24, 25 — founder-approved 「 all 9 yes 」, landed byte-exact from READ-FIRST №2 §2.
- **RE-SECTIONED (1):** tip 17 `Studio` → `Settings`. `SECTIONS` derives from the tips, and 17 was the sole `Studio` carrier, so the pill dies with the dead word. `Settings` already existed, so this **merged into a live pill and minted nothing**.
- **EXCLUDED (1):** tip 22, the peek nav — untouched, pending **F-09.90's V5 word**.
- **KEPT (13):** the remainder.

---

## 3 · THE PROOF

`tdw09_uivendor` **61/61 cured · 15/61 uncured** — 46 cure cells red by mutation of production code. All ten mutations **bitten from a fresh copy**, not reported.

The parser carries **M10's lesson structurally**: it anchors on the assignment bracket of `const TIPS: Tip[] = [`, never the first `[` after the name — which is the `[]` of the *type annotation*, closes after two characters, and returns an empty body every absence-cell then passes over. §0.2 and §0.3 refuse an implausible body rather than reporting a silent zero. **Nothing in this bench is line-anchored** (path-over-range).

§3 is **F-09.125's law as a cell class**: for each of the nine, the superseded byte must be *gone from the file whole*. An approved-copy batch that only asserts its replacement cannot tell a landed cure from a duplicated one.

Every uncured-green is a `[GUARD]`, a non-vacuity assertion, or a preservation cell (`§1.6` tip 22 untouched · `§6.4` the demo twin untouched · `§5.6` the More door kept).

### Floor, bare, at the cured tree

`type 16/16` · `doors 86` · `tier 98` · `retint 76` · `p2c 53` · `landing 103` · `shell 57` · `profile 48` · `portfolio 119` · `roles 131` · `palette 18` · `m3_report_chip 31/31` **read to a count, not accepted at exit 0**.
Attributed reds **unmoved**: `deck 191/193` (F-10.62) · `f0774_stripper 32/34` (F-10.49).
`tsc --noEmit` exit 0.

Both moved counts moved **because of this delivery and only because of it**: `type` 13/16 → 16/16 (F-09.105 cured), `tier` 97/98 → 98/98 (the labelled amendment). Both were re-derived at `503b254` *before* any byte landed, so the deltas are measured, not assumed.

---

## 4 · DISCLOSURES, BY NAME

**Four inherited, carried forward verbatim from the predecessors:**

1. **JSX comment in expression position** — seven `tsc` errors from a first F-09.105 comment. Caught by the gate.
2. **The parser opened on `Tip[]`** — the bracket walk found the declaration's bracket, closed after two characters, returned an empty body. A **silent zero**. Cured structurally here, not patched.
3. **§M.9 was vacuous twice** — `<VictorModeChipX` still matched `<VictorModeChip`; then `onThreadResetXX` still contained `onThreadReset`. Re-aimed a third time at removing the prop.
4. **A hand-rolled comment stripper** — exactly the naive rule F-07.74 condemns and `f0774_stripper` §6.3 exists to catch. Replaced with `scripts/lib/stripComments.mjs` plus the canary and the §0.Z INVOCATION marker.

**Five of this chair's own:**

5. **The base moved and I nearly did not check.** §0 above. The charter's base was stale by one commit and that commit touched one of my eight files.
6. **Apostrophe escaping cost two clean-tree resets.** My first tips patch hard-coded `\'` through three layers of quoting and missed on `Manish\'s` and `won\'t`. Replaced with a **line-anchored patcher that raises on a missed anchor** rather than skipping — the anchor *is* the current-string assertion. The failed run had already written `Header.tsx`, so the tree was reset to base and the whole apply re-run rather than continued.
7. **§5.4's row matcher was truncating on `=>`.** `<DItem[^>]*>` stops at the first `>`, which an arrow function supplies mid-handler — so the cell was reading half a row. It **failed**, which is the only reason I found it; had the truncated half happened to contain the token, it would have passed on nothing.
8. **§6.4 read the demo twin with a spaced regex against minified source.** `app/demo/vendor/[handle]/tds/page.tsx` is written without spaces (`fontFamily:F.display,fontSize:28`). The preservation cell failed rather than falsely greening — but a preservation cell that cannot see its subject is the same class as a silent zero.
9. **§7.2's absence cell was over-broad and convicted an innocent.** Unscoped, it caught the page-head back chevron `‹`, which shares the `A.brassWarm · F.display · 20` signature but is **not** in F-09.121's ruled pair. Re-scoped to the two stepper glyphs by their own characters. *(Sighted, not touched: that chevron is arguably F-09.119's class — a display-serif glyph — but it is outside the ruled radius and the founder has not reported it. Not filed; named here so the next reader knows it was seen.)*

**Two count divergences between the chair text and the tree — the tree is what I built to:**

- The inventory line reads **11 KEEP**; the tree carries **13** (25 = 13 keep + 9 reword + 2 retire + 1 exclude).
- The More ruling reads **nine rows kept**; the tree carries **8** (Couture, Featured, Team Hub, TDS, Contracts, Notes to Self, Settings, Sign Out).

Neither changes a byte — the ruling names *which* items move, and every unnamed row and tip is untouched. Both are the same class as **F-09.126**: a count carried rather than re-derived. Reported, not adapted around.

**One correction to the in-file record:** `app/vendor/settings/page.tsx` carried a note saying **three** sub-floor sites were F-09.105's. Re-derived by running `tdw09_type` at `503b254`, it reads **two** — `:363` at 13px and `:392` at 12px. The third had died under some earlier edit and the count was carried. The note is corrected in-file.

**One number confirmed, not corrected:** `f0774_stripper` reads **32/34**, not 33/35, on a pristine `503b254` checkout with none of my bytes in it. The predecessor's correction stands.

---

## 5 · THE REGISTER

Nothing new issued this sitting. `.128+` remains chair-free.

Riding: **F-09.118** (Settings unreachable from the coin + stale mini-manual) · **F-09.119** (bar-glyph FAB) · **F-09.120** (the forced mode pill) · **F-09.121** (stepper pair) · **F-09.122** (diverged chip mounts) · **F-09.123** (demo twin — FILED, cure HELD behind F-09.89's demo wall, **zero bytes**) · **F-09.124** (no instrument could redden a stale tip — cured by this bench) · **F-09.125** (an approved byte outlived its render site) · **F-09.126** (path drift in a census) · **F-09.127** (`SectionLabel` defined seven times in two incompatible signatures; the shared export already exists at `lib/vendor/studioShared`). **F-09.127 is NOT cured here** — the deletion in `more/page.tsx` is file-scoped and only file-scoped.

**C1 is DROPPED AS MOOT** (Fork A(a)): its render site was retired at `0be7370` while the approval was in flight.

---

## 6 · THE WALK CARD

The founder's four glances, re-run, plus two:

1. **Avatar → Settings.** Tap your initials. A **Settings** row sits in ATELIER, directly beneath Discover Profile, with a ⚙ and *Profile, billing, preferences*. It opens Settings. (More → Account → Settings still works too — kept by ruling.)
2. **TDS FAB.** It reads as a **`+`**, not a bar — and it is **visibly smaller** (54 → 46). **That is the cure, not a side effect.**
3. **More.** No Business/Advisor pill. **One list**, no brass section rules, in the old order: Couture, Featured, Team Hub, TDS, Contracts, Notes to Self, Settings, Sign Out.
4. **Settings → capacity stepper.** The `−` and `+` both read as real glyphs.
5. **Settings → billing copy.** The two small paragraphs are no longer sub-floor small.
6. **Tips scroll.** Avatar → Tips & Features. **23 tips where there were 25**, and **no Studio filter pill**.

---

## 7 · WHAT IS NOT IN THIS ZIP

- The **Package 2 thumb walk** — that is `docs/TDW_09_P2C_ZIP3_LEDGER.md` §8 and the founder's eyes, not this delivery's.
- **F-09.123** (demo twin) and **F-09.127** (SectionLabel duplication) — filed, uncured, by ruling.
- Any sequencing beyond this sitting. That is the founder's.
