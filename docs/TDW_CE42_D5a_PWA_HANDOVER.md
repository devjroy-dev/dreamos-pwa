# CE-42 · SEAT D2 · D5a (dreamos-pwa) — the button, the enquiry page, two lines and two lies

**Cut at** `cc109bd75aaf2f3413d153cb1403dddbc7d1d3b8`. No migration, no frame. `b20_a3` **180/180**, `tsc --noEmit` clean.

**What shipped:** F-42.1 — `/r/` strips the literal `{{1}}` Meta stored in the button base (both spellings; **no decode**, c-42.5 — a second decode would have thrown `URIError` on a review code holding a bare `%`), and `/r/` + `/e/` now stamp `tdw-build` (R-38.22). F-41.156 — `/e/` reads `GET /api/v2/public/enquiry/enq-<8hex>`, renders Service · City · Month · Budget, every value HTML-escaped, on the public light ground with `noindex` in the page **and** in `robots.ts`. F-41.144 — the ninth flag gets its two vetoed lines in `GATE_COPY` **and** its room in `ROOM_OF`. F-41.145 — the note announcing a duplication E2 (iv-b) retired, and the frozen row arithmetic beside it, both gone.

**On the glass:** the outsider taps **Visit website** and lands on her enquiry, not the review sentence. The Switchboard's Concierge room gains *Tell a vendor a concierge forward landed*, still **Off**.

**Not proven:** the tap itself — only the founder's handset witnesses the Meta button resolving. `next build` is his gate (R-40.66); the seat's container cannot fetch Google Fonts. **Owed, not cured:** `/e/`'s vendor-join CTA and the lead materialising by last-ten on join — declared in that file's header since D2 and still owed. `app/v/[code]/page.tsx:177` spells its build fallback `??` and carries the same empty-string hole cured here; out of radius, named so it is not lost.

**Next:** D5, the public intake `/plan` (R-41.94) — its read-first is answered and holds on eight rulings.
