# CE-41 · SEAT D · D3 (dreamos-pwa) — the tick · HANDOVER

**Cut at** dreamos-pwa `7b76858c75c5c6217bb59abd0c3ce8df2e2607e6`.

## ⚠ 0 · APPLY D3a5 (dream-os) FIRST, OR THIS SHIPS AN INVISIBLE ROW

The queue's rows come from the **read** door, which hydrated prospects without the consent columns — so `f.consent` is `undefined` on every row and *Consent noted* can never render. **F-41.104**, cut as **D3a5** at `e660d0e`. It is F-41.103's class one door over: the send path was cured, the read path was not.

## 1 · One optional tick (R-41.133 / R-41.135)

Above **Forward**, because it is something he does *before* sending, not a setting. Unticked forwards still send and carry no record (R-41.132).

Three vetoed bytes, **one home apiece** at the top of the file:

| | |
|---|---|
| label | *I asked her for this number and she said yes.* |
| stored | *Founder asked her for this number and she gave it and agreed to be messaged.* |
| source | `founder_attested` — the value `0157` admits |

**The label and the sentence say the same thing on purpose.** The first draft's label promised only consent while the record claimed both limbs; the chair vetoed them together so the founder is never ticking something narrower than what gets written under his name. Both name limb (a) — she **gave** the number — and limb (b) — she **agreed**.

**The sentence is TDW in the third person.** Never her words from a tick: that was the boolean `0156` refused, wearing her name.

**An untouched box sends nothing at all** — not an empty string, which would read as a record that says nothing.

## 2 · One word, or silence

*Consent noted* when `consentState` ≠ `none`; **nothing otherwise**. No caution ink, no refusal.

**A consequence worth naming:** with no paste box, `her_words` is unreachable from the UI — SQL is its only writer — and the queue renders it identically to `founder_attested`. That reads coherent rather than wrong: **the queue is for the founder, who needs to know a record exists; the column is for Meta, where the distinction still stands.**

The row reads `consentState` and **never** re-derives from `consent_source`. A cell asserts the absence of `consent_source ===` anywhere in the page.

## 3 · One seat A cell re-cut

It pinned `forwardToProspect(item.id, { phone: phone.trim()` **on one line**, and the call is multiline now that the tick adds conditional fields. **A cell that breaks on a line-wrap is asserting formatting, not behaviour.** Whitespace-tolerant now, still asserting what it means.

## 4 · Floor

`b20_a3` **GREEN 127/127** cured, **RED 119/127** uncured. `tsc --noEmit` clean. **`npm run build` is the founder's gate** — this container cannot fetch Google Fonts (F-40.134).

**Range F-41.105–F-41.107 unspent.**
