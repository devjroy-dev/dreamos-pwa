# CE-41 · SEAT D · D3d (dreamos-pwa) — the chip, the edge, the seam · HANDOVER

**Cut at** dreamos-pwa `30e3103e877953a229e0cec4b042187eaa514760`.

## ⚠ 0 · APPLY THE DREAM-OS HALF FIRST

The room comes down on the done event, the JSON reply and the history rows — all three land in D3d dream-os. Until that half is live every message reads `null` and the thread renders **unmarked**, which is B3's shape and not a lie, so nothing breaks. But the seam cannot draw.

**The relay's base was stale.** R-41.139 is **pushed** — it is the pwa tip `30e3103e`, all three props removed. The relay's `256d3eb+` and *"unpushed"* were both against the tree (c-41.88, the chair's).

## 1 · The three parts

**The chip** at the thread's head, from the current assertion, reading the same literal the `useChat` call passes — never a second constant, so it cannot outlive the assertion.

**The edge** is the bubble's **own hairline, recoloured**. `MessageBubble` already draws a left rule at `left: 4`; a second element on one edge is two things that can disagree. When the message was answered in the advisor room it thickens to 2px and takes the accent, so **the room reads on every reply**, not only at the seam.

**The seam** where consecutive answered rooms differ — teal into Advisor, card-border grey into Business, a rule line and never a bubble, the same shape D-7's fresh-thread seam already uses.

## 2 · Three things the seam gets right, each because it could have gone wrong

**It walks back, never `idx-1`.** Only assistant messages carry a room; a naive adjacent compare would draw a seam at **every user turn**.

**It draws from the messages' own fields**, never a pathname and never the request's assertion — R-41.141's rule carried. The assertion is what was *asked for*; a seam must mark what **happened**, or it would draw on a turn the engine had overridden.

**`null` draws nothing.** Consult is unmarked, never folded to `business` — business is the one room carrying the estate, and labelling a consult turn as business would have the glass assert the estate was somewhere it was deliberately kept out of.

## 3 · `advisorThreadNote` retired, with its reason

It said *"Moving between Advisor and the ask bar starts a fresh conversation each time."* That stopped being true the moment one thread crossed rooms: both surfaces now read the same conversation, and the seam marks where the room changed rather than where a thread began. **A note describing the old behaviour is worse than no note.**

## 4 · Floor

`b20_a3` **GREEN 141/141** cured, **RED 131/141** uncured. `tsc` clean.

**The build is the founder's gate:** 16 font-fetch failures here, zero type errors — F-40.134. **An earlier run reported exit 228 with zero font failures; that was the container out of disk**, not a result. Re-run after reclaiming it. A verdict from a full disk is neither green nor red.

**Range F-41.106–F-41.107 unspent.**
