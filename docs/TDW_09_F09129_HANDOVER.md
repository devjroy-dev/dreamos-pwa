# TDW_09 · F-09.129 — THE MODE CONTROL, RE-HOMED

**Built at `dreamos-pwa` `4517daf`.** Head-guard `"name": "web"`. `tsc --noEmit` exit 0.
Fork A → **(a)**, Fork B → **(b1)**, founder-worded 「 your recomendation 」.

---

## 1 · WHAT WAS RULED, AND WHY IT IS NOT A RETIREMENT

The founder's original word was 「 the mode pill**s** looks forced and out of place. remove it 」 — **plural, and naming no surface**. F-09.120 read it as the More mount alone and retired only that one. He walked the result at `thedreamwedding.in/vendor` and asked again: 「 ddnt we decide to retire this advisor business pill? 」

His plural governs (Fork A(a)). But the verdict was on **placement and register**, not on the capability — and the surviving chip is not chrome:

- truth lives at `engine.agents.victor_mode`, server-side (chair-derived: `loop.ts:232/:261`, `vendorMode.js` the sole door)
- written via `PATCH /api/v2/vendor-e/mode`
- a flip returns `thread_reset` and **resets Victor's thread**
- `useVictorMode` holds **no localStorage** by design — a stale local copy would lie about which room Victor is serving

Retiring it on a chrome complaint would strand a live server field with no vendor-reachable door — F-09.125 inverted, killing a mechanism because its presentation was convicted. So: **re-homed, not retired** (Fork B(b1)). Nothing server-side moved.

## 2 · THE SIX FILES

| path | what landed |
|---|---|
| `app/vendor/page.tsx` | The Hub masthead mount **removed**. The chip **re-mounted inside `{risen && (`**, directly above the room label it publishes. Wired identically: `onThreadReset={markFreshThread}` + `onMode={setVictorRoom}`. |
| `components/vendor/BottomNav.tsx` | Its 「 the ONE mode control 」 comment kept — **address updated** per the mechanism-comment law. |
| `scripts/tdw09_uivendor.proof.mjs` | §8.2 **amended** to the new seat; new **§10** (7 cells) + **§M.13/§M.14**. 67 → **76**. |
| `scripts/tdw09_home.proof.mjs` | §12's pill cell **inverted, labelled**. Count preserved **67 → 67**. |
| `scripts/tdw09_p2r1.proof.mjs` | §2.2 **re-aimed** to the publisher at its new seat. Count preserved **13 → 13**. |
| `docs/TDW_09_F09129_HANDOVER.md` | this file. |

## 3 · THE SEAT, AND WHY THAT SEAT

Beside the masthead that already prints `Business` / `Advisor` / `Chat` — because that label **is a read-only mirror of this very control**, published by the `onMode` above it. Control and mirror now share one block: the switch and the room it selected are visible together, and they cannot disagree — one hook call, one truth.

`doors 3.8`'s 「 one surviving mode control 」 stays **true** with its address updated. `home §12`'s cell **inverts** rather than dies: it used to assert the chip mounts *above* the rise so the rise could not swallow it. The intent was always *reachable from inside the chat*; the control is now not near the chat but **in** it. An unguarded absence there would let a future edit put the pill back on the Hub silently, so the cell guards the absence instead.

## 4 · ONE BEHAVIOUR DELTA, NAMED IN-COMMENT

The chip used to mount on every Hub load, so `victor_mode` was read once at page open. Mounted inside the risen branch, **it reads on each chat open instead**. Safe only because `useVictorMode` re-reads server truth on mount and caches nothing — **if that hook ever gains a cache, this seat is the thing to re-read.** Until the first read lands the mirror says the standing `Chat` byte, exactly as it always did while a first read was in flight. Written into the mount comment (F-06.85), not left to this document.

## 5 · PROOF

`tdw09_uivendor` **76/76 cured · 74/76 with `app/vendor/page.tsx` reverted alone.**
`tdw09_home` **67/67 · 66/67 uncured.** `tdw09_p2r1` **13/13 · 12/13 uncured.**
Both-ways as ruled: **§10.1 (no pill on the Hub masthead) reds at the uncured tree**, and **§M.14 proves the re-homed control is wired** by removing a prop and watching §10.3 bite.

**Floor, bare:** `type 16` · `doors 86` · `tier 98` · `retint 76` · `p2c 53` · `landing 103` · `shell 57` · `profile 48` · `portfolio 119` · `roles 131` · `palette 18` · `m3 31/31` read-to-count. Attributed reds unmoved: `deck 191/193` (F-10.62) · `f0774 32/34` (F-10.49). `tsc` exit 0.

## 6 · COPY INVENTORY — **ZERO**

No new byte reached a vendor. The chip carries its own founder-vetoed pair (`Business` / `Advisor`) and travelled with them; the mirror below still speaks the same two words plus the standing `Chat` fallback. **§10.6 asserts it** — scoped to `SEGMENTS`, exactly two words, in order. No veto slot is owed.

## 7 · DISCLOSURES

1. **§10.6 convicted an innocent on first run.** An unscoped `label: '` sweep over `VictorModeChip.tsx` caught the *font token* `F = { label: 'var(--font-label, inherit)' }`. A copy cell that cannot tell copy from CSS is not a copy cell. Re-scoped to the `SEGMENTS` array. Same class as §7.2 earlier in this sitting — **that is twice in one sitting for over-broad absence cells, and it is the pattern I am carrying forward.**
2. **§8.2 was mine to amend and I said so first.** I shipped the cell asserting Home's mount survives, and named it in the F-09.129 packet before the ruling came back. Amended, credited, not quietly rewritten.
3. **Adjacency is a founder glance, not a byte.** The chip and its mirror now sit one above the other, and the mirror restates the active segment. That is the ruling as worded (the mirror cells were re-aimed, not retired) and I did not adapt around it. If it reads as duplicative on the walk, that is a copy ruling and zero bytes were spent presuming it.

## 8 · WALK CARD

1. **Hub.** Open `/vendor`. **No pill under the masthead.**
2. **Chat.** Tap into the chat so the room rises. The **Business · Advisor switch is there**, above the room label — and **flipping it works**: the label follows, and the thread resets as it always did.

Register: **.129 spent · .130+ chair-free.** Sequencing is the founder's.
