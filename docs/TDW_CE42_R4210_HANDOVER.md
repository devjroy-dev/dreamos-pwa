RELEASED 2026-09-10 by seat D3 s2, re-pinned 893c3136 -> f538d984. Its condition
arrived: `/plan` landed and was walked green at f538d984, so the link no longer
points at a route that 404s. The HELD preamble is REPLACED rather than left
standing, because a withheld block whose condition has been decided is no longer
withheld — it is dead ink that reads as a plan, which is F-42.55's own lesson.

TWO CHANGES AT RELEASE, BOTH D3 s2's AND NEITHER E3's: the bench is renumbered
b70 -> b72 (F-42.156 — 70 and 71 were taken by the two R8 packets while this one
sat held), and this preamble. EVERY OTHER BYTE OF THIS DOCUMENT IS E3's, UNTOUCHED
— including §5's floor, which is a record of THEIR cut at 893c3136 and is not a
claim about this one. The floor at the release cut is in D3 s2's own handover
beside this file.

CE-42 · R-42.10 · THE /plan ENTRY ON THE LANDING (dreamos-pwa, base 893c3136) · [CE-42 LE-E3]

1 · WHAT SHIPPED. One line on the entry screen, in seat A as ruled: the two doors,
    then `New here? Sign up`, then `Not ready to sign up? Tell us what you need →`,
    then the legal row. It is an ANCHOR at `/plan`, not a third door — a real href
    rather than a router push, because nothing on that screen holds a session yet
    and a plain link is what a crawler follows and what a long-press copies.
    `b20_a4`'s element census is amended BY LABEL 30 → 31 with a second cell
    pinning the delta to anchors alone. `b70` is new: twenty-six cells and eight
    mutations, each RED at the cured tree.

2 · PARITY IS BY CONSTRUCTION, NOT BY COMPARISON. The founder's rule was that the
    two lines differ only in the bytes of their words. That could have been two
    matching inline blocks and a cell comparing them; it is ONE object instead —
    `ENTRY_LINE` and `ENTRY_LINE_VERB`, hoisted beside `GLASS` and `INPUT` on this
    file's own idiom, named twice each and spread nowhere. Parity asserted by a
    cell can drift for a day before anyone runs the cell; parity by a shared
    identifier cannot drift at all. `b70` C3 asks whether both sites name the SAME
    identifier, which is a question a copy-paste can never pass, and one of its
    mutations is a spread-with-override — compliance in shape only.
    The values are `Sign up`'s own, MOVED and not retyped, margin-top 16 included.
    No ink is minted: this surface predates the token layer and its literals are
    HELD by R-O6, because there is no ThemeProvider anywhere in the route group.

3 · ONE ARM, AND THAT WAS DERIVED RATHER THAN CHOSEN. The frame was asked for in
    both arms. There is no second arm to draw: `app/(landing)/layout.tsx` is a bare
    passthrough, no light theme can reach this surface, and the theme-blind surface
    census holds fifty near-white literals on exactly that mechanism. Inventing a
    CHALK palette here would have created the defect that census was built for.
    Chair accepted at the veto.

4 · THE FIRST VETO RESURRECTED A KILLED BYTE, AND THE ROSTER CAUGHT IT.
    `Planning a wedding` is entry 11 of `tdw09_landing` §8.1's DEATH ROSTER — the
    bytes TDW_09 killed, with a cell that reds by name if one returns. It died as
    the invite-era role label, against `A wedding professional` at entry 12. Filed
    under §0.2 before a byte was written; the chair RE-VETOED rather than striking
    the entry, so all thirty-nine roster entries still hold and the guarantee is
    intact. The replacement was run against every entry before it was written, and
    `b70` C2 keeps that check pointed at the new line — it reads the roster from
    its own home, asserts entry 11 is STILL ON IT, and re-runs all of it.
    e-1, mine: the first cut of C1 policed apostrophes and went RED on correct
    code, because the straight quotes it found were the JSX separator the Sign-up
    line has always used. Prose bytes and syntax are two different things, and
    `b40` C102 already owns the prose rule. Re-cut as a parity cell on the idiom.
    e-2, mine: the first cut of the comment block spelled element names in angle
    brackets. `b20_a4`'s census counts raw text and does NOT strip comments, by its
    own e-8 lineage — the count went to 34 with the button pair split 19/17, which
    is precisely the failure e-8 exists to catch, arriving from the one direction
    nobody watches. Both comment blocks now carry that as a standing rule on this
    file.

5 · THE HASH. Base 893c3136f511f66e54e90854536cfab83993aaf4, re-derived by
    `git fetch -q origin && git rev-parse origin/main` at the cut, not carried.
    b70 26/26 · b70 --mutate 42/42 (eight mutations, each RED, tree restored
    byte-for-byte) · b20_a4 76/76 · b20_a3 203/203 · b69 71/71 · b69 proof 42/42 ·
    tsc --noEmit exit 0. Census at the cut: button 17/17 · input 4 · a 3 ·
    BackBtn 4 · GoldBtn 3 = 31.
    ⚠ NO BUILD NUMBER IS CLAIMED. `npm run build` was RUN and fails in this seat's
    container on `app/layout.tsx`'s four `next/font` families — the container
    cannot reach fonts.googleapis.com, so the compile dies before it reaches any
    file this packet touches. THE GATE IS THE FOUNDER'S RUN.
    DECLARED, INHERITED, NOT CURED (F-42.94 and one more): b40 FLOOR RED 2
    (C50, C102), b42 175/176 (C7), and tdw09_landing 99/100 (§2.3, a provisioning
    failure reported as exclusivity) are all red at 893c3136 before this packet
    touches anything, and are red after it by the same cells and the same counts.
