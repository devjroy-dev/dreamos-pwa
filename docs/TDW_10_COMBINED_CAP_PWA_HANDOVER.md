# TDW_10 · F-10.100 — THE COMBINED AI CAP · ZIP B (dreamos-pwa) · EXECUTOR HANDOVER

**Built at:** dreamos-pwa `97b8b8f` · **dream-os companion:** ZIP A at `8ce5422`
**REBASED, and the rebase is the point.** Cut first at `1959023`/`556e164`; both origins moved before the founder applied. `scripts/tdw10_tier.proof.mjs` **collided** — THE BILLING TAB re-aimed it to `components/vendor/SubscriptionCard.tsx` in the same window I amended it. Applying the first cut would have wiped that re-aim: the F-09.128 collision class, live. Merged and verified cell-by-cell — all four of the billing sitting's markers (`CARD` read, the `every()` guard, `strip(CARD)`, and the un-re-aimed `strip(SETT)` anchor cell) confirmed present in the shipped file, alongside my four.
**Rulings built under:** CE R-26.7 §C — **F-4 arm (β)**, the console retirement, **M-1 refused-to-widen**
**Sequenced BEHIND ZIP A.** ZIP B renders a control that the server's `meta` must already carry.

---

## 1 · WHAT SHIPPED — FOUR FILES, TWO OF THEM BENCHES

| File | State | What |
|---|---|---|
| `app/vendor/page.tsx` | MOD | **F-4 (β)** — the Upgrade affordance, seated at page level, gated on `state === 'capped'`, beside the disabled input. |
| `app/admin/config/page.tsx` | MOD | Two vendor cap groups retire into one `Vendor AI` group on the `vendor_ai_*` family. Couple groups byte-untouched, with their reason in-file. |
| `scripts/tdw07_p1_discover.proof.mjs` | MOD | **Labelled amendment**, 43 → 44. |
| `scripts/tdw10_tier.proof.mjs` | MOD | **Labelled amendment**, 98 cells, four re-aimed. |

**`components/vendor/TierMeter.tsx` is BYTE-UNTOUCHED**, and that is the ruling rather than an omission — see §2.

---

## 2 · THE CONTROL INVENTORY — ONE CONTROL, MOVED, NET ZERO

The estate's only Upgrade control lived **inside** `TierMeter`, behind that component's own guard: `if (!meta || !meta.turns_cap) return null`. A cap of zero is falsy. So at precisely the tier that most needs a way to buy AI, the meter returned null, the link inside it never rendered, and `page.tsx:1165` disabled the input. She was refused, and handed no path from the place she was standing.

| Control | Disposition |
|---|---|
| `TierMeter`'s Upgrade anchor | **KEPT** — still renders at `nearing` and at a spent **nonzero** cap. That is a meter reading, and a vendor watching her allowance run down should be able to act on it. |
| The page-level Upgrade anchor | **ADDED** as the same control's second seat, reachable when the meter cannot render |
| The 0/0 progress bar | **REMOVED-BY-RULING** — arm (α) was refused |
| Every other control on the page | **KEPT**, byte-untouched (`tdw09_uivendor` 76/76, `tdw09_home` 67/67) |

**Arm (α) — relaxing `TierMeter`'s guard so a `0/0` bar renders — was refused on its own ground:** a progress bar reading zero-of-zero says *unused*, which is the same lie this sitting is retiring, set in a different font. `TierMeter` stays a usage widget and keeps its right to hide when there is no usage to show.

**And the seat is durable rather than convenient.** A sale living inside a usage widget is hidden again by the next person who tidies the widget. This one sits next to the thing that just stopped working, which is where a person looks.

**The href is the server's own** (`meta.upgrade.href`). This surface mints no destination.

---

## 3 · THE CONSOLE — F-10.87 RESOLVED BY RETIREMENT

`Vendor WhatsApp` had **zero readers** (two independent methods: literal grep across `src/`, and an interpolation sweep finding exactly two config keys in the whole tree). `Vendor PWA` was read, but named for one lane while governing an allowance both lanes spend. The screen's header promises 「 changes take effect immediately 」; for eight of those sixteen dials the promise was false, and the founder-witnessed victim was the founder.

Both retire into one group on the ruled `vendor_ai_*` family. **The precedent is this estate's own:** the Appearance swatches were removed *whole* rather than wired, because a switch on a pinned reader lies (CE-209). A dial nobody reads is that switch.

The old rows stay in the database — deleting config is destructive and reversible only from a backup — but they are no longer **offered**. The value stays legible; it stops pretending to be a control.

**Group label, OWED-VETO:** 「 Vendor AI — app chat and WhatsApp share one allowance 」. Admin-console chrome on the founder's own screen, shipped as my proposal; one word changes it.

**THE TWO COUPLE GROUPS ARE UNTOUCHED, DELIBERATELY.** They also have zero readers — `src/api/couple/chat.js` reads no cap, counts no turns and refuses nobody — and they keep promising immediate effect. **Retiring them was not ruled.** Widening a ruling to cover what it did not name is an executor deciding what the founder meant to say. `§3.5b` now asserts their survival **by name**, so this delivery carries the evidence that it did not.

---

## 4 · THE PROOF

- **`tsc --noEmit` → 0 errors**, run on a cleared `.next` with `node_modules` installed.
- **`tdw07_p1_discover` 44/44** cured · **43/44 at pristine surfaces** — the amendment reddens.
- **`tdw10_tier` 98/98** cured · **94/98 at pristine surfaces** — four cells red.
- **FLOOR, unmoved:** `tdw09_uivendor` **76/76** · `tdw09_home` 67/67 · `tdw09_money` 18/18 · `tdw09_hotfix` 38/38 · `tdw09_p2c` 52/52 · `tdw08_console` 55/55 · `tdw09_type` 16/16 · `tdw10_p1_shell` 57/57 · `tdw10_p2_retint` 76/76 · `tdw09_landing` 103/103 · `tdw07_f0784_panel` 34/34 · `tdw09_frost_parity` 75/75.
- **Attributed reds reproduced UNMOVED:** deck **191/193** · `f0774_stripper` **32/34** (settled at R-26.7 §D; not re-litigated).

---

## 5 · THE TWO AMENDMENTS, LABELLED

**`tdw07_p1_discover §3.5`** asserted four token-cap groups survive the Discover ranking group's arrival. It is a **blast-radius cell, not a census of the console**. The property is unchanged; the console moved by ruling, 4 → 3. Split into §3.5 (the vendor groups are now one) and **§3.5b** (the couple groups survive untouched), so the cell now carries the *refusal to widen* as evidence rather than as prose.

**`tdw10_tier`'s four cap-editor cells** pinned the `_basic` keys 0115 seeded, because a dial naming an unseeded key saves into nowhere — the PATCH route 404s on a key with no row. Offering the old keys now would be **the same defect pointed the other way**: a dial whose key no longer reaches the reader. Re-aimed to the ruled family; the negatives are carried in the same cells.

Both amendments were witnessed RED against the pristine surfaces before being trusted.

---

## 6 · WHAT THIS DELIVERY DID NOT DO

- `TierMeter.tsx` — zero bytes, by ruling.
- `app/vendor/settings/page.tsx` — zero bytes. The picker's gate is untouched; **it was already reachable by navigation** (`Header.tsx:232`, `more/page.tsx:143`), which is the correction to F-10.100's (c) limb that the chair withdrew and re-scoped.
- The couple console groups — zero bytes, above.
- **The destination divergence is NOT reconciled.** This surface sends her to `/vendor/settings#tier`; the WhatsApp refusal sends her to **Billing**. Filed for the chair. Re-pointing a live control is a ruling.

---

## 7 · THE FOUNDER'S WALK

Sequenced **after** ZIP A and after the `0116` block. Precondition, founder-witnessed: `9888294440` on tier `basic`, its `vendor_ai_daily_basic` at `0`.

The card is authored from his pasted SELECT rows and ships in its own message — never before them.
