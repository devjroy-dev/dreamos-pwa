# CE-41 · SEAT D · D3c pwa — the link, the cap's question, two refusals

**Cut at** dreamos-pwa `727090ad1947d2ccff643393c2f08ba2b263ef89`.

**Apply D3c3 (dream-os) first** — `wa_link` is built server-side; the link line hides without it.

## What's here

**The link line** — *"Or send her this link"* with a Copy button under the outsider form. The queue copies; it never builds a `wa.me` URL (the number is a server env).

**The cap asks** — `fanout_reached` now renders a question with **Forward anyway** / **Not now**, on both the vendor and outsider arms. `confirm` is sent only on the retry he asks for.

**Two refusals got words** — `fanout_reached` and `already_a_vendor` were a bare 409 with an unchanged sheet. Both fold into the **existing** `REFUSAL_WORDS` map, and for these two the **server's sentence wins** because it names the handle and the count.

## What's not here, and why

**The Switchboard restrictions row has no data source.** D3c3 logs `account_update` and acts on nothing by design — there's nothing for a row to read. It needs a store, which is a migration. **CE-42.**

**F-41.152 is already built** — `TypedIntake`, a "Type a request" sheet wired to `createAssistanceTyped`. I filed it against a grep for the wrong symbol. **Withdrawn.**

**F-41.144/.145 are on neither tree.** One line from the chair and they ship next.

## Floor

`b20_a3` **150/150**, `tsc` clean, build fails only on 16 font fetches. Two sealed cells moved: the vendor-call cell pinned the old signature, and R-41.101's key-set cell caught `T.surface` as a new colour key — dropped.

## Walk

Open an item at its cap → forward → **"This is 4 of 3 for a photographer. Forward anyway?"** → tap it and the send goes. Forward to `9888294440` → **"already on TDW as @DEV440"**. Open the outsider sheet → the link line with Copy.
