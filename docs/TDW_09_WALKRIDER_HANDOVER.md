# TDW_09 · THE WALK RIDER — F-09.85 / .86 / .87 · EXECUTOR HANDOVER

**Session:** the P1 executor, continuing under the chair's rider charter (founder word 「 Now and a. 」).
**Repo:** `dreamos-pwa` ONLY. **Base:** `d132fbf` (P3 rider 5), re-fetched at packaging, unmoved.
**Shared-ground proof at the actual tips:** P3's rider 5 touched `app/vendor/discover/page.tsx` at hunk `@@ -306` (the state rendering); the hero overlay this rider cures sits at `:128–:155`, byte-unmoved by P3 — non-overlap derived, not assumed. This ZIP bases on `d132fbf` and applies clean over P3's bytes.

## 1 · THE FOUR CURES

**F-09.85 — the Discover photo gate joins the-landing-is-the-law.** The headline read `var(--atelier-ink)` — themed — over a pinned dark scrim, so Paper rendered near-black on near-black (founder-walked invisible; the pinned-cream tagline beneath it was the pattern). Headline pinned `#F0E6D2`, eyebrow pinned `#C9A84C` (the dark theme's own rendering; ≈7.8:1 on the scrim, label use — F-09.3's gold-never-body law untouched), R-M6 mechanism comment in-file. The tagline's literal unmoved — it was never a patient.

**The vetoed copy, founder word (a), verbatim:** 「 Appear before couples planning their wedding. 」 replaces 「 Appear before qualified brides. 」 whole. **The reasoning, riding here as chartered:** 「 qualified 」 promised lead-vetting the product does not perform — the curation is of *vendors*, at the Discover approval queue, never of the demand side; and the demand side is *couples* everywhere in this product. The old line was the honesty bound breached on a vendor-facing promise.

**F-09.86 — the Masthead sub-line takes the body voice.** Authored as Jost 10 (a whisper); T-1's floor raise moved it to 16 and Jost-at-body-size read wrong. Now `F.body` (DM Sans, resolved on the shared `SliceRow` const — verified present, since a missing font key is a silent CSS no-op). Both drifted 「 Jost 10 」 citations amended in the same edit (F-10.34's citation class).

**F-09.87 — the leads page's gold inks themed; the ◆ plug wears the page.** The three `rgba(201,168,76,0.7)` ink sites (`The Returns` · the ledger labels · `Last Thirty Days`) → `A.brassWarm` (`--atelier-label`: `#E0BC6E` dark / `#7A3828` light). The ◆ plug's pinned `#1F1612` → `var(--atelier-page-bg)` — it masks the border behind the glyph and must wear the *page's* ground. **The hairline/wash family (0.04–0.4 borders and washes) deliberately REMAINS** — F-09.84's queued class, per the ruling's per-site scope; bench §4.2 asserts they stand, so an over-deletion reddens. The F-09.84 entry gains this rider as its evidence-of-damage cross-reference. **Dark-theme note, stated:** the three cured eyebrows render slightly brighter on Espresso (`#E0BC6E` vs the old composited ochre) — a lawful delta under the filed finding, on the founder's re-shoot to accept.

**DISCLOSED EXTENSION (ratify-or-revert, labelled in-file):** the same walked screen carried one more broken ink outside the ruling's 「 gold-alpha 」 wording — the insight line's pinned-cream `rgba(240,230,210,0.78)`, cream-on-cream on Paper. Cured to `A.inkSoft` so the smoke's 「 returns legible 」 bar can pass whole. The revert is one literal.

## 2 · PROOF

`tdw09_walkrider.proof.mjs` **15/15** at cured. Both-ways, each file reverted ALONE (restore-then-revert — my first loop accumulated reverts and proved nothing per-file; discarded, disclosed as D-1): discover → **5 RED** (§1.1–1.3, §2.1–2.2) · leads → **5 RED** (§4.1/.3/.4, §5.1/.2) · Masthead → **2 RED** (§3.1/.3). Guards (§1.4 tagline-unmoved · §3.2 font-resolves · §4.2 hairlines-remain) green at both tips by design.

**`tdw09_roles` 130 → 131, LABELLED AMENDMENT** (CE-199 precedent): cell ④ measures literals against the themed page grounds and correctly convicted the new pinned gate inks — the pin-trio's whole-file exemption model cannot carry a *partial* surface, so ④ gains **per-site scrim exemptions that must prove their property**: each pair is skipped only while the pinned scrim AND the F-09.85 comment both stand, and a new companion cell reddens if the scrim dies while the exemption survives. **Mutation-proven:** scrim neutralised at the cured tree → 2 RED (④'s survivors + the companion); restored → green. First cut of walkrider §3.3 convicted my own amendment's epitaph — re-aimed at the two dead *claims*, the correction earned in-cell (D-2).

**The floor whole at the cured tree:** `home 67 · landing 98 · type 16 · surface 51 · roles 131 (labelled) · money 18 · palette 18 · theme_retire 16 · p3_landing 89 · console 55 · factory 45 · invite_spent 14 · prospects_console 54 · shell 53 · bridge 44 · retint 76 · deck 192 (P3 rider-5 growth at my true tip, derived) · p4b_probe 35 · p4b_slice1 30 · p1_canon 24 · walkrider 15 NEW` · tsc 0.

## 3 · COPY INVENTORY
One vendor-facing string changes, **founder-vetoed (a) verbatim**. Zero other strings. No persona names; money register expected-zero, confirmed; W-1 clean.

## 4 · FOUNDER SMOKE — three glances on Paper (Light theme)
| # | screen | look at |
|---|---|---|
| 1 | `/vendor/discover` | the headline reads — 「 Appear before couples planning their wedding. 」 legible over the photo |
| 2 | `/vendor/list/leads` | the sub-line (「 enquiries · across 1 open 」) in the body voice, not the old font |
| 3 | `/vendor/discover/leads` | the eyebrows and figures legible, no dark smudge under the title, the insight line readable |

Espresso stays your call on glance — the three eyebrows on screen 3 render slightly brighter there (the lawful delta above).

## 5 · DISCLOSURES
**D-1** — the first both-ways loop accumulated reverts (each iteration stacked on the last) and proved the all-reverted state, not per-file isolation; caught by reading my own FAIL counts, re-run restore-then-revert. **D-2** — walkrider §3.3's first cut read epitaphs as claims and convicted my own amendment comment; re-aimed at the dead claims, correction labelled in-cell. **D-3** — the roles-bench collision was found by the floor run, not predicted at build; the amendment shape (conditional per-site exemption + companion cell) is proposed for ratification with the mutation proof above.

## 6 · OPEN
The chair's seal band carries P1 + this rider + 「 green it 」 + dispositions .77/.82–.87 as one story, per the relay. F-09.84 keeps its queue with the new evidence clause. The disclosed extension and the roles amendment await ratify words. Next free finding number derives at the chair's desk (.88 per the relay, re-derive at origin).

Sequencing beyond this sitting is the founder's.
