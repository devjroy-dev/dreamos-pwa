# CE-42 · SEAT D2 · D5b (pwa) — R-42.7 at the form, F-42.64 at the queue

**Cut at** `e5b3b23e28f2883ddf9973efafbacbf68daad0f9`. `b20_a3` **190/190** (base 180), `tsc --noEmit` clean. One source file.

**R-42.7 — the form will not file without a city.** The City field carries its marker (*Needed*) and the submit guard reads `!city.trim()`, so a city of one space is not a city. **This is the courtesy, not the rule** — the rule is the door byte in D5b (dream-os), which must be applied for the guard to mean anything to a caller who is not this form.

**F-42.64 — the refusal is read, not caught.** The founder walked D5's city gate twice, saw nothing, and found the reason in a Railway log: `Toast` lives three seconds at `bottom:76px`, which in a 374 frame lands on the outsider sheet's own link row. The cure is not a louder toast — the request header already prints *city not given*, so **the outsider door does not open**, and the reason sits still beneath it. The toast stays as the backstop for the race the disable cannot cover (the city blanked between load and tap), with `no_city` in `SERVER_WORDS_WIN` so the founder's own byte renders by rule rather than by the local map being empty. **The TDW arm is untouched:** F-42.58 gates the outsider arm only, and gating both here would put a rule on the glass the door does not hold — a cell reds if it ever does.

**Not proven:** nothing. Eight production mutations, all red, plus a no-op control green; `next build` is the founder's gate (R-40.66). One duplicated comment line at the outsider sheet's foot removed in passing.

**Next:** `/plan` — the sheet extraction, `lib/auth/otpSignup.ts`, the layout branch, the sitemap. Frame v2 is vetoed and waiting.
