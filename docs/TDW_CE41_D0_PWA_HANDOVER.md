# CE-41 · SEAT D · D0 (dreamos-pwa) — the residue built · HANDOVER

**Cut by** LE-D **at** dreamos-pwa `501d7bf2ad4acc01f9ea322b493fbe993d3c3e57` (seat F's F2), derived fetch-first by command.

**Collision derivation (R-40.82).** `git diff --stat dfc8f27..501d7bf` across this seat's whole radius — `app/admin/assistance/page.tsx`, `app/admin/_components/AdminUI.tsx`, `lib/admin-api/assistance.ts`, `lib/admin-api/_base.ts`, `docs/mocks/TDW_20_CONCIERGE/` — is **empty**. Seat E's E1a and seat F's F2 touched the Switchboard, the cockpit mock and `lib/admin-api/{index,modelRoutesCopy}.ts`. Nothing of mine. Ancestry confirmed.

**Shape and words only. No colour byte moved** (R-41.101) — accounted site by site in the manifest.

---

## 1 · F-41.30 / F-41.58 — one column, behind the sealed frame

The two-up grid put a search field, a `name + @handle + city` span and a button into ~180px at 374. **The founder's own glass showed it clipping** — `Instagram handl`, `WhatsApp numl`, `Name, if you knc`, the note running under the toast (F-41.79, the walked specimen).

Built to `F1-item-374` / `F5-item-430` as vetoed: one column at **both** widths, each forward door a full-width control opening its own sheet — the vendor wall (`F2-wall-374`) and the outsider fields (`F3-outsider-374`). **The 430 arm does not bring the grid back**; a desk is not the surface this queue is walked on.

Ratified copy landed with it: **S2-12** (`Jaipur first, then everywhere else. A–Z within each.`), **S2-16** kept as drawn, and **S2-3** — `Forwarded 2 of 3` → `2 of 3`, the byte I still owed from the veto.

## 2 · F-41.40 — the portal, at its one home

`position:fixed` positions against the nearest transformed ancestor, not the viewport. The admin content wrapper animates with `fade-up`, so it **creates a stacking context** and every fixed child — the sheet and its scrim — sat under the bottom bar regardless of its zIndex. A9 shipped a padding cure; **seat A named the true cause in the same handover and shipped the weaker fix anyway** (its close note §3.6), and it did not hold on the glass.

`BottomSheet` now renders through `createPortal` to `document.body`, SSR-guarded. **No zIndex value changed** — the cure is leaving the ancestor, not raising a number. Every admin sheet in the estate inherits it; this is `AdminUI.tsx`, the one control home.

The padding stays, but it is no longer *the* cure: a sheet still needs its last control clear of the bar once it is out of the trap.

## 3 · F-41.62 / F-41.81 — a code is not a sentence

The outsider send's synchronous refusal arrives on the **success shape**: the writer's catch returns `ok:true` with the code on the row, the door answers **201**, and `AdminApiError` never constructs — so `REFUSAL_WORDS` was never reachable by these. `FORWARD_CODE_WORDS` is their own home, keyed on what the row carries.

`131049` renders the chair's ratified **S2-5**: *Meta's marketing limit blocked this number. Forward someone else.* **The bare code never reaches glass** — it stays in the log and on the row, where it is to be grepped, not decoded.

**F-41.81's pwa half:** `interrupted` gets words of its own — *The send was interrupted before it left. Forward again.* It is written by the boot reconciler, never by Meta, and it must not read as a Meta refusal, because Meta never answered.

The row and the toast read **one home** (`forwardWords`), not two.

## 4 · S2-4 / F-41.76 — the register key leaves the glass

`source tdw_assist` no longer renders on the admin row or the forward toast. This is F-41.62's own class, and it is the specimen the copy rule surfaced rather than any instrument.

## 5 · c-41.38 — the palette stand-in, named on the tree

Two docs bytes, in the mock header and the veto sheet: `F1`–`F5` are drawn in Graphite; `/admin/assistance` reads `T` from `AdminUI.tsx`, which is **navy and oxblood** (`#0A0F18` / `#F0EAE0` / `#C44058`). Seat A labelled the same Graphite a stand-in; this seat inherited it and called it a transcription — true for `V1`/`V2`, false for these. **Colour is seat E's under R-41.71**, so Graphite is the destination and `AdminUI.T` is what leaves.

## 6 · Three cells in seat A's sealed pwa proof — disclosed, awaiting a number

The count did not move by these; `b20_a3` goes 88 → **102** purely by the +14 new cells.

| cell | was | why it moved |
|---|---|---|
| `:194` | `out.dark.reason` shown **verbatim** | **the chair struck that byte at the S2 veto (S2-6).** `dark.reason` reads *`template.tdw_assist_lead_outside` is off on the switchboard* — register grammar on glass, F-41.17's class. The dark state is still shown; only its vocabulary changed. |
| `:195` | `Forwarded {n} of {fanout}` | S2-3 struck the word `Forwarded`. |
| `:269` | `paddingBottom: ABOVE_ADMIN_BAR` appears **exactly 2** times | **a call-site count.** This rider legitimately adds two. Relaxed to `>= 2`, the guarantee the label claims. F-41.65's class; c-41.39 struck the same shape from `b20_a2:404`. **Third specimen this sitting.** |

**I have not minted a correction number for these** — corrections are the chair's to issue, and the last two both collided. Awaiting one.

## 7 · Two errors of mine, caught by my own new cells

**e-7** — `read()` already applies `P()`; I wrote `read(P(...))`, double-joined, and three F-41.40 cells silently read an empty string and failed. **e-8** — my S2-4 absence cell read the **raw** file and convicted the comment that explains the strike. **R-40.105 says an absence cell reads comment-stripped code**, and this is the third specimen of that class this sitting — the first one I authored into a cell rather than inherited. Both cured before the cut.

## 8 · Floor — `scripts/floor-manifest-ce41-d0-pwa.txt`

`b20_a3` GREEN **102/102** cured, RED **90/102** uncured. `tsc --noEmit` exit 0.

**Declared, not absorbed:** `npm run build` **fails in this container** — `next/font` cannot fetch DM Sans, Italiana and Jost from Google Fonts. That is F-40.134's known egress limit and precisely why R-40.66 makes `next build` **the founder's gate at apply** and `tsc --noEmit` the seat's sweep. **The build is unproven from here**; the verify chain runs it where the fonts resolve.

## 9 · What follows

**Seat E re-tokens `app/admin/assistance/page.tsx` on top of this shape**, per R-41.101. The key set is byte-identical before and after, so the re-token has nothing to reconcile — only values to swap.

**Owed and unchanged:** F-41.63's wire half (one screenshot from `9327715877`, then Discard) · F-41.79 closes with this build once walked at 374 · F-41.82 and F-41.83 deferred.

**Range: F-41.84–F-41.85 unspent.**
