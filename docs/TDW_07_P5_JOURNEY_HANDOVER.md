# TDW_07 P5 — THE JOURNEY'S STATE · EXECUTOR HANDOVER
**Base:** `dreamos-pwa @ 2351af4` · **Executor:** Opus-LE · **Date:** 2026-07-31

One file: `app/(frost)/frost/canvas/sanctuary/page.tsx` — the "Enquired" rows gain
a `Sent` label. tsc whole-tree: 0 errors. W-1 clean.

## WHY `SENT` SHIPS ALONE

Not a placeholder for a pair. `couple_enquiries` has **no state column** (9 columns,
witnessed) and no writer of "replied" exists anywhere in the estate. The row's
existence IS the state, and `Sent` is the honest whole of what we know.

The spec's §P5.4 asked this surface to "read sent/replied from existing enquiries
routes." It was reading a ghost — the route serves rows that have never carried a
reply state. `REPLIED` is chartered to the bride blocks, where reply-detection lives.

Rendered as its own element rather than folded into the meta line, because
`PHOTOGRAPHY · DELHI · SENT` reads as a third attribute of the vendor rather than
the status of her enquiry.

## UNMINTED, NOTED IN PASSING

`sanctuary/page.tsx:580` hardcodes `wa.me/917982159047` for the Enquired row's
WhatsApp control — the F-05.20 wa.me-constant class, and `lib/waNumbers.ts` exists
as its home. Out of this movement's scope; named rather than silently passed over.

## NEXT

The walk card, which cannot be authored until the fixture-state SELECT returns —
see the accompanying message.
