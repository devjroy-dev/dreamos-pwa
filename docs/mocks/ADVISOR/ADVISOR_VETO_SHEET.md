# R-41.139 · The Advisor page renders its own conversation · RATIFIED

**Frame ratified** by the chair on the read, 2026-09-09 — zero stray literals, tokens equal to `lib/worklist/theme.ts`.

## The frame, and the instrument that draws it

`advisor-inline-frame.html` is **emitted**, not typed: `tools/advisor_frame_emit.mjs` reads `lib/worklist/theme.ts` and writes both arms' token blocks with the commit in a header line. **No hand-typed `:root`** (R-41.140).

**F-41.105 is why.** The first frame declared its own palette — an ivory `#F0E6D2` retired at R-U19, gold as a UI accent when gold is the brand mark's, invented card colours, and Fraunces where the page reads `--wl-t1`, which is **Cormorant**. Twenty literals, none of them the tree's. The typeface was wrong too, not only the colour.

**It recurred inside the redraw and the emitter did not catch it**, because it was outside the emitted block: the gallery chrome hand-typed five hex values I had just read in the emitter's own output. Correct values, transcribed — the same defect at a shorter distance. The chrome reads the tokens now, and the file contains **zero** hand-typed hex outside the emitted stylesheet.

**The emitter refuses rather than guesses.** It threw twice while being built: once parsing zero tokens (`theme.ts` writes `'page-bg'` quoted and kebab, not `pageBg`), once on the count guard. A frame drawn from a half-read token file is F-41.105 wearing a script.

## What was vetoed

**The shape, not the words.** Every string is already on the tree — `COPY.advisorTitle`, `COPY.advisorEmpty`, `COPY.advisorThreadNote`, `InputBar`'s own placeholder. The founder ratified the header and intro at 06:12.

**A1 → A2:** the intro gives way once a turn exists. At 374 the page cannot carry header, intro, note and thread without the conversation starting below the fold.

**Both arms**, because the vendor lane has both and he walks in whichever he last set (F-38.41).
