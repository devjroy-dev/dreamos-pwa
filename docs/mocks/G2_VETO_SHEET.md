# G2 · GOOGLE REVIEWS — VETO SHEET · OPEN

**Base** `dreamos-pwa` `main` **`2bcd2bf`**, sibling `dream-os` `main` **`d91ec6e`**, both derived
at origin by command at the head of this delivery. **Every string in the CURRENT column is the
byte in the tree at those tips, read out of the file named beside it, not remembered.** Where
the current column says *(absent)* the product does not say the thing at all today.

**⚠ THIS SHEET WAS FIRST CUT AT `6eea5bf` AND RE-DERIVED AT `2bcd2bf`.** Two commits landed
between them — `45be1aa` (R-40.33/.34) and `2bcd2bf` (G5.1's mock). **Neither touched a single
file this delivery ships, and neither touched a single file it transcribes from:**
`git diff --stat 6eea5bf..2bcd2bf` over `app/v/[code]/page.tsx`, `wedding-pages-mock.html`,
`SolutionsPieces.tsx`, `lib/solutions/copy.ts`, `invoice-document-mock.html`,
`S2_VETO_SHEET.md` and `tools/mock_shot.cjs` returns **zero lines**. Every byte below is true
at the new tip because the sources are unmoved, not because they were carried.

**Mock** `docs/mocks/google-reviews-mock.html` — six frames, 15 captures. The invoice sheet is
**not** in this file: R-G2.8 puts it in `docs/mocks/invoice-document-mock.html` as frame
`S3-seal`, so the invoice document keeps one home and one veto sheet. That sheet gains **one
row, H5**, by labelled amendment. *(Seat decision, declared: the kickoff's "plus the invoice
paper frame" is read as locating the frame by R-G2.8 rather than as a second drawing of a
document that already has a mock. If the chair meant otherwise it is one move.)*

**How to read this.** One **YES / NO per numbered string.** A NO with your own words beside it
is the byte; a NO with nothing beside it sends the string back to be re-drafted, not shipped.
Nothing here is ruled until you say so. **Ten strings, and two of them already exist.**

---

## A · THE ROOM — bands and states

Frames `G1-room`, `G1-empty`, `G1-gbp`. The `W2-room` Leads-card idiom, unchanged: two lines
in the card, the state on the right, **no money anywhere on this room**.

| # | Current | Proposed | Note | YES / NO |
|---|---|---|---|---|
| 1 | *(absent)* | **`Asked`** | Band label over what went out. A noun, and the room's first band because it is the only one with rows in it on day one | |
| 2 | *(absent)* | **`Reviews`** | Band label over what came back. **It reads `0` until your Google listing is connected**, and string 6 is why — a band that says nothing looks broken | |
| 3 | *(absent)* | **`Your seal`** | Band label. `Your` because it is hers and it is the one thing on this room she cannot edit | |
| 4 | *(absent)* | **`Google listing`** | Band label. `listing` and not `page` or `profile`: Google's own product name in the vendor's mouth is *listing*, and `page` already means her `/v/` page in this product | |
| 5 | *(absent)* | **`Asked 3 Sep 2026`** | The row's state, right column. Date is the house format (`src/lib/format.js`'s month table — `Sep`, never `Sept`, and never `27/10/26`) | |
| 6 | *(absent)* | **`Reviews appear here once your Google listing is connected.`** | Under the `Reviews` band while it reads 0. Names what is missing and what still works, the shape `COPY.indexUnavailable` already carries. It does not apologise | |
| 7 | *(absent)* | **`On your page`** | The seal card's state, right column, accent ink. It says where the seal has gone — the storefront — rather than repeating that it exists | |
| 8 | *(absent)* | **`Counted every night from your own weddings. It is not editable.`** | Under the seal. Two facts a vendor will ask about in this order: where the numbers came from, and whether she can change them. **The second sentence is the whole trust position** and it is stated plainly rather than implied by the absence of a control | |

---

## B · THE EMPTY STATE — `G1-empty`, DEV440 today

The room's real first state and the one you will walk. **No control anywhere**: an ask is not
something she presses — it follows a published wedding page — so a button here would be a
second door onto a thing she does not drive.

| # | Current | Proposed | Note | YES / NO |
|---|---|---|---|---|
| 9 | *(absent)* | **`No reviews asked yet`** | Heading. Says the state, not "nothing here" | |
| 10 | *(absent)* | **`When you publish a wedding page, we ask that couple for a Google review. Once, and never again.`** | The line under it. It names the trigger (**publishing**, which is hers) and the guarantee (**once per couple, ever**), because the second is the thing a vendor is actually anxious about — she does not want us pestering her clients | |
| 11 | *(absent)* | **`Your seal appears after three delivered weddings.`** | Where the seal card would be. **A sentence, never a greyed badge.** A dimmed seal would be a control that lies about being available | |

---

## C · THE LISTING BAND — `G1-gbp`, drawn absent until 27 Oct 2026

**Never a disabled button.** There is no control here, greyed or otherwise, because there is
nothing she can do before the date and a dimmed *Connect* would tell her there is.

| # | Current | Proposed | Note | YES / NO |
|---|---|---|---|---|
| 12 | *(absent)* | **`We can claim and sync your Google listing from 27 Oct 2026.`** | The date is real and it is Google's, not ours: **a Business Profile must be verified and active for 60+ days** before the API will admit it. House date format | |
| 13 | *(absent)* | **`Google requires a profile to be 60 days old before it will let us in. Yours is counted from the day it was verified.`** | The second line names **who is waiting on what** — the shape `COPY.withheldNote` already carries (F-19.20). It also tells her the clock is hers and already running | |
| 14 | *(absent)* | **`What happens then`** + **`Your name, hours, service areas and photos stay in step with your rooms, and every couple we ask lands on your listing.`** | What she gets on the date. **No promise about traffic, ranking or "being found"** — master §7 refuses platform-SEO claims and this is the byte where such a claim would slip in | |

---

## D · THE SEAL — `G2-seal-light`, on the storefront a couple sees

**The one new element on `/v/<code>`.** Drawn above *Enquire*, on its own hairline, as the last
fact she reads before she acts.

| # | Current | Proposed | Note | YES / NO |
|---|---|---|---|---|
| 15 | *(absent)* | **`TDW-verified`** | The mark. Tracked caps, Jost 300, the cream lane's label face. **Hyphen, not an en dash** | |
| 16 | *(absent)* | **`4 weddings · delivers in 34 days`** | The facts. **No rating segment** — R-G2.2 withheld it because no source exists until GBP, and a seal that shows a blank star row is worse than one that never claims a rating. **No `Rs`, ever.** Lining tabular numerals so `4` and `34` read as numbers | |

**Three shape questions on this one element, each a real choice:**

- **PLACEMENT.** Above *Enquire* as drawn, or below the strip near the close? Above puts the
  proof beside the action; below keeps her photographs first. Drawn one way so you can see it,
  not because it is settled.
- **WEIGHT.** `G2-seal-light` sets the facts line at 13px in the page's own ink, one step under
  her `about`. **This seat's own reading of its own capture: it sits close enough to the
  paragraph above that it can read as another sentence rather than as a mark.** Named rather
  than quietly retuned — the lighter alternative is the facts line at 12px in the muted ink,
  which trades presence for separation.
- **SINGULARS.** At exactly three weddings string 16 renders `3 weddings · delivers in 34 days`.
  Nothing in it goes singular, because the seal never shows under three.

---

## E · THE COUPLE'S STOP — WhatsApp, bride line (R-G2.7)

The review ask is a **MARKETING** template (F-40.12), so a per-recipient opt-out is a condition
of sending it at all. `Stop messages` currently reaches our webhook and **nothing happens**
(F-19.08) — this string is the reply that makes it real.

| # | Current | Proposed | Note | YES / NO |
|---|---|---|---|---|
| 17 | *(absent)* | **`You won't get any more messages like this from us.`** | Sent to the couple when she taps `Stop messages` or types STOP. **Past tense of a thing already done**, not a promise: the flag is written before this goes out. **No persona name, no apology, no "are you sure"** — a stop that argues is not a stop. It does not say "from The Dream Wedding", because she may not remember us and the vendor is who she thinks she is hearing from | |

---

## F · ALREADY IN THE TREE — no new byte, listed so the pass is complete

| Byte | Home | State |
|---|---|---|
| `Google reviews` (the row and the room's masthead label) | `lib/solutions/copy.ts` `ROOM_ROWS` | **RULED** R-40.1, byte-frozen |
| `Open` (the row's chip) | `lib/solutions/copy.ts` `CHIPS.open` | **VETOED** by you, 2026-09-05, on your walk |
| `Your website & SEO` (row 3, drawn in `G5-hub`) | same | **RULED** R-40.26, supersedes `W5-hub`'s older byte with no re-shoot owed to that mock |
| `For your business` · `Something broken?` · `Message us on WhatsApp` | same + `lib/worklist/copy.ts` | **KEPT** from the live surface, unchanged |
| `None yet` | `COPY.noneYet` | exists; **not used here** — string 6 says the specific thing instead |
| `Hi {{1}}, thank you for choosing {{2}} for your wedding. If you have a minute, a Google review would mean a lot to them.` + button `Write a Review` | `TDW_19_P0A_LEDGER.md` B1 | **APPROVED AT META** verbatim, `Write a Review` capital R locked. R-G2.10 registers it as-is. **Not yours to veto — Meta holds these bytes** |

---

## G · WHAT THE MOCK CANNOT SETTLE

It is a drawing, so it proves layout, register and vocabulary. It does **not** prove that the
ask fires, that `reviews_asked` takes exactly one row per couple, that the nightly job counts
what string 8 promises, that the seal disappears under three weddings on a real row, or that
the URL button reaches Meta in the right shape. Those are the build's cells and the founder
card's SELECTs.

**Two things this sheet is honest about not having:** the seal on `G2-seal-light` shows **4
weddings** and DEV440 has **one**, so the present-seal frame is a seeded studio and its numbers
are drawn, not read. And **`Reviews` will read 0 on your walk and keep reading 0 until 27 Oct
2026** — that is the product working, not the walk failing.

**And one thing it cannot settle alone: the hub is now drawn by two seats.** `2bcd2bf` landed
G5.1's `referrals-mock__R5-hub`, which draws *Referrals & partners* live and *Google reviews*
still `Coming`; this sitting's `G5-hub` draws *Google reviews* live. **Neither is wrong** — each
draws its own room's completion — but the hub that actually ships is whichever sitting builds
last, and that is the chair's sequencing to state, not a byte on this sheet.
