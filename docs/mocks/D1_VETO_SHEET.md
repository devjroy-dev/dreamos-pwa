# D-1 · VETO SHEET — SEALED

**Founder 2026-08-29: "I'm good with the designs; CE decides."** Every proposed string below is **YES by founder delegation, recorded at the chair's hand 2026-08-29**, with the chair's substitutions applied. That formula is the provenance of every row in this sheet and is not repeated per line.

## THE PICK

| Surface | Shape | Frames that are the build target |
|---|---|---|
| **Today** | **Shape 1 — morning brief**, three in place | `A1-work` `A1-open` `A1-trunc` `A1-rest` `A1-first` |
| **Books** | **Shape 2 — month groups** | `B2-months` |
| **Studio** | **Shape 2 — one room, three tabs** | `C2-rooms` `C2-team` `C2-tasks` `C2-pay` |

**The unpicked shapes are RETAINED IN THE FILE, captioned `NOT PICKED`, for the record only** — `A2-*`, `B1-register`, `C1-*`. **A charter §2 must therefore name the FRAMES, not only the file**: *match `docs/mocks/<file>@<hash>`, frames `A1-*`* — otherwise a cell reading the file finds two answers. Stated here rather than discovered by the first executor.

**Count movement, disclosed not padded:** Today 12 frames → 10. `A1-fold2` and `A2-fold2` are **retired**: they were evidence for F-39.24 and the kind line is its cure, so the reader they served has moved. Books 2, Studio 7, both unchanged. Captures 54 → 50.

---

**Base** `08a6dfe` (dreamos-pwa/worklist, "S4/2 — the walk's cures"), sibling `7a03699` (dream-os/main).
Mocks pin nothing. Every token, rung and grid constant in the three files is **derived from `lib/worklist/theme.ts` at this tip by the generator**, not transcribed.

**How to read this.** One **YES/NO per string**. One **shape per surface** (or "neither, closer to X"). Your pick and your vetoes become the §2 of the build charters — S4/3 Today, 2c Books, 2c Studio — as *match `docs/mocks/<file>@<hash>`*, with "the founder says it reads like the mock" as card step ①.

**How to open.** Each `.html` is one self-contained file: the two faces (Cormorant Garamond 500, DM Sans 400/500) are embedded as data URIs, so it renders true on your phone with no signal. One switch at the top flips Graphite/Chalk for every frame at once. **The fold control is live** — press "Show all 11".

---

## 0 · WHAT THE MOCKS PROVED, AND ONE THING THEY DISPROVED

### F-39.24 — filed as measured, and CURED by the kind line

The finding stands as filed: at 374×844 the work area is **714 px**, and with three cards in place Shape 1 put Contracts at 723 and Team at 835 — two kinds below the fold. Shape 1 could not clear the clause at any in-place count above one.

**The chair's cure does not compress the masthead and does not cut the in-place count.** The five kinds sit above the fold **as the kind line**, measured at this render:

| | measured |
|---|---|
| kind line top / bottom | **96 / 110** px, one line at 14 px |
| work area | 714 px |
| anchors resolving to a live section | **5 of 5** — `#sec-leads` `#sec-invoices` `#sec-events` `#sec-contracts` `#sec-team` |
| truncated arm | reads `20+ leads`, same geometry |

Three cards in place survive; the sections still scroll, and that is now correct rather than a defect, because the line is both the knowing and the way down.

### The other three, confirmed rather than discovered

- **F-39.21** — the Books door projects no particular. Every particular on Surface B is marked below.
- **F-39.22** — `.wl-tcgatecta` is the estate's one surviving `.32em`; 14 other declarations are at `.08em`. The mocks use `.08em` only. S4/3 retires the byte.
- **F-39.15** — the mock numeral is **11**, in the real Cormorant. Your last mock could not see the oldstyle defect because its numeral was a zero, and zero is the one digit identical in both figure styles.

---

## A · TODAY — `today-working-mock.html`

Frames: `A1-work` `A1-open` `A1-trunc` `A1-rest` `A1-first` · `A2-work` `A2-open` `A2-trunc` `A2-rest` `A2-first` · evidence `A1-fold2` `A2-fold2`.

**Shape 1 — morning brief.** Kind-grouped, eyebrow + count per group, done today as a quiet ledger beneath a metal rule.
**Shape 2 — ranked stream.** No section chrome; the kind rides each card as a leading label in accent; same ledger as a footer.

### Every visible string

| # | Current at `08a6dfe` | Proposed | Note | YES / NO |
|---|---|---|---|---|
| A1 | *Saturday, 29 August* | unchanged | date line, t5 tracked caps | |
| A2 | `open items` | unchanged | caption beside the numeral | |
| A3 | *(no status line on working)* | unchanged | R-39.13 — the numeral is the status | |
| A4 | `Leads` `Invoices` `Events` `Contracts` `Team` | unchanged | read from `ROOMS`, never spelled | |
| A5 | `+` (truncation suffix) | unchanged | the 20+ tell — frames `A1-trunc`, `A2-trunc` | |
| A6 | — | **`Show all 11`** | **NEW.** R-39.14. **Sentence case at t4**, the shell's button register — not tracked caps. Reads `Show all 20+` when capped | RULED |
| A7 | — | **`Show fewer`** | **NEW.** Sentence case at t4 | RULED |
| A8 | — | **`Show all 11 leads` / `Show fewer leads`** | **NEW, Shape 2 only** — a stream has no section to name the kind, so the control must | |
| A9 | `Upgrade to Essential tier or above to connect with your lead.` | unchanged | your 2026-08-25 byte, character for character | |
| A10 | `See plans` | unchanged | your byte | |
| A11 | — | **`owed`** | **NEW.** t5 caption under the invoice figure | |
| A12 | — | **`Due today`** when `due_date` is today, else **`Due 29 Aug`** | **NEW.** Chair's substitution; the year drops on the non-today arm | RULED |
| A13 | — | **`Done today`** | **NEW.** t5 eyebrow over the ledger in the *working* state. The resting state keeps `All clear.` at t1 | |
| A14 | `All clear.` | unchanged, **resting only** | never over cards that disprove it | |
| A15 | `Counts cover invoices, contracts and tasks — the three that record when they were finished.` | unchanged | the one scope line; there is no second | |
| A17 | — | **the kind line** — `11 leads · 1 invoice · 1 event · 1 contract · 1 task` | **NEW, five nouns.** Chair amendment (1); the cure for F-39.24. Each segment anchors to its eyebrow. Truncated arm reads `20+ leads` | RULED |
| A18 | — | **`See all in Leads`** | **NEW.** Closes the expanded section when the wire capped the kind, beside the `+` tell | RULED |
| A16 | `Get started` / `Enquiries` / `Every enquiry is answered on WhatsApp in your name.` / `Open WhatsApp` / `Ask TDW` / `Try` / `Copy` / `Your link` | unchanged | first-run is **carried, not proposed** — shown so you see the state in the family | |

### What each shape asks of the contract

**Nothing.** Every field either shape renders is already on the wire and witnessed in `lib/vendor/types/vendor.ts`:

- lead — `name`, `wedding_city`, `wedding_date`, `budget_min`, `budget_max`, `redacted` ✓
- invoice — `client_name`, `invoice_number`, `amount_total`, `amount_paid`, `amount_owed`, `due_date` ✓ (so `Rs 0 of Rs 45,000` on **Today** needs no change — unlike Books)
- event — `title`, `kind`, `event_time`, `event_date` ✓
- contract — `title`, `state`, `sent_at` ✓ · task — `title`, `state`, `priority`, `due_date` ✓
- **done today** — `invoice_number`, `client_name`, `amount_total`, and task `title` are all on the wire and are read by nobody today. The ledger showing *Ritika Sharma · TDW/DEV440/02 · Rs 30,000* instead of the bare count **`1`** is a client change only.

**R-39.14's server constraint is honoured and not oversold:** "Show all" reaches the twenty the wire holds, the `+` tell stays, and neither shape promises a twenty-first row.

### ⚠ D-1/c3 — the kind line's nouns are NOT derivable from `ROOMS`

The amendment says "singular/plural from ROOMS labels". Four singularise cleanly — Leads/Invoices/Events/Contracts. **The fifth room is labelled `Team` and the ruled line reads `1 task`**, which is the *kind's* noun (`team_tasks`), not the room's. So A17 is **five new vendor-facing bytes**, not a formatting of the registry, and S4/3 cannot derive them from `ROOMS` the way `TodayCards` derives its eyebrows. Rendered as ruled; filed here rather than passed off as derived.

### Two declarations

1. **The eleven lead rows are specimen.** The relay carried the *count*, not the names. The fold claim is a claim about card heights and card count, and the measurements above are unaffected by which names sit in them. Every other string on this surface is yours.
2. **The event has no time and the contract no sent date** in the forwarded body, so the cards read `Call · Today` and `Sent`. If those fields are populated on the real row, the detail line grows to `11:00 · Call · Today` without a layout change.

---

## B · BOOKS — `books-register-mock.html`

Frames: `B1-register` (one period) · `B2-months` (month-grouped). Fixture: your DROY550 grid, verbatim. Chain 15,000 → 35,000 → 29,999 → 24,999. Head Received Rs 35,000 · Outstanding Rs 0.

A real `<table>` with `scope="col"`, `table-layout:fixed`, every numeral in **lining tabular** figures right-aligned, a hairline closing each movement. `Rs X,XX,XXX`, no glyph, no shorthand, no truncation. **No string on this surface is prose.**

### Every visible string

| # | Current at `79fc1db` | Proposed | Note | YES / NO |
|---|---|---|---|---|
| B1 | `Received` | unchanged | | |
| B2 | `Outstanding` | unchanged | | |
| B3 | `Date` | unchanged | | |
| B4 | `Credit` | **`Received`** | Chair's ruling; `Cr` struck. No convention left to misread | RULED |
| B5 | `Debit` | **`Paid out`** | Chair's ruling; `Dr` struck | RULED |
| B6 | `Balance` | unchanged | | |
| B7 | column order Credit \| Debit \| Balance | **Received \| Paid out \| Balance** | money in, then money out, then the running figure | RULED |
| B8 | `no date on file` | unchanged | rides the row it qualifies, never a legend | |
| B9 | — | **`Opening`** | **NEW.** period/group head | |
| B10 | — | **`Closing`** | **NEW.** period/group head | |
| B11 | — | **`14 Jul – 22 Jul 2026`** | **NEW** (formatted range, Shape 1) | |
| B12 | — | **`July 2026`** | **NEW** (formatted month, Shape 2) | |
| B13 | — | **the particular line** — `Kavya · TDW/DROY550/01 · Rs 15,000 of Rs 15,000` and `assistant · Payment to Swati Roy` | **NEW. F-39.21 — contract change both sides** | |
| B14 | `No money movements yet.` / `Could not read your books just now — try again in a moment.` | unchanged | the two opposite empties; neither drawn here | |

**⚠ D-1/c4 — `Received` now appears twice on one surface, meaning two different things.** The head figure `Received` is the vendor's lifetime total; the column head `Received` is this row's credit. Both are correct English and a reader scanning the surface meets the word twice at two scopes. Not a blocker and rendered as ruled — raised because Books is the surface where a word doing two jobs costs the most. If the chair wants it cured, the cheapest cut is the head figure, since the period head already carries Opening and Closing.

### What each shape asks of the contract

**F-39.21, and only the particular.** The typed door returns `{ id, date, undated, credit, debit, balance }`; `money.js` selects `amount_total` and `amount_paid` but projects neither onto a movement, and selects no client, no invoice number, no milestone label and no category. So **B13 is the whole of the contract change**, on both sides, chartered into 2c §3B.

**Opening and closing are NOT a contract change and NOT a computation.** Opening is zero by the door's own construction — the balance runs from zero at the first movement — and closing is the last movement's own `balance` cell, read. In Shape 2 a group's opening is the previous group's closing, also read. **This surface sums nothing**, and that is F-04.13's tuition kept rather than repaid.

**`invoices.description` is rendered nowhere.** Your rule; the fixture shows why (F-39.23, not this seat's).

### One declaration

**Shape 2 — THE PICKED SHAPE — shows a single group** — your grid spans one month. The repeated rhythm the shape exists for is shown by the group chrome, not proven by the data. No second month was invented to make the picture look better.

---

## C · STUDIO — `studio-rooms-mock.html`

Frames: `C1-rooms` `C1-tasks` `C1-pay` · `C2-rooms` `C2-team` `C2-tasks` `C2-pay`.

**What you met, and why.** `/w/team` already exists and is a **room of declared doors**: the structure crossed, the three destinations did not. Its rows leave the shell into `/vendor/studio/*` — which is the whole of "it loads as a separate room", and the animation on first load is `pinFadeIn`/`slideDown` on the old-shell side. Both shapes close that leak. Neither carries a boot animation, a second masthead or a second nav; both inherit the tokens.

**Shape 1 — three rooms.** Tasks and Team Payments join Team in the business band. **Rooms geometry changes: 19 → 21 tiles**, business band 11 → 13, work band untouched at 8. The two new tiles are outlined in `C1-rooms` so you can see where they land. Tile height stays 64 and all 21 stay above the fold.
**Shape 2 — one room, three tabs.** `/w/team` hosts Team · Tasks · Payments. **Tile count unchanged at 19**; the declared-doors body is *replaced*, not linked out of.

### Every visible string

| # | Current | Proposed | Note | YES / NO |
|---|---|---|---|---|
| C1 | `Team` | unchanged | registry label | |
| C2 | — | **`Tasks`** | **NEW** — a tab (Shape 2 picked) | RULED |
| C3 | — | ~~`Team Payments`~~ | **WITHDRAWN** — Shape 1 was not picked, so no room label is minted | n/a |
| C4 | — | **`Payments`** | **NEW** — the tab label | RULED |
| C5 | `your work` / `your business` | unchanged | band labels | |
| C6 | — | **`Open` / `Done today`** | **NEW** — Tasks tab sections | RULED |
| C7 | — | **`Members`** | **NEW** — Team tab section | RULED |
| C8 | — | **`Unpaid` / `Paid`** | **NEW** — Payments tab sections | RULED |

### What each shape asks of the contract

**Unknown, and declared as unknown.** No Studio fixture was forwarded and none was invented into a claim. **The team members and the payment rows in `C1-pay`, `C2-team`, `C2-pay` are specimen** — the one real row on this surface is your DEV440 task, `Confirm second shooter for 7 Mar`. Before 2c is chartered, the same read-first this sitting did for Books is owed for `/vendor/studio/team`, `/tasks` and `/team-payments`: what those three surfaces read today, and whether their rows carry the fields these cards show.

**Shape 2 is the pick, so the Rooms geometry does not move.** `ROOM_COUNT_EXPECTED` stays 19 and `BOTTOM_BAND_EXPECTED` stays 11; the three constants Shape 1 would have shifted are untouched, and no registry entry or route is minted. What 2c-Studio builds is the replacement of `/w/team`'s declared-doors body with the three tabs.

---

## D · THE RULES THE MOCKS KEPT

Derived from `theme.ts` at `08a6dfe`, not from taste:

1. **Six rungs, closed.** No seventh tuple, no literal font declaration anywhere in the three files.
2. **t0 is one element per app** — the Today numeral. **Books does not reach for it**; its head figures are t2, the largest rung available to it.
3. **One t1 per surface.** Today's resting head; Books and Studio take none.
4. **Cormorant at t0 and t1 only.** Never prose, never italic in chrome.
5. **Letter-spaced uppercase in two places, at `.08em`** — dock seats and section eyebrows. **The `.32em` survivor does not appear.**
6. **`font-variant-numeric: lining-nums tabular-nums` after every `font` shorthand on any numeral.** 21 declarations across the three files.
7. **The column owns the gutter.** No surface sets a horizontal inset.
8. **Grid untouched** — 4/8/16, tile 64, row 52, FAB 56 at bottom 136, one home.
9. **44px tap floor** on every control drawn, including the fold button.
10. **No persona name in chrome**, `Rs X,XX,XXX`, no `₹`, no k/L/Cr.

---

## E · CAPTURES

`tools/mock_shot.cjs` (the fourth file, granted at R-2) shot **50 frames**: every frame at **374×844** in both modes, and each shape's lead frame again at **390×844** (R-3). `deviceScaleFactor: 2`. Naming: `<mock>__<frame>__<mode>__<width>.png`.

**What a PNG from this arm cannot prove:** your device's own rasteriser. The faces are correct — they are in the file — but hinting and subpixel rendering are the phone's. Open the HTML for the final word on type.
