# TDW · CE-41 · SEAT E — THE COCKPIT CENSUS (E0)

**Derived at `dreamos-pwa` `60e66cf94d19b1f7ab01e55b48f34988a01e0b1f` (`origin/main`, fetch-first, 2026-09-09) · dream-os `fad5e68cbd4fd5e3dedbfb7d980b256b66b714fb` read-only.**
**Re-cut twice from the `bc76e24` original at the chair's word — first to `44b8590`, then to `60e66cf`. Collision derivation, by command: `git diff --stat bc76e24..60e66cf -- docs/mocks/COCKPIT/` is empty — no other seat wrote into this folder, so the file lands as authored. The seven commits between `bc76e24` and this tip touched `app/admin/switchboard/page.tsx` (C3, C5), `app/admin/assistance/page.tsx` (A9, c-41.23), `app/admin/_components/CommandPalette.tsx` (C3) and added `lib/admin-api/switchboardCopy.ts`; and the seventh (D1, seat D) added three docs files — `docs/TDW_CE41_D1_HANDOVER.md` and two under `docs/mocks/TDW_20_CONCIERGE/`, none in this folder and none in this seat's code radius (`git diff --stat 44b8590..60e66cf -- app/admin lib/admin lib/worklist lib/admin-api scripts tools` is empty). Every count in this document was re-derived at `60e66cf` by command, not carried: 700/675/58/53/49/131/855/139 · 37 routes · 27 button files · 191 `--admin-*` reads · 493 `T.<colour>` reads · 436 family names — **no movement from `44b8590`.**
Investigation only. No product byte is written by this packet. Every count below was produced by a command over the comment-stripped tree (R-40.105); the raw-with-comments figure is given beside it where the chair's kickoff quoted the raw number.

**Stands on:** R-41.71 (the cockpit adopts the vendor palette) · R-41.72 (the home is `lib/worklist/theme.ts`, CSS variables via `scopeCss`) · R-41.73 (`scopeCss('.adm')` in `app/admin/layout.tsx`; `tokens.css` deleted; no alias file) · R-41.74 (Graphite only; the `dark` arm pinned) · R-41.75 (`AdminUI.tsx` is the one control home) · R-41.76 (`--admin-metal-*` alphas → `card-border` / `row-hover` until R-40.133's set is minted) · R-41.77 (type and radius in scope) · R-41.78 (`tdw10_p2_retint` retires with its subject) · R-41.82 (the Switchboard is re-shaped, not re-tokened) · R-41.89 (persona names on Model routes only) · R-41.98 (rows are verb + state) · R-41.99 (seat E owns `switchboardCopy.ts`) · R-40.129 (seven inks; roles are inks never grounds; toasts read the surface; brand marks only on the mark) · c-41.19 / c-41.20 (the kickoff's home and path corrections).

---

## §0 · THE HEADLINE

| | chair (raw) | derived, raw | derived, comment-stripped |
|---|---|---|---|
| routes (`page.tsx` under `app/admin`) | 37 | **37** | — |
| files under `app/admin` + `lib/admin` | — | **46** (37 pages, `layout.tsx`, `ContentPage.tsx`, 6 `_components/*`, `lib/admin/waDial.ts`) | — |
| 6-hex literals | 700 | **700** | **675** |
| distinct 6-hex | 58 | **58** | **53** |
| 3-hex literals | — | 49 | **49** (`#111` ×37, `#FFF` ×8, `#CCC` ×4) |
| `rgb()` / `rgba()` | — | 138 | **131** (84 distinct) |
| `hsl()` · named colours in a colour property · `&#NNNN;` entities | — | 0 · 0 · 0 | |
| **every colour literal, all forms** | — | — | **855 sites · 139 distinct** |
| files carrying a `<button>` | 27 | **27** | **116 sites** (117 raw; one in a comment) |

**MOVEMENT SINCE `bc76e24`, DISCLOSED NOT ABSORBED.** Every colour count is unchanged (700/675/58/53/49/131/855/139) — the six intervening commits moved logic and words, not paint. Two counts did move: `T.<colour>` reads **491 → 493** (+2, seat C's C5 on `switchboard/page.tsx`, 37 → 39), and font-family names **427 → 436** (+9, C3/C5 and A9). `--admin-*` reads are unchanged at **191** (the raw `grep` over the folder returns 234; 43 of those are `tokens.css`'s own *declarations*, which are not reads — the file is deleted whole under R-41.73, so it is counted once as a deletion, not 43 times as a re-point).

The cockpit runs **three colour systems at once**, and none of the three is the vendor palette:

1. **`_components/tokens.css`** — 34 `--admin-*` roles, espresso, every one citing `lib/vendor/theme.ts DARK` as its donor. The donor moved to Graphite; **every citation is false at tip** and `scripts/tdw10_p2_retint.proof.mjs` reads 21/76 RED (on the floor's NAMED BASE as a known red). Readers: `layout.tsx` 46, `Bridge.tsx` 65, `MintSheet.tsx` 21, `CommandPalette.tsx` 16, `approvals/discover/page.tsx` 41, `AdminUI.tsx` 2 — **191 read sites**.
2. **`AdminUI.T`** — a navy/oxblood JS object (`bg #0A0F18`, `gold #C44058` = oxblood, `ink #F0EAE0`, three state roles each with a `Soft` tinted ground). Readers of a colour key: 16 files, **493 sites** (`demo` 102, `AdminUI` 110, `assistance` 38, `switchboard` 39, `makers` 30, `prospects` 24, `vendors/portfolio` 24, `conversations/vendors` 23, `conversations/brides` 21, `dreamers` 21, `config` 17, `hot-dates` 16, `content/spotlight` 10, `couture` 9, `approvals/photos` 8, `MintSheet` 1).
3. **Inline cream** — 16 pages carrying 640 of the 675 stripped hexes, all Editorial-Paper literals (`#888580`, `#111111`, `#E2DED8`, `#F8F7F5`, `#555250`, `#FFFFFF`, `#F0EEE8`). These pages paint **white cards with `#111111` ink inside the espresso shell** — light islands on the founder's dark glass today.

The vendor home, by command: `lib/worklist/theme.ts` — `GRAPHITE` / `CHALK`, 33 keys, `TOKEN_COUNT_EXPECTED = 33`, emitted by `scopeCss(scope)` as `--atelier-*` (24 keys) and `--role-*` (9 keys), mounted at `WorklistShell.tsx:133` and `WorklistBoot.tsx:115` under `.wl[data-wl-mode]`. Type: six rungs `--wl-t0..t5` from `typeCss`, two families (`TYPE_ROLE.feature` Cormorant / `TYPE_ROLE.body` DM Sans), `TAP_MIN = 44`, `GRID { base 4, step 8, gutter 16, tile 64, row 52 }`. The control register: `.wl-btn` at `WorklistShell.tsx:251-257` (44 min-height, 3px radius, `font: var(--wl-t4)`, `.08em` uppercase, transparent ground; `.pri` = `--atelier-accent-text` ground on `--role-ink-deep` ink; `:focus-visible` = 2px `--atelier-accent-text` outline).

`lib/vendor/tokens.ts` (42 lines: `COLORS {cream, warm, gold, ink, muted, border}`, `FONTS`, `FONT_WEIGHTS`, `EASE`, `CURRENCY_PREFIX`) is **the cream stub, not the home** (c-41.19). Its four readers (`components/vendor/ListRow.tsx`, `ActionCard.tsx`, `ListSlicePicker.tsx`, `components/shared/VendorProfileContent.tsx`) are outside `b59`'s scope and outside this seat's lane; named, not touched.

---

## §1 · (a) EVERY ADMIN ROUTE AND ITS CONTROLS

Counted over comment-stripped source. `<button>` = a raw element; the four named columns are `AdminUI`'s primitives; `onClick` counts every handler as an upper bound on interactive verbs (CE-115 control-inventory law — this is the list the E2 riders are accounted against, each control KEPT; a token swap removes nothing).

| route / file | `<button>` | GoldBtn | GhostBtn | ActionChip | ActionLink | `<input>` | `<select>` | `<textarea>` | `<a>` | onClick | colour system today |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `/admin` (`page.tsx` → `Bridge.tsx`) | 2 | · | · | · | · | · | · | · | · | 4 | `--admin-*` |
| `/admin/approvals` | 13 | · | · | · | · | 1 | 2 | 2 | 1 | 15 | inline cream |
| `/admin/approvals/discover` | 3 | · | 7 | · | · | 2 | · | · | · | 12 | `--admin-*` |
| `/admin/approvals/photos` | · | · | · | 4 | · | 1 | · | · | · | 4 | `T` |
| `/admin/assistance` (seat A) | · | 2 | 3 | · | · | · | · | · | · | 6 | `T` |
| `/admin/collab` | 4 | · | · | · | · | · | · | · | · | 4 | inline cream |
| `/admin/config` | 1 | · | · | · | · | 1 | · | · | · | 1 | `T` |
| `/admin/content/exploring` `heroes` `landing` `muse-pool` `surprise-me` (→ `ContentPage.tsx`) | · | · | · | · | · | · | · | · | · | · | `T` via `ContentPage` (UploadZone / ImageGrid primitives) |
| `/admin/content/spotlight` | · | 2 | 1 | · | · | · | 1 | · | · | 3 | `T` |
| `/admin/control-room` | 2 | · | · | · | · | 1 | 1 | · | · | 2 | inline cream |
| `/admin/conversations/brides` | · | · | · | · | · | · | · | · | · | 2 | `T` |
| `/admin/conversations/vendors` | · | · | · | · | · | · | · | · | · | 2 | `T` |
| `/admin/couples` | 8 | · | · | · | · | 5 | 1 | · | 1 | 8 | inline cream |
| `/admin/couture` | · | · | · | 1 | · | · | · | · | · | 1 | `T` |
| `/admin/dashboard` | 4 | · | · | · | · | · | · | · | · | 5 | inline cream |
| `/admin/data` | 3 | · | · | · | · | · | · | · | · | 3 | inline cream |
| `/admin/demo` | 4 | 2 | 7 | · | · | 1 | · | 2 | 3 | 14 | `T` (+ one `#25D366` WhatsApp button, §3) |
| `/admin/discover-heroes` | 10 | · | · | · | · | 5 | · | · | · | 11 | inline cream |
| `/admin/dreamers` | 1 | · | 1 | 2 | 1 | · | · | · | · | 5 | `T` |
| `/admin/exploring` | 8 | · | · | · | · | 2 | · | · | · | 9 | inline cream |
| `/admin/featured` | 2 | · | · | · | · | 1 | · | · | · | 3 | inline cream |
| `/admin/health` | 1 | · | · | · | · | · | · | · | · | 1 | inline cream |
| `/admin/hot-dates` | 1 | 2 | 1 | · | · | · | · | · | · | 4 | `T` |
| `/admin/images` | 7 | · | · | · | · | · | · | · | · | 7 | inline cream |
| `/admin/login` | 1 | · | · | · | · | 1 | · | · | · | 1 | inline navy (the retired `#213650` family, 3 sites) + oxblood |
| `/admin/makers` | 2 | · | 1 | 5 | 1 | · | · | · | · | 9 | `T` |
| `/admin/messages` | 2 | · | · | · | · | 1 | · | · | 1 | 2 | inline cream |
| `/admin/photos` | 5 | · | · | · | · | 1 | · | · | · | 5 | inline cream |
| `/admin/preview` | 5 | · | · | · | · | 1 | · | · | · | 5 | inline cream |
| `/admin/prospects` | · | 4 | 8 | · | · | · | · | 1 | · | 12 | `T` |
| `/admin/switchboard` (seat C) | · | 2 | 4 | · | · | 2 | · | · | · | 6 | `T` |
| `/admin/vendors` | 9 | · | · | · | · | 4 | 2 | · | 1 | 9 | inline cream |
| `/admin/vendors/portfolio` | · | 1 | 1 | · | · | · | 1 | · | · | 2 | `T` |
| `layout.tsx` (shell: nav, bar, drawer, sign-out) | 7 | · | · | · | · | · | · | · | · | 10 | `--admin-*` |
| `_components/AdminUI.tsx` (the primitives' own bodies) | 9 | 1 | 2 | · | · | 4 | 1 | · | · | 18 | `T` + 2 `--admin-*` |
| `_components/CommandPalette.tsx` | 1 | · | · | · | · | 1 | · | · | · | 2 | `--admin-*` |
| `_components/MintSheet.tsx` | 1 | · | 3 | · | · | · | · | · | · | 4 | `--admin-*` |
| **total** | **116** | **16** | **39** | **12** | **2** | **35** | **7** | **5** | **7** | **211** | |

**Route notes.** `adminNav.ts` names 39 hrefs: the 37 pages minus `/admin/login` (off-nav), plus `/admin/money`, `/admin/revenue`, `/admin/subscriptions` inside a commented tombstone block (F-10.76) — not live, not in radius. Five `content/*` routes are 5-line wrappers around `ContentPage.tsx`; re-tokening `ContentPage` re-tokens all five. The Assistance forward and the Switchboard flip are the two doors the walk taps (kickoff §8); both are `GoldBtn`/`GhostBtn` readers and move with `AdminUI.tsx` in E2 (i), not with their pages.

---

## §2 · (b) EVERY COLOUR LITERAL, BY FILE

855 sites over 29 files. "Distinct" is per file. The six most frequent literals are shown; the full per-literal table with meaning is §3.

| file | sites | distinct | top literals |
|---|---|---|---|
| `approvals/page.tsx` | 113 | 26 | `#888580`×31, `#111`×14, `#E2DED8`×14, `#C9A84C`×8, `#FFFFFF`×8, `#F8F7F5`×7 |
| `control-room/page.tsx` | 94 | 16 | `#888580`×40, `#111111`×15, `#C9A84C`×13, `#F0EEE8`×7, `#E2DED8`×4, `#FFFFFF`×3 |
| `vendors/page.tsx` | 60 | 10 | `#555250`×15, `#111111`×12, `#E2DED8`×8, `#F8F7F5`×8, `#C9A84C`×6, `#FFFFFF`×4 |
| `couples/page.tsx` | 55 | 10 | `#555250`×14, `#111111`×12, `#F8F7F5`×8, `#E2DED8`×7, `#C9A84C`×4, `#FFFFFF`×4 |
| `_components/AdminUI.tsx` | 51 | 41 | `rgba(196,64,88,·)` ×11 (oxblood at seven alphas), `rgba(255,255,255,·)` ×8, `#0F1622`×2, `#10171F`×2, `#161310`×2, `#2A1010`/`#0F1F14` (the toast's tinted grounds) |
| `discover-heroes/page.tsx` | 45 | 14 | `#E2DED8`×8, `#111`×7, `#F8F7F5`×7, `#111111`×4, `#FFF`×3, `#FFFFFF`×3 |
| `collab/page.tsx` | 43 | 14 | `#555250`×13, `#F0EEE8`×5, `#111111`×5, `#F8F7F5`×4, `#C9A84C`×3, `#E2DED8`×3 |
| `messages/page.tsx` | 41 | 17 | `#888580`×8, `#111111`×5, `#FFFFFF`×4, `#F8F7F5`×3, `#E2DED8`×3, `#F4F1EC`×2 |
| `images/page.tsx` | 38 | 14 | `#111111`×10, `#E2DED8`×5, `#888580`×5, `#FFFFFF`×3, `#4A7C59`×3, `#F8F7F5`×2 |
| `dashboard/page.tsx` | 37 | 15 | `#111111`×5, `#888580`×5, `#E2DED8`×4, `#F0EEE8`×3, `#F8F7F5`×3, `#FFFFFF`×3 |
| `exploring/page.tsx` | 37 | 14 | `#111`×7, `#E2DED8`×5, `#F8F7F5`×5, `#FFF`×4, `#FFFFFF`×3, `#555250`×2 |
| `_components/tokens.css` | 36 | 24 | `#16100C`×6, `#120F0E`×4, `#1F1612`×3, `rgba(31,22,18,0.78)`×2, `rgba(240,230,210,·)`×10 |
| `photos/page.tsx` | 36 | 10 | `#111111`×7, `#E2DED8`×6, `#555250`×6, `#F8F7F5`×5, `#FFFFFF`×4, `#C9A84C`×4 |
| `preview/page.tsx` | 36 | 10 | `#888580`×12, `#111`×7, `#E2DED8`×6, `#F8F7F5`×3, `#FFFFFF`×2, `#C8C4BE`×2 |
| `featured/page.tsx` | 35 | 13 | `#888580`×8, `#111111`×6, `#F8F7F5`×4, `#FFFFFF`×3, `#C9A84C`×3, `#E2DED8`×3 |
| `data/page.tsx` | 34 | 11 | `#111111`×8, `#888580`×7, `#F8F7F5`×5, `#FFFFFF`×3, `#E2DED8`×3, `#C9A84C`×2 |
| `health/page.tsx` | 28 | 17 | `#111111`×4, `#4A7C59`×3, `#9B4545`×3, `#888580`×3, `#F8F7F5`×2, `#F0EEE8`×2 |
| `login/page.tsx` | 11 | 11 | `#C44058`, `#F0EAE0`, `#213650`, `#18293E`, `#122031` (the retired navy), `rgba(240,234,224,0.5)`, `rgba(255,255,255,0.12)` |
| `demo/page.tsx` | 10 | 9 | `rgba(255,255,255,0.04)`×2, `#FFF`, `rgba(212,160,23,0.15)`, `rgba(78,201,148,0.1)`, `rgba(37,211,102,0.12)`, `rgba(37,211,102,0.35)`, `#25D366` |
| `conversations/brides/page.tsx` | 3 | 3 | `rgba(240,234,224,0.12)`, `rgba(240,234,224,0.15)`, `rgba(240,234,224,0.06)` |
| `approvals/photos/page.tsx` | 2 | 2 | `#10171F`, `rgba(255,255,255,0.04)` |
| `config/page.tsx` | 2 | 2 | `rgba(255,255,255,0.06)`, `#0A0908` |
| `conversations/vendors/page.tsx` | 2 | 2 | `rgba(255,255,255,0.06)`, `rgba(240,234,224,0.6)` |
| `_components/CommandPalette.tsx` | 1 | 1 | `rgba(0,0,0,0.7)` |
| `approvals/discover/page.tsx` | 1 | 1 | `rgba(0,0,0,0.6)` |
| `content/spotlight/page.tsx` | 1 | 1 | `rgba(255,255,255,0.04)` |
| `layout.tsx` | 1 | 1 | `#1F1612` (the `<meta name="theme-color">`, §3 row ⊘-1) |
| `switchboard/page.tsx` | 1 | 1 | `rgba(255,255,255,0.04)` |
| `vendors/portfolio/page.tsx` | 1 | 1 | `rgba(255,255,255,0.04)` |
| **17 files with zero literals** | 0 | | `ContentPage.tsx`, `Bridge.tsx`, `adminNav.ts`, `page.tsx`, `assistance`, `couture`, `dreamers`, `hot-dates`, `makers`, `prospects`, five `content/*` wrappers, `lib/admin/waDial.ts` — these read `T` or `--admin-*` and carry no value of their own |

**The load is the 16 cream pages.** They hold 640 of 675 stripped hexes and no local constant: every one is inline at the call site. `approvals/page.tsx` alone carries 113 sites and 26 distinct literals.

---

## §3 · (c) EVERY LITERAL → ITS VENDOR TOKEN, OR NO EQUIVALENT

The property each literal is applied to was read from the 60 characters before it (`background`/`gradient` → ground; `border`/`outline`/`stroke` → edge; `color`/`fill` → ink; `boxShadow` → shadow). Graphite values are quoted from `GRAPHITE` at `theme.ts:173-219` for the reader's eye; **no cut will carry a value — every site becomes a `var()` read** (R-41.72/.73).

### §3.1 · Surfaces

| literal | sites | painted as | meaning | vendor token (Graphite) |
|---|---|---|---|---|
| `#F8F7F5` | 67 | ground 57 · ink 9 | the cream page / the ink on a dark chip | ground → `--atelier-page-bg` (`#141516`); ink → `--role-ink-deep` when it sits on an accent ground, `--atelier-ink` otherwise |
| `#FFFFFF` + `#FFF` | 58 | ground 53 · ink 5 | the white card | `--atelier-card-bg` (with `--atelier-card-border`); the 5 inks → `--role-ink-on-metal` on a gold ground, `--atelier-ink` otherwise |
| `#F0EEE8` | 31 | ground 19 · edge 10 | the warm chip ground / the table hairline | ground → `--atelier-section-bg`; edge → `--atelier-card-border` |
| `#F4F1EC`, `#E8E5DF`, `#FAFAFA`, `#EEECE8`, `#FFF8F8` | 19 | ground / edge | warm surfaces and one pink-tinted row | `--atelier-section-bg` / `--atelier-card-border`; `#FFF8F8` (a tinted row) → `--atelier-row-hover` |
| `#111111` + `#111` | 132 | **ground 43** · ink 87 | the ink — and, 43 times, a **black chip/toast/selected-tab ground** | ink → `--atelier-ink`; ground → `--atelier-sheet-bg` for toasts (R-40.129 ②), `--atelier-accent-text` for the selected tab (the shell's `.pri` shape) |
| `--admin-bg-top/-mid/-bot`, `--admin-bg` (`#1F1612`, `#16100C`, `#120F0E`, the 165° gradient) | 8 + 191 reads | ground | the espresso page, three stops | `--atelier-page-bg` — **⊘-3: the gradient dies; the shell is flat** |
| `--admin-nav-top/-bot/-bg` | | ground | the sidebar, darker than the page | `--atelier-header-bg` (`#1A1B1D`); the deeper stop → `--atelier-bg` (`#0F1011`) — **⊘-3** |
| `--admin-bar-bg`, `--admin-header-bg` `rgba(31,22,18,.78)` + `backdrop-filter` | 2 | ground | the translucent blurred top bar | `--atelier-header-bg`, opaque — the blur is decoration under R-40.60; named for the chair |
| `T.bg #0A0F18`, `T.surface #0F1622`, `T.sheet`, `#10171F` | 7 | ground | navy chrome (`AdminUI` `T`, `login`, `approvals/photos`) | `--atelier-page-bg` / `--atelier-sheet-bg` |
| `#213650`, `#18293E`, `#122031` | 3 | ground | the retired navy gradient, surviving on `/admin/login` only | `--atelier-page-bg` |
| `#161310`, `#111009`, `#1A1614`, `#1E1C1A`, `#2A2825` | 8 | ground | `AdminUI`'s sheet gradient, a photo frame, the skeleton shimmer in `messages` | `--atelier-sheet-bg` / `--atelier-sheet-top` / `--atelier-sheet-bot`; the shimmer reads `--atelier-row-hover` |
| `T.card rgba(255,255,255,.055)`, `T.cardHover .09`, `--admin-card-bg .035`, `rgba(255,255,255,.03/.04/.05/.06)` | 19 | ground | the translucent card | `--atelier-card-bg`; hover → `--atelier-row-hover` |
| `rgba(0,0,0,.4/.5/.55/.6/.7/.72/.85)`, `--admin-scrim`, `T` overlays | 21 | ground / shadow | scrims and drop shadows | scrim → `--role-scrim`; shadow → `--atelier-card-shadow` (`rgba(0,0,0,.50)`) |
| `--admin-option-bg #16100C` | 1 | ground | `<option>` needs an opaque ground | `--atelier-sheet-bg` |

### §3.2 · Inks

| literal | sites | painted as | meaning | vendor token |
|---|---|---|---|---|
| `#888580` | 123 | ink 117 | the muted ink (sub-labels, notes, eyebrows) | `--atelier-ink-mute` (`#888B8E`) |
| `#555250` | 60 | ink 51 | the soft ink | `--atelier-ink-soft` (`#C8CACC`) |
| `#C8C4BE`, `#CCC` | 8 | ink | the faded ink | `--atelier-ink-fade` (`#585B5E`) |
| `--admin-ink` `#F0E6D2` · `--admin-ink-soft/-mute/-dim/-fade` (`rgba(240,230,210,·)`) | 5 + reads | ink | the five-rung ladder | `--atelier-ink` / `-ink-soft` / `-ink-mute` / `-ink-dim` / `-ink-fade` |
| `T.ink #F0EAE0`, `T.soft .62`, `T.muted .42`, `T.dim .22`, `rgba(240,234,224,·)` ×14 | 21 + 491 reads | ink | `AdminUI`'s ladder | `--atelier-ink` / `-ink-soft` / `-ink-mute` / `-ink-fade`. `T.dim` at .22 is under every bar; it maps to `ink-fade`, the shell's lowest rung |
| `#0A0908` | 1 | ink | the ink on a gold Save button (`config`) | `--role-ink-on-metal` (`#141516`) |
| eyebrows / labels (`--admin-ink-mute` at 10px `.14em`) | | ink | the section eyebrow | `--atelier-label` (`#B9BCBF`) at `--wl-t5` |

### §3.3 · The accent and the metal

| literal | sites | painted as | meaning | vendor token |
|---|---|---|---|---|
| `T.gold #C44058`, `T.goldDim .70`, `T.goldSoft .18`, `T.borderFocus .55`, `rgba(196,64,88,·)` ×11 | 24 + reads | ground (GoldBtn), ink, edge (focus), shadow | **the oxblood — the cockpit's primary accent** | `--atelier-accent-text` (`#68C9B4`). `GoldBtn` → the shell's `.pri`: accent ground, `--role-ink-deep` ink. Focus ring → the shell's `:focus-visible` (2px `--atelier-accent-text`). The `.22` glow → `none` |
| `#C9A84C` | 51 | ink 26 · ground 9 · edge 8 | the gold — on `control-room`, `approvals`, `data`, `photos`: a switch knob, a progress bar, a count pill, an approve button, tier chips | **ink → `--role-metal` (`#C9A84C`); ground → ⊘-2 (below); edge → `--role-metal` at full weight** |
| `--admin-metal #C9A84C` | 1 + reads | ink | wordmark + genuine alerts | `--role-metal` |
| `--admin-metal-soft .55`, `-line .28`, `-wash .10`; `rgba(201,168,76,.06/.08/.10/.25/.3/.4/.9)` ×19 | 22 | ground 17 · edge 5 | gold at alpha — chip washes, hairlines | **R-41.76:** ground → `--atelier-row-hover`; edge → `--atelier-card-border`. Sites: `AdminUI.tsx` (metal-soft/line/wash readers), `layout.tsx:188` (active nav wash), `control-room:88`, `dashboard:135`, `approvals`, `data`, `featured`, `photos`, `collab`. Re-point to `--role-metal-line/-wash/-edge` when R-40.133's set lands — grep `R-41.76` in the rider |
| `rgba(201,168,76,.9)` | 1 | ground | a gold toast ground | `--atelier-sheet-bg` (R-40.129 ②) |

### §3.4 · The state roles

| literal | sites | painted as | meaning | vendor token |
|---|---|---|---|---|
| `#4A7C59`, `#4CAF50`, `#388E3C`, `#2A7A4B`, `T.success #4EC994`, `--admin-positive #7FBE85` | 21 | ink 13 · ground 5 | positive | ink → `--role-positive` (`#6FC98C`); **ground → ⊘-2** |
| `#9B4545`, `#C0392B`, `#E57373`, `T.danger #E0574E`, `--admin-critical #E07B5C`, `rgba(217,88,88,·)` | 27 | ink 12 · ground 9 · edge 4 | critical / deny | ink → `--role-critical` (`#E8836B`); edge → `--role-critical`; **ground → ⊘-2** |
| `T.warning #D4A017`, `#F0A500`, `--admin-caution #E0A870` | 4 | ink | caution | `--role-caution` (`#DFAE6C`) |
| **tinted state grounds:** `T.successSoft rgba(78,201,148,.15)`, `T.dangerSoft rgba(224,87,78,.15)`, `T.goldSoft`, `rgba(74,124,89,.08/.1/.2)`, `rgba(155,69,69,.08/.2/.9)`, `rgba(76,175,80,.1/.85)`, `rgba(212,160,23,.15)`, `rgba(78,201,148,.1)`, `#FFEBEE`, `#FFCDD2`, `#E8F5EE`, `#FFF5F5` | 28 | ground | **the pink / green approve-deny chip and badge backgrounds** (`ActionChip` all three tones, `health` status cards, `dashboard` legend, `approvals` deny box, `images`/`featured` status pills) | **⊘-2 — no equivalent by law (R-40.129 ①)** |
| `AdminUI.Toast` grounds `#2A1010` (error) / `#0F1F14` (ok) | 2 | ground | the cockpit toast | `--atelier-sheet-bg` with a `--role-critical` / `--role-metal` dot — the shell's `WlToast` shape (R-40.129 ②) |
| `#25D366` + `rgba(37,211,102,.12/.35)` | 3 | ground · edge · ink | the WhatsApp **button** on `/admin/demo:810` | **ground, border and label — all three forbidden by R-40.129 ③**; becomes the shell's ghost button. No brand mark survives (palette handover §2 ③ precedent) |

### §3.5 · Hairlines, inputs, focus

| literal | sites | meaning | vendor token |
|---|---|---|---|
| `#E2DED8` | 80 (edge 41 · ground 39) | the cream hairline; and 39 times a **disabled/neutral button ground** | edge → `--atelier-card-border`; ground → `transparent` + `--atelier-card-border` edge (the shell's ghost shape; `opacity .5` for disabled, `.wl-btn:disabled`) |
| `T.border .10`, `T.borderStrong .18`, `--admin-hairline .10`, `-firm .18`, `rgba(255,255,255,.10/.12/.18)`, `rgba(240,234,224,.15)`, `rgba(248,247,245,.08)` | 12 + reads | hairlines, two weights | `--atelier-card-border` (`.11`) / `--atelier-sheet-border` (`.14`) |
| `--admin-input-bg`, `--admin-input-border .22` | 2 + reads | inputs | `--atelier-input-bg` / `--atelier-input-border` (teal at `.58` — the field edge goes teal, as the shell's does) |
| `--admin-sheet-border .14` | 1 | the sheet edge | `--atelier-sheet-border` |
| `T.borderFocus` oxblood `.55` | 1 + reads | the focus ring | the shell's `:focus-visible` outline in `--atelier-accent-text` |
| `--admin-row-hover`, `rgba(245,235,212,.03)`, `rgba(240,234,224,.04/.06/.12)` | 7 | hover grounds | `--atelier-row-hover` |

### §3.6 · THE NO EQUIVALENT ROWS — **ALL FIVE RULED, 2026-09-09**

**⊘-1 · The `<meta name="theme-color">` literal** — `layout.tsx:422` `content="#1F1612"`. A meta attribute cannot read a CSS variable, so this one site stays a literal **by mechanism**. Proposed: the value is `GRAPHITE['page-bg']` written at build (`import { GRAPHITE } from '@/lib/worklist/theme'` in the layout, the attribute reads the constant), and the E2 (i) census cell allows exactly this one site and asserts its value equals the token — `b59`'s second allowance class (the `/v/` preview frame, palette handover §6.1). **RULED: yes.** The literal is derived from `GRAPHITE['page-bg']` at build and the cell asserts the equality.

**⊘-2 · The tinted state grounds die** — 28 sites plus every `ActionChip` (12 sites), every `#C9A84C`/`#4A7C59`/`#9B4545` fill (23 sites), and the `#E2DED8` neutral button fills (39). Under R-40.129 ① a role is an ink and never a ground. Every approve / deny / revoke chip, status pill and alert box becomes **transparent ground · full-weight role edge · role ink** — the shape the vendor rooms' `Mark lost` and `Cancel` already draw, and the shape `.wl-btn.dan` draws (`StudioSheets.tsx:469`). The gold switch knob (`control-room:44`), the gold progress bar (`data:106`), the gold count pill (`photos:94`) and the gold approve button (`approvals:385`) lose their gold *fills*: knob and bar read `--atelier-accent-text` (the shell's affirmative), the pill becomes a `--role-metal` outline, the approve button becomes `.pri`. **RULED: all of it dies** (founder delegated to the chair, 2026-09-09). No row kept.

**⊘-3 · The gradients die** — the page ground (three espresso stops), the nav ground (two), `AdminUI`'s sheet gradient, the `StatCard` accent rule (`linear-gradient(to right, oxblood, transparent)`), the skeleton shimmers. The shell is flat: one `--atelier-page-bg`, one `--atelier-header-bg`, one `--atelier-sheet-bg`. The nav still reads darker than the page because `header-bg` (`#1A1B1D`) is lighter than `page-bg` (`#141516`) — **the relationship inverts**: today the sidebar is darker than the page; on the shell's tokens it is lighter, as the shell's own header is. Named because it is a layout-reading change a token swap forces (charter §4). The shimmer becomes a `--atelier-row-hover` sweep. **RULED: the gradients die; the shell is flat.**

**⊘-4 · The translucent blurred bar** (`--admin-bar-bg` at `.78` + `backdrop-filter: blur`) — the shell's header is opaque. The blur is a carried byte doing no work on a flat ground (R-40.60). **RULED: the blur dies; opaque `--atelier-header-bg`.**

**⊘-5 · The cockpit's own alpha-gold** (`--admin-metal-soft/-line/-wash`) — ruled by R-41.76 to `row-hover`/`card-border`; listed here only so the founder knows the gold *washes* behind the active nav item and the `StatCard` accent go neutral until R-40.133's tokens exist.

Nothing else in the cockpit carries a meaning the seven inks do not.

---

## §4 · (d) THE BUTTON CENSUS

**116 `<button>` sites in 27 files, and five ways of drawing one.**

| implementation | home | files | sites | how it paints |
|---|---|---|---|---|
| **`GoldBtn`** | `AdminUI.tsx:98` | 8 readers | 16 | oxblood fill, `T.ink` label, Jost 300 `.2em` uppercase, radius 10, min 44, `scale(.97)` press, oxblood glow |
| **`GhostBtn`** | `AdminUI.tsx:131` | 12 readers | 39 | transparent, `.5px` hairline (`T.borderStrong`; danger → `rgba(217,88,88,.4)` → `T.danger` on hover), Jost 200 `.2em`, radius 10, min 44 |
| **`ActionChip`** | `AdminUI.tsx:164` | 4 readers | 12 | **tinted ground** (`successSoft`/`dangerSoft`/`goldSoft`) + role edge + role ink, Jost 600 `.12em`, radius 9, min 44 |
| **`ActionLink`** | `AdminUI.tsx:215` | 2 readers | 2 | `ActionChip`'s twin as an `<a target="_blank" rel="noopener noreferrer">` (CE-225 fork C — the element swap is the point; kept) |
| **raw `<button>` with inline style** | 27 files | 27 | 116 | every one hand-authored: 19 distinct radii (`8`×45, `12`×38, `100`×36, `10`×27, `6`×26, `14`×26, `4`×23, `2`×19, `20`×18 …), 23 distinct `fontSize`s, 19 distinct trackings, cream / navy / espresso fills by page |

Raw `<button>` load by file: `approvals` 13, `discover-heroes` 10, `AdminUI` 9 (the primitives' own bodies + `UploadZone`/`ImageGrid`/`Counter` controls), `vendors` 9, `exploring` 8, `couples` 8, `layout` 7 (nav seats, drawer, sign-out, palette trigger), `images` 7, `preview` 5, `photos` 5, `demo` 4, `dashboard` 4, `collab` 4, `data` 3, `approvals/discover` 3, `messages` 2, `makers` 2, `featured` 2, `control-room` 2, `Bridge` 2, and one each in `login`, `hot-dates`, `health`, `dreamers`, `config`, `MintSheet`, `CommandPalette`.

**The one home (R-41.75): `app/admin/_components/AdminUI.tsx`**, re-authored to read the tokens. Its four primitives keep their symbol names — `GoldBtn` becomes the shell's `.pri` shape (accent ground, `--role-ink-deep` ink, 3px radius, `--wl-t4` `.08em`); `GhostBtn` the shell's `.gho` shape (transparent, `--atelier-card-border` edge, `--atelier-ink-soft` ink; danger → `--role-critical` edge and ink); `ActionChip` loses its tinted ground and becomes `GhostBtn`'s shape with the role on edge and ink (⊘-2); `ActionLink` unchanged in element, re-tokened in paint. The 116 raw `<button>`s are **re-tokened in place** (a value becomes a `var()`; radius, font and tracking take the shell's rungs per R-41.77) — **not** rewritten onto the primitives, because a rewrite is a reshape and the charter forbids it. Which raw buttons *should* become primitives is E2's per-group question for the chair, listed in each rider, never done silently.

**The shell's register cannot be imported** (`.gho`/`.dan` live in `StudioSheets.tsx:468-469`, F-41.47, the shell seat's); `AdminUI.tsx` carries the same four rules by value, cited to `WorklistShell.tsx:251` and `StudioSheets.tsx:468`, with a digest cell that reddens if either donor moves — the estate's CITATION-NEEDS-A-CELL law, `tokens.css`'s own header, which is exactly what stopped protecting it when the donor moved.

---

## §5 · (e) NOT A COLOUR, BUT A SECOND ARRANGEMENT

The shell's arrangement (`theme.ts:36-135`): **six type rungs, two families, one tracking (`.08em`, t4/t5 only), a 4/8 grid, 44 tap floor, 3px control radius, 64 tile.** The cockpit:

| dimension | shell | cockpit at tip | in scope (R-41.77) |
|---|---|---|---|
| font families | 2 (Cormorant t0/t1; DM Sans t2–t5). Jost and Italiana **retired** (R-38.4) | **Jost 211 sites**, DM Sans 159, Cormorant 55, `monospace` 2 (`AdminUI` code cells). Cormorant is used at 20–42px page titles and at 300 *italic* on the Bridge figures — the shell forbids italic in chrome | yes — every `fontFamily` becomes `font: var(--wl-tN)`; Jost goes |
| sizes | 6 (46/24/17/14/12/11) | **23 distinct** (7–42px): `9` ×143, `13` ×100, `8` ×93, `12` ×87, `10` ×56, `14` ×52, `11` ×46 … — the mode is **9px**, under the shell's 11px floor (`TYPE_FLOORS.label`), at 143 sites | yes — the nearest rung; 7/8/9/10 all rise to t5 (11px). **Forced change: every eyebrow and chip label grows.** Named |
| weight | 400/500 (body), 500 (feature) | 200/300/600/700 in play (`GoldBtn` 300, `GhostBtn` 200, `ActionChip` 600, `PageHeader` 700) | yes — the rung carries the weight |
| tracking | `.08em` at t4/t5 only | **19 distinct**: `.2em` ×70, `.15em` ×34, `.12em` ×30, `.18em` ×25, `.1em` ×25, `.14em` ×22, `.25em` ×17 … up to `.34em` | yes — `.08em` on uppercase controls and eyebrows; none elsewhere. **Forced change: the engraved register goes.** Named |
| radius | 3px controls; tiles/cards per `.wl-card`; `999` for the coin | **19 distinct** (`8`, `12`, `100`, `10`, `6`, `14`, `4`, `2`, `20`, `50`, `9`, `3`, `16`, `13`, `999`, `7`, `18`, `11`, `0`) | yes — 3px on controls; the card radius reads `.wl-card`'s (derived at E1, not assumed here) |
| tap floor | 44 | `GoldBtn`/`GhostBtn`/`ActionChip` honour 44; raw buttons at `height: 36` (`dashboard:135`), `padding: 7px 14px` at 9px (`vendors:122`) do not | yes — 44 on every control; R-37.73 ① |
| spacing | 4/8, gutter 16 | ad hoc (`padding: '13px 22px'`, `'9px 16px'`, `marginBottom: 32`, `gap: 16`) | named only; a spacing sweep is a reshape unless the chair rules it into a rider |
| motion | `cubic-bezier(0.22,1,0.36,1)` | the same easing, declared locally as `EASE` in `AdminUI.tsx:9` and inline in `data:106` | one home: `lib/vendor/tokens.ts EASE.luxury` exists; the cockpit re-declares it. Named |

---

## §6 · WHAT E2 (i) RE-POINTS — THE COUNT THE CELL MUST COVER

- **191 `--admin-*` reads** in six files (`layout` 46, `Bridge` 65, `MintSheet` 21, `CommandPalette` 16, `approvals/discover` 41, `AdminUI` 2) → `--atelier-*` / `--role-*`. `tokens.css` deleted; `layout.tsx:37`'s `import './_components/tokens.css'` deleted; `<style>{scopeCss('.adm')}</style>` mounted and the admin root wears `className="adm"` with `data-wl-mode="dark"` pinned (R-41.74). The `scopeCss` selector form is `${scope}[data-wl-mode="dark"]`, so the attribute is required, not optional.
- **493 `T.<colour>` reads** in 16 files keep their symbol and change their value: `T` is re-authored so `T.ink === 'var(--atelier-ink)'` etc. — a symbol becomes a `var()` string. This is why the cut is one rider per group and not one rider: the *value* moves at E2 (i) for every reader at once, and each group's rider is then the literal sweep plus the type/radius pass, walked separately.
- **The Switchboard's words** are now this seat's home (R-41.99): `lib/admin-api/switchboardCopy.ts` gains the four state words this frame needs — `Granted`, `Not filed`, `Not requested`, `Dark until A10` — into `STATUS_WORD`'s home, with one cross-seat note in the header naming seat C as the author and this seat as the successor. Cut in E2, not here.
- **The census cell** (`scripts/b6N_cockpit_ink_census.js`, number allocated by the chair, R-40.86) reads `codeOf()` over `app/admin/**` + `lib/admin/**`, reddens on any `#RGB`/`#RRGGBB`/`rgb()`/`rgba()`/`hsl()` outside the one `theme-color` allowance (⊘-1, asserted by value against `GRAPHITE['page-bg']`), any `--admin-` read, any `fontFamily:` naming a family, any `letterSpacing` other than `.08em`, any `borderRadius` other than the ruled set. It is scoped per E2 group by a path list and **must RED at `bc76e24`** — this document is the number it reds with: 855 literals, 191 `--admin-*` reads, 436 family names.
- **`tdw10_p2_retint.proof.mjs`** retires in the same rider with its reason in-file (R-41.78); the floor's NAMED BASE loses one red — the chair's re-base.

---

## §7 · DECLARED, NOT CLAIMED

- No capture in this packet. The shot arm's browser (`@sparticuz/chromium`) is installed at E1 or declared absent there.
- No contrast arithmetic is redone here: every proposed token already carries its measured ratio in `theme.ts` against the ground it is ruled for. E1's frames are the check; the founder's phone at 374 is the measurement.
- Two things outside this seat's lane, named so the silence is not misread: `lib/vendor/tokens.ts`'s four cream readers in `components/vendor` and `components/shared`; and F-41.47 (`.gho`/`.dan` in `StudioSheets`). Neither is touched by any E packet.

**Derived at `60e66cf94d19b1f7ab01e55b48f34988a01e0b1f`, `origin/main`, `dreamos-pwa`, 2026-09-09. Every number by command over the comment-stripped tree; none recalled. All five ⊘ rows are ruled and closed. STOP — E1's frames stand for the chair's veto.**
