# CE-41 · SEAT E · THE COCKPIT ON THE SHELL'S TOKENS — THE VETO SHEET (E1)

**Mock:** `docs/mocks/COCKPIT/cockpit-mock.html` · ten frames, eleven captures (`E1-grid` is primary and shot twice, 374 and 390) · authored at dreamos-pwa `60e66cf94d19b1f7ab01e55b48f34988a01e0b1f`, derived at origin.
**Not re-shot at this re-base.** The mock and its eleven captures are byte-identical to the `44b8590` cut (`cockpit-mock.html` sha256 `14e2169ec7964f9c…`); D1 moved no byte this file draws, so re-shooting would have produced the same PNGs and claimed a run that changed nothing. The frames were shot once, at `44b8590`, by the estate's own arm.
**Shot by**, `tools/mock_shot.cjs`, `@sparticuz/chromium` 149 from npm, `HeadlessChrome/149.0.7827.0`. Dark only: R-41.74 pins the arm's mode and the file's script never sets `light`.
**Rulings this sheet stands on:** R-41.71 amended by R-41.77 (type and radius in scope) · R-41.72/.73 (the home; `scopeCss('.adm')`; `tokens.css` deleted) · R-41.74 (Graphite only) · R-41.75 (`AdminUI.tsx` is the one control home) · R-41.76 (the alpha-gold mapping) · R-41.82 (the Switchboard is re-shaped) · R-41.89 (persona names on Model routes only) · R-41.98 (rows are verb + state, no prose) · R-41.99 (seat E owns `switchboardCopy.ts`) · R-40.129 (seven inks) · R-40.60 (no decoration) · the wallet law.
**Findings minted from the chair's range F-41.66–F-41.75:** F-41.66, F-41.67, F-41.68 below. Seven numbers unspent.

**What is NOT open here.** Every colour value: they are the shell's, ratified at CE-40, and this sheet renegotiates none. The five ⊘ rows: ruled 2026-09-09 and closed in the census. C3's gate wording (`switchboardCopy.ts`) where this sheet does not name a change — seat C's vetoed bytes stand.

---

## §A · THE SHELL — `E1-grid`, `E1-grid-sheet`, `E1-grid-wide`

| # | Where | Proposed | Current |
|---|---|---|---|
| 1 | Top bar ground | opaque `--atelier-header-bg`, **no blur** | translucent `.78` + `backdrop-filter: blur(20px)` |
| 2 | Wordmark | **The Dream Wedding · cockpit**, the separator in `--role-metal` | same words, gold at `.55` |
| 3 | Sidebar ground (1180) | `--atelier-header-bg` — **lighter** than the page | espresso gradient, darker than the page |
| 4 | Active room | `--atelier-accent-text` on `--atelier-row-hover` | oxblood on a gold wash |
| 5 | Phantom rooms in a domain sheet | shown, `--atelier-ink-fade`, state word **Not wired** | not shown |
| 6 | Figure tiles | `.wl-tile` — card ground, 3px, the figure at `--wl-t1` | 8–12px radius, 4 sizes |
| 7 | A figure that did not answer | **—**, never a zero (the Bridge's own law, carried) | unchanged |
| 8 | Degrade band | `--role-critical` on edge and ink, no tinted ground | tinted ground |
| 9 | Bottom domain bar | the shell's `.wl-nav`, 56px seats, active seat in accent | blurred bar, oxblood active |

## §B · THE LIST ROOM — `E1-queue`, `E1-queue-sheet`, `E1-queue-wide`

Seat A's room **re-tokened, not re-shaped** (kickoff §5). No door, no copy, no layout moved.

| # | Where | Proposed | Current |
|---|---|---|---|
| 10 | Row card | `.wl-card` — card ground, 3px, two lines, state right | 10px radius, same two lines |
| 11 | Status word | `Open` `Forwarded` `Closed` in `--role-caution` / `--role-metal` / `--role-positive`, **ink only** | same words, `T.gold` fill on Forwarded |
| 12 | Every figure | `Rs 1,20,000.00` — wallet law, unchanged | unchanged |
| 13 | Refusal | `--role-critical` edge + ink, one sentence, C3's words | tinted ground |
| 14 | `+ Type a request` | primary (accent ground, `--role-ink-deep` ink) | oxblood fill |
| 15 | `Forward`, `Close request` | ghost / ghost-danger | ghost, oxblood-tinted |

## §C · THE SWITCHBOARD, RE-SHAPED — `E1-switchboard`, `E1-switchboard-wide`

R-41.82. Grouped by room. **One row = a verb phrase + the dotted spec + a state word** (R-41.98).

| # | Where | Proposed | Current |
|---|---|---|---|
| 16 | Groups, in order | **Payment reminders · Contracts · Concierge · Reviews · Google · Instagram · Ads · Introductions · Wedding reel · Model routes · Notices** | four register kinds (`Features you switch on`, `Message templates on Meta`, `Meta app permissions`, `Google access`) |
| 17 | Group head, right | the count — `2 of 2 shown`, `5 rows · none filed` | none |
| 18 | Row, line one | the verb: **Send payment reminders** · **Ask the couple for a Google review** · **Alert an outside vendor** · **Verify a vendor's website** · **Manage a vendor's Business Profile** · **Read a vendor's Instagram account** · **Reply to Instagram DMs** | C3's names, which are nouns: *Payment reminder*, *Google review ask*, *Outsider join alert* |
| 19 | Row, line two | C3's dotted spec, unchanged: `to the client · couple line · Utility` | same |
| 20 | Row, line three | the state word + one fact: `On · since 6 Sep` · `Approved · checked 07:40` · `Dark until A10` · `Not filed` | status word + the register key |
| 21 | The register key | **off the row**, one tap deep with the evidence; the palette still matches it | on the glass under every name |
| 22 | The walk seal field and *Switch on automatically once approved* | **one tap deep** with the evidence | on the row |
| 23 | The row's controls | **the switch only** — `On` / `Off`, plus `Check now` where a gate is probeable | switch + seal + auto-on + check |
| 24 | Evidence, one tap deep | `Meta's words, the key and the seal ›` opening a `--atelier-section-bg` panel: the template name, status, category, the body, the filing date and ID, the key, the seal field, the auto-on hand | a separate Meta table at the foot of the page |
| 25 | The palette's landing | seat C's F-41.53 lit hairline, re-tokened to `--atelier-accent-text` | oxblood |
| 26 | Search | one field, any word — a room, a recipient, a Meta name, the key | same behaviour, `gateMatches` unchanged |

**Four state words are new and have no home yet** (R-41.99 gives this seat the file): **Granted · Not filed · Not requested · Dark until A10**. They go into `switchboardCopy.ts` beside C3's `STATUS_WORD` in E2, with one header note naming seat C as the author and this seat as the successor. Vetoing them here vetoes the words that ship.

## §D · MODEL ROUTES — `E1-switchboard-model`

Seat F's plane; this seat draws the room. Live value from F1's read door over `admin_config` `model.*`; the switch writes live and busts the router cache.

| # | Where | Proposed | Current |
|---|---|---|---|
| 27 | Group head | **Model routes** · `5 surfaces` | none — the group does not exist |
| 28 | Surface rows | **Answer vendors on WhatsApp · Answer vendors in the app · Answer couples on WhatsApp · Write the marketing outreach · Harvest prospects** | none |
| 29 | Tiers, beneath a surface | **Trial · Essential · Signature · Prestige · Advisor** (the router's five, not the makers page's four) | none |
| 30 | Role rows, under a tier | **Victor** and **Donna**, each its own 44px row with its own two-way switch — the only chrome in the estate carrying a persona name (R-41.89) | none |
| 31 | Live value, in words | `Anthropic · code default` · `DeepSeek · changed 4 Sep` | none |
| 32 | Divergence | chip **Differs from code**, `--role-caution` edge and ink, on the tier head | none |
| 33 | The env banner | one sentence: **LLM_PROVIDER is set to deepseek on the server, so every switch below shows what the code would do, not what it is doing.** | none |
| 34 | The couple line | **Read-only** — the switch drawn as an outline, not a fill; `BRIDE_LLM_PROVIDER decides` | none |
| 35 | Marketing roles | **Mira** and **The nudge** | none |

## §E · THE CONTROL FAMILY — `E1-family`

R-41.75. `AdminUI.tsx`'s four primitives keep their symbols; the paint is the shell's, cited to `WorklistShell.tsx:251` and `StudioSheets.tsx:468`.

| # | Where | Proposed | Current |
|---|---|---|---|
| 36 | `GoldBtn` | accent ground, `--role-ink-deep` ink, 3px, `--wl-t4`, `.08em`, 44 min | oxblood fill, 10px, Jost 300, `.2em`, oxblood glow |
| 37 | `GhostBtn` | transparent, `--atelier-card-border` edge, `--atelier-ink-soft` ink | transparent, oxblood-tinted hairline |
| 38 | `GhostBtn danger` | `--role-critical` edge and ink | `rgba(217,88,88,.4)` edge |
| 39 | `ActionChip` | **transparent ground**, role edge, role ink | tinted ground (dies, ⊘-2) |
| 40 | Focus | 2px `--atelier-accent-text`, offset 3 | oxblood `.55` border |
| 41 | Toast | `--atelier-sheet-bg`, sheet edge, a role dot | `#2A1010` / `#0F1F14` tinted grounds |
| 42 | Chips | `Approved` `Paused by Meta` `Rejected` `New vendor` `Differs from code` `Not filed` — edge + ink | tinted |
| 43 | Two-way switch | new: segmented, the live side accent-filled; the read-only variant outlined | none |
| 44 | Field | `--atelier-input-bg`, `--atelier-input-border` (teal) when focused | warm-white edge |

---

## §F · FINDINGS THIS SEAT FILES AGAINST ITS OWN FRAME AND THE INSTRUMENTS

| # | Finding | Where |
|---|---|---|
| **F-41.66** | The disabled primary reads as a filled slab. `.wl-btn:disabled{opacity:.5}` over an accent ground gives a muddy teal that still reads as the page's main action; at 374 the disabled *File the request* is as loud as the live one. The rule is the **shell's own** (`WorklistShell.tsx:255`), so the defect is the shell's and this frame only exposed it. **Proposed for the cockpit:** a disabled primary drops to the ghost shape at `.5`, not the filled shape at `.5`. The shell's own cure is the shell seat's. | `E1-family`, first row |
| **F-41.67** | The Model routes tier row overflowed at 374 in the first cut: three columns (tier · switch · hand) in a 328px box clipped the DeepSeek segment off the glass. **Cured in this ZIP** — the tier is a heading and each role is its own 44px row. Filed because the first frame was shot before it was seen, and a frame that was wrong once is a finding even when the cure lands in the same packet. | `E1-switchboard-model` |
| **F-41.68** | `scripts/tdw10_p2_retint.proof.mjs` reads **21 of 76 RED** at `60e66cf` and sits on the floor's NAMED BASE as a known red: `tokens.css` cites `lib/vendor/theme.ts DARK` as donor for all 34 roles, the donor moved to Graphite at CE-40, and one cell (`--admin-bg-bot`) computes `NaN`. R-41.78 retires the cell with its subject in E2 (i). Filed so the retirement is a ruled act with a number, not a silent deletion. | `scripts/tdw10_p2_retint.proof.mjs` |

## §G · THE SHOT LIST

| frame | width(s) | what it is for |
|---|---|---|
| `E1-grid` | 374, 390 | the shell and the Bridge — the primary |
| `E1-grid-sheet` | 374 | a domain opened; phantom rooms shown greyed |
| `E1-grid-wide` | 1180 | the sidebar; the ⊘-3 lightness inversion |
| `E1-queue` | 374 | the list room re-tokened |
| `E1-queue-sheet` | 374 | the Forward door and the refusal |
| `E1-queue-wide` | 1180 | the same room in the content column |
| `E1-switchboard` | 374 | the re-shape: two groups, the evidence open and closed |
| `E1-switchboard-model` | 374 | Model routes, five surfaces |
| `E1-switchboard-wide` | 1180 | Google and Instagram as rooms |
| `E1-family` | 374 | every control, alone |

## §H · WHAT IS NOT IN THIS SHEET, AND WHY

- **No 1180 capture of the family or the model group.** The founder's measurement is the phone; the wide frames exist to prove the sidebar and the row width, not to be vetoed twice.
- **No light arm.** R-41.74. The shell's Chalk tokens are present in the file's token block because it is transcribed verbatim from one home, and the script never sets `light`.
- **No new colour anywhere.** The mock holds five hex literals, all in the gallery chrome around the frames (the page ground, the caption ink, the sheet's rule) — inside every `.wl` frame, every paint is a `var()`. That is checkable by command on the file and is the same property E2's census cell will assert on the code.
- **No layout beyond what the tokens force.** Each forced change is a numbered row above: #1 (the blur), #3 (the inversion), #6 (the tile), #21–#23 (the row stripped to switch + state, which is R-41.98's, not the palette's), #29–#30 (a group that did not exist).

**Authored at `60e66cf94d19b1f7ab01e55b48f34988a01e0b1f`, `origin/main`, `dreamos-pwa`, 2026-09-09. STOP — the chair's veto (R-41.98 delegation). No cut before it.**
