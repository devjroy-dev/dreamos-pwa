# TDW_09 · PACKAGE 2 · PHASE C — ZIP 3 · THE SANCTUARY PRESSED, AND PACKAGE 2'S CLOSING LEDGER

**Base:** `dreamos-pwa @ 77635cc`. **Under:** the twenty-fifth chair · P2C sweep relay #6.
**Scope:** limbs ④ ⑤ ⑦ ⑧ ⑨. `sanctuary/page.tsx` · `scripts/tdw09_p2c.proof.mjs` (new) · this document.
**Gate:** `npx tsc --noEmit` → **0**. Every bench in `scripts/` run bare; exit codes read, not verdict lines.

> **THE SENTENCE THIS DELIVERY PUTS IN THE RECORD:**
> **A roster is a claim until a compiler has disagreed with it.**
> ④'s ruled ten-map census died on TS2304, at a tree restored byte-clean, before a byte was written.

---

## 1 · THE TIP, RESTATED — three non-sweep deltas, not one

The charter expected ZIP B alone above `6de3d6c`. Three sit at this seat, attributed by hash:

| hash | sitting | bearing on Package 2 |
|---|---|---|
| `77635cc` | TDW_10 Tier & Money ZIP B | three `app/admin` pages deleted · `settings/page.tsx` +110 · `tdw10_p2_retint` amended to **76/76** |
| `8c1351a` | TDW_09/10 walk hotfix micro | below ZIP 1; already in its base |
| `7e39385` | TDW_10 billing A4 rider | below ZIP 1; already in its base |

All three are **outside Package 2's story**. ZIP B's expected marks reproduce exactly.

## 2 · THE FLOOR AT DELIVERY — every bench in `scripts/`, run bare (F-09.92)

**Three reds, every one attributed. Forty-one green. tsc 0.**

| bench | verdict |
|---|---|
| **tdw09_p2c** | **GREEN 53/53** — NEW, this delivery |
| tdw09_landing 103/103 · tdw09_roles 131/131 · tdw09_p2_doors 86/86 · tdw07_p4b_body 133/133 · tdw07_p3_portfolio 119/119 · tdw07_p2_profile 48/48 · tdw10_tier 98/98 · tdw10_p2_retint **76/76** · tdw10_p2_bridge 44/44 · tdw09_hotfix 38/38 · tdw_f0770_authority 104/104 · tdw07_f0772_circle 128/128 · and the rest | GREEN |
| `tdw09_type` **13/16** | **RED — F-09.105, fenced-attributed**, carrier NAMED: Billing v2 |
| `tdw10_p3_deck` 191/193 | RED — F-10.62, attributed, byte-stable |
| `tdw_f0774_stripper` 33/35 | RED — F-10.49, attributed, byte-stable |

**A RUN-NOTE THAT IS LOAD-BEARING AND WAS NEARLY A FALSE FINDING.** `tdw_f0774_stripper` §5.1 named-skips when the dream-os sibling is not at `../dream-os`, and the bench then reads **32/34** rather than 33/35. My first clone sat at `../os` and I was one step from filing the environment as a defect. **The floor's stated number presumes the sibling clone at its expected path.** Whoever runs this floor next inherits that condition.

**THE CLASS SENTENCE EARNS ITS THIRD SPECIMEN, AND A PROMOTION IS PROPOSED.**
ZIP 1 minted it: *a sitting's floor list is the benches its DIFF can redden, never the benches its charter names.* F-09.93 (P2A), F-09.94 (P2B), and now **F-09.105 (ZIP B)** — three sittings paid for the narrower reading inside one week. **Proposed for promotion to standing law: a delivery's floor is every bench in `scripts/`, run bare.** The cost of the wide reading is four minutes of wall clock. The cost of the narrow one is now three findings.

## 3 · LIMB ④ — THE SANCTUARY PRESSED · NINETEEN HAND-VERIFIED EDITS

**The hook.** A local `usePress()`, minted with reasons rather than as a rules-list (LD-5). It carries why a hook and not a polymorphic component — nineteen controls are 13 `<div>`, 5 `<button>`, 1 `<a>` across nine owning components, and a wrapper would have to reproduce three element contracts to give them one shared behaviour; state is the only thing they actually share. `pressedStyle` is **imported** from `@/lib/vendor/controls`, never re-rolled: hand-rolling a copy of an existing primitive is the F-07.52 class L3 retired one delivery earlier. It carries its F-06.85 mechanism note — the pressed *values* live in the primitive, the hook owns only *which* control is lit — and its header **does not quote the roster needle**, de-fanged by construction per the predecessor's disclosure #2.

**The keying, as ruled at relay #6.**

| class | n | keys |
|---|---|---|
| MAP — composed on the element's own loop discriminator | 7 | `mode:${mode}` · `member:${m.id}` · `muse:src:${f.value}` · `muse:cer:${f.value}` · `event:${ev.id}` · `mood:${mood.key}` · `slice:${slice.key}` |
| STANDALONE — static per-site | 12 | `settings:wa` · `settings:signout` · `disc:namebar` · `disc:blind` · `disc:filter` · `disc:save` · `circle:contacts` · `circle:add` · `pages:cta` · `concierge:compact` · `concierge:full` · `slice:dream` |
| HELD — F-09.107 | 1 | `Row` — zero bytes |

**A COLLISION THAT FELL OUT OF BUILDING THE SPLIT RATHER THAN COUNTING IT.** The two Muse filter maps *both* discriminate on `f.value`. Per-map, each is internally correct; shared, one key would light a source pill and a ceremony pill together. They take **distinct namespaces** (`muse:src:` / `muse:cer:`). No count-based cell and no per-map check can see this — §4.7 exists because the edit surfaced it.

Verified mechanically: **19 `press()` · 19 `pressed()` · 9 hook seats · tsc 0.**

## 4 · LIMB ⑤ — THE CENSUS, CLOSED · THREE COLUMNS, THREE METHODS

### 4.1 The pressed roster — the 29 + 3 − 2 = 30 chain, reproduced

| method | figure |
|---|---|
| roster grep (`WebkitTapHighlightColor` ∧ file lacks `pressedStyle\|onPointerDown`) | **29 sites / 7 files** — reproduces at this tip with **zero drift** |
| + sites the dream-os instrument counts in CARRIER files the roster excludes by predicate | **+3** (`app/vendor/page.tsx` · `app/vendor/portfolio/page.tsx` · `components/vendor/BottomNav.tsx`) |
| − sites the instrument is BLIND to (F-09.95's string trap) | **−2** (sanctuary `:3608` · `:3615`) |
| = instrument headline `tap_suppress` | **30** ✔ |

**F-09.95'S BLINDNESS, STATED BY NUMBER, WITH THE LABELLED AMENDMENT.** `tools/tdw09_stage2.py:51` treats the `/*` inside `accept="image/*"` as a comment-open. **147 lines of `sanctuary/page.tsx` are SPANNED by the two phantom blocks; 143 are FULLY destroyed** — the labelled amendment against ZIP 1 §5's figure of 145. Every headline that instrument emits over this file — `below44`, `interactive`, `height_decls`, `pressed`, not only `tap_suppress` — is computed over a file it cannot fully see. The cure is a dream-os byte and remains **STOP-and-fork** from a pwa-only phase. This delivery's own bench carries the trap specimen by name (`§0.W`) at the site that proves it harmless.

**After this delivery the roster reads 6 sites / 3 files — all demo, all F-09.89-held.** Sanctuary has left the roster by its own carrier predicate (§4.8).

### 4.2 The ink census — settled by the stripper, not by grep

| bucket | code-only | disposition |
|---|---|---|
| **in-radius** | **0** | all 34 adopted; residual zero |
| demo-held | **25** | F-09.89, frozen until the twin's rider |
| canon homes | **7** | `globals.css` 6 · `theme.ts` 1 — lawful, fenced in cells §1.1/§1.5 |

**A CORRECTION OWNED IN-BAND.** My first census was a raw grep and read demo=24, canon=13, total=37. It was counting the estate's own **cure comments** — the sentences that quote the byte they retired. Run through `stripComments`, it settles at 25/7/0 and reconciles exactly with ZIP 2's roster. *A census that counts its own explanations is the F-07.74 species with the polarity reversed.* Corrected before it reached a cell.

**F-09.101 holds at this tip:** the lowercase `#1a120e` sighting is now **0**; the tree carries the uppercase form only. Every hex cell in the new bench is case-insensitive and carries the uppercase canary (§1.2) that proves the case-blindness is real rather than asserted.

### 4.3 The `#111` / `#111111` stragglers — radius 25, annotated

Ruled IN at relay #6, honesty over tidiness, listed **KNOWN-STANDING for admin P6**:

```
  4  app/vendor/studio/team-payments/page.tsx      2  app/vendor/studio/team/page.tsx
  3  components/vendor/NotesBody.tsx               2  app/components/CitySearchDropdown.tsx
  3  components/vendor/AddSheet.tsx                1  components/vendor/CalendarDaySheet.tsx
  3  app/vendor/studio/tasks/page.tsx              1  components/vendor/CalendarBlockSheet.tsx
  2  components/vendor/CalendarCrewSheet.tsx       1  app/(frost)/.../sanctuary/page.tsx
                                                   1  app/(frost)/.../muse/page.tsx
                                                   1  app/(auth)/couple/onboarding/page.tsx
  ── 1  app/globals-v2.css  ◄ THE ONE NON-TSX HOME. Liveness UNDERIVED-STATED:
        whether this stylesheet is loaded by any live route was not derived at
        this seat and must not be assumed by the sitting that picks the list up.
```

Excluded by wall: `app/admin/**` (post-ZIP-B state) and the demo organs.

### 4.4 P-1(a) — CORPSE DUTY DISCHARGED WITH DERIVATION

Corpse count **0**, asserted with evidence rather than assumed. Every primitive's first production consumers:

| primitive | live consumers at this tip |
|---|---|
| `pressedStyle` | `app/vendor/page.tsx` · `components/vendor/BottomNav.tsx` · `components/vendor/ChatThread.tsx` · **`sanctuary/page.tsx`** (this delivery) |
| `touchBox44` | `components/vendor/ChatThread.tsx` (three chips) |
| `rowBaseline` | `app/(landing)/page.tsx` |
| `rowGlyphSlot` | `app/(landing)/page.tsx` |

The CE-201 named-carrier comment in `controls.ts` named Package 2 as the carrier for all three. **The carrier delivered.** No primitive is left consumer-zero; no retirement line is owed.

## 5 · LIMB ⑦ — THE CLASS BENCH · `scripts/tdw09_p2c.proof.mjs` · 53/53

Organised by **adoption class**, not by site: a per-site bench is sixty cells that fail together and none of which say why.

| § | class | what it defends |
|---|---|---|
| §0 | canary | the stripper must not swallow live code — incl. `0.Z INVOCATION` (F-07.99) and `0.W`, which proves the subject really carries the trap specimen |
| §1 | ink | `INK_DEEP` a CONSTANT not a field · **§1.3 the donor cell `theme.ts:94` cites by number** · fenced canon home, F-09.35 by sameness |
| §2 | ink-on-metal | the conditional role **must never flatten** · **§2.4 the 2.05:1 dark guard, derived** · §2.6 the SVG stroke site takes the ternary, because `var()` in a presentation attribute renders as nothing |
| §3 | coin | F-09.100 across **all three gradient stops, parsed from the stylesheet** |
| §4 | pressed-keyed / map-discriminator | seven map keys · **§4.5 the negative** convicting a React-keyed element that presses on a bare literal · §4.6 its non-vacuity · §4.7 the cross-map collision · §4.9 F-09.107's hold |
| §5 | pressed-boolean | the lawful single-control case, with the condition that would un-lawful it named in-cell |
| §6 | touch | **three** chips (F-09.103) · heights unmoved · the no-op law that makes that possible |
| §7 | baseline | L3's retirement · `alignSelf:'center'` pinned at the primitive, its owner |
| §M | absent-subject | fail-closed, refusal path DRIVEN not merely held |

**THE STRUCK CENSUS, RESTATED AT 7.** `20 style spreads / 20 handler spreads` greens on a build where one press lights ten siblings. It was struck before a byte of ④ was written. What replaced it is structural and it is what §4 asserts.

**MUTATION LEDGER — eight, every one run, production source `cmp`-restored byte-identical. Cured copies banked before any revert.**

| W | mutation | cell | result |
|---|---|---|---|
| W-1 | a map site re-keyed to a bare literal | §4.4 + §4.5 | **51/53 — bites twice** |
| W-2 | `pressedStyle` hand-rolled locally in sanctuary | §4.2 | 52/53 |
| W-3 | one hook seat removed | §4.3 | 52/53 |
| W-4 | `--role-ink-on-metal` flattened to one value | §2.2 | 52/53 |
| W-5 | `brass` LIGHT moved off `#C9A84C` | §1.3 | 52/53 |
| W-6 | the third chip's `touchBox44` removed | §6.1 | 52/53 |
| W-7 | `rowGlyphSlot` re-defined locally on the landing | §7.2 | 52/53 |
| W-8 | `Row` adopted despite F-09.107's hold | §4.9 | 52/53 |

## 6 · DISCLOSURES — OWNED BY NAME, NONE QUIETLY PATCHED

1. **My §3.3 hardcoded three gradient stops from memory and went red against a correct tree.** The bench was wrong, not the product. Re-authored to **parse** the stops out of `globals.css`, so a re-tint re-aims the arithmetic instead of leaving a stale pass. A stop list typed into a bench is the same class of assumption as a column name typed into SQL.
2. **W-8 did not bite on first draft — a vacuous green, self-caught by running it.** My §4.9 used one regex spanning `const Row = (...)` with `[^)]*` for the props; the props type contains `()=>void`, so the group closed early and **the pattern matched nothing in either direction**. Re-authored to read the block structurally, with §4.9a asserting the declaration was actually found. *A cell that cannot fail is not a cell.*
3. **My first ink census counted the estate's own cure comments** (§4.2) — corrected through the stripper before it reached a cell.
4. **I nearly filed an environment artifact as a defect** — `tdw_f0774_stripper` at 32/34 because my clone sat at `../os`. Derived the skip's cause instead of reporting the number.
5. **My first floor read used a `grep | tail` for verdict lines and mis-read six benches** (`tdw09_p2_doors` as 5/5 against its true 86/86). Re-derived from each bench's own verdict line, then settled a second time by **exit code** — a different failure mode, per the independent-method law.

**Three of my five were caught by running rather than by reading.** The pattern is now four sessions deep in this package.

## 7 · LIMB ⑧⑨ — PACKAGE 2'S CLOSING LEDGER

### 7.1 The arc

**A** five stable doors (R-X27a), mode+pager retired, storefront door, F-09.89 held · **R1** the ledger is the books door, crest retired, risen chat names Victor's room — **commit-grade reconstruction, provenance labelled at that grade: `5c88e8a` shipped zero `docs/`, and no retroactive handover was authored** · **B** profile story + tag fold, vocabulary home + arbiter · **R2** risen masthead one register, F-09.92 · **ZIP 1** the floor made honest · **ZIP 2** the ink made honest · **ZIP 3** the sanctuary pressed.

### 7.2 Findings .89 → .107

`.89` demo organs held · `.90` mode vocabulary · `.91(b)` the books door · `.92` bare proof lines · `.93` P2A's false caller-zero · `.94` P2B's meter move · `.95` the stripper trap's uncovered species · `.96` the retired gold literal · `.97` the dead submit edge · `.98` cited at L3 · `.99` the kickoff's underived ink headlines · `.100` the coin · `.101` the needle's case · `.102` ink on a ground that themes · `.103` the third chip · `.104` the mint that broke a proxy · **`.105` ZIP B's body-floor breach** · **`.106` the promoted provisional roster** · **`.107` the Row with no live handler**.

**Register: `.105–.107` spent · `.108–.110` remain, unspent and returned.**

### 7.3 The six chair defects

1. `「 landing AT them 」` — dropped parenthetical; R-X24 rules ON-the-line.
2. The kickoff's underived ink headlines — **F-09.99**.
3. The `「 couple-lane 」` founder sentence; truth: all 34 vendor-lane.
4. The dropped `「 + pressed state 」` clause — since ruled in.
5. The lowercase needle — **F-09.101**.
6. **The provisional ten-map roster promoted to a ruled census by chair hand — F-09.106**, owned at relay #6 §0, falsified TS2304-grade at a tree restored clean.

Five predecessors caught five. The charter asked for a sixth and named the standard: evidence over narrative, *including the charter*.

### 7.4 The predecessors' disclosures

ZIP 1's five and ZIP 2's seven ride their own handovers at `2452eb5` and `6de3d6c`, indexed here, not restated. ZIP 2's seventh names the species this delivery was built to avoid: **a roster derived by text pattern rather than by structure.** ④ was ruled to hand-verified edits on that ground, and F-09.106 is that same species found one layer up — in the ruling itself.

### 7.5 L3, recorded

O-1's hand-rolled duplicate retired onto its primitive (F-07.52 live again, now dead). `alignSelf:'center'` is the primitive's law and it moves the flag — the founder's **specimen ② baseline shot** is the acceptance. `「 SHOT ① 」` cured to `②` citing F-09.98. **The label collision ZIP 2 declared UNRESOLVED remains the chair's**: the file already carries a `SHOT ②`, and applying the ruling literally yields two. Not applied here either.

---

## 8 · THE FOUNDER'S CARD — C④ · walked against the recorded account

Account: **couple `9f1f84d5-e688-4d4f-9e44-9f5da6315e52` · login `+919625759924` · Dev Test 23 · Jaipur** (ZIP 2 §9, already founder-run; not re-asked).

**What only your device can witness, named per the provable-equivalent doctrine:** the bench proves each press key *references* its own discriminator. **It cannot prove the sibling does not light.** That is FEEL, and it is yours.

1. **Sanctuary → Pages → "How are you feeling?"** Press and HOLD one mood dot. **The one you are holding should dim and shrink very slightly. The other eleven must not move.** ← the whole ruling, in one glance.
2. **Sanctuary → Muse.** Press and hold one Source pill. **Only that pill.** Then one Ceremony pill — **the Source pill must stay still.**
3. **Sanctuary home.** Press and hold one slice row. **One row.** Then press **Dream Ai** at the bottom — it should acknowledge too.
4. **Sanctuary → Circle** (or People). Press a member row, then the dashed **+**. Both acknowledge, independently.
5. **Vendor chat.** Press each of the **three** chip kinds — a clarify chip, the report chip, a suggestion chip. All three acknowledge; **none of them changes size.**
6. **Vendor calendar, Editorial Paper.** Glance at today's coin: **the numeral must read cream, not near-black.**
7. **Landing, country row.** Specimen ② baseline shot — the small text ON the line.

**Bench-proven only, no thumb-path:** the M-3 report chip's touch box, and every contrast ratio in §5 — those are arithmetic, and the arithmetic is in the bench.

**Package 2 seals at your combined thumb walk: A's five rows · B's smoke · C④ above.**

---

## 9 · WHAT IS NOT IN THIS ZIP

`app/admin/**` (post-ZIP-B state) · `app/vendor/settings/page.tsx` · the six demo organs · **dream-os: zero bytes** — F-09.95's instrument cure remains STOP-and-fork. **Copy: ZERO** vendor-facing or model-voiced bytes; no veto slot is owed.

**F-09.105's carrier is Billing v2, chair-named at relay #6.** Until it lands, `tdw09_type` stands **13/16 fenced-attributed** on every floor list.

Sequencing beyond this sitting is the founder's.
