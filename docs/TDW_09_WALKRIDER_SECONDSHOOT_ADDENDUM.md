# TDW_09 · WALK RIDER · SECOND-SHOOT ADDENDUM — the Discover page's own species members

**Base:** `8c6cb6d` (walk rider live; founder re-shoot: screens 1–3 PASS). **Radius:** `app/vendor/discover/page.tsx` + the walkrider bench (labelled growth 15→20). Founder words executed: the ink question (「 is the yellow supposed to be so light? 」 — no) and the copy byte 「 As couples will see it, curated. 」 verbatim.

## The cures
1. **`:236`** — the portfolio-ledger labels (`PIECES · APPROVED · PENDING · HELD`): `rgba(201,168,76,0.72)` → `A.brassWarm`. F-09.87's species, this page's member.
2. **`:298` + `:318`** — BOTH state-card italics: pinned cream `rgba(240,230,210,0.8)` → `A.inkSoft`. The founder shot the Approved card; the Under-Review card was one state away from the same invisibility — cured together, disclosed. (`:300`'s 「 reviewed by Swati 」 SIGHTED-NOT-TOUCHED — F-09.9's queued family; bench §6.5 guards it against out-of-sitting deletion.)
3. **`:419`** — 「 As couples will see it, curated. 」 replacing the brides line whole, founder byte verbatim.
The hero's scrim-grounded pinned creams STAND (per-site law; §6.3 asserts exactly one 0.78 survives).

## Proof
`walkrider` **20/20** cured · uncured at pristine `8c6cb6d`: **17/20** (§6.1/.2/.4 RED — both-ways). `roles` 131/131 (the scrim exemptions unmoved). tsc 0. Floor swept green EXCEPT:

## ⚠ CROSS-SESSION FINDING FOR THE CHAIR — the deck bench is red at P3's own tip
`tdw10_p3_deck` = **191/193 at PRISTINE `8c6cb6d`** (derived by reverting my file — identical 2 FAILs with and without my cures; not this delivery's). Mechanism derived: P3's rider 5 reordered the state disjunction to `state === 'hidden' || state === 'revoked'` (`page.tsx:325`) while two deck cells still assert rider 4's operand order `'revoked' || 'hidden'` (`deck.proof.mjs:477,:482`). The behaviour the cells guard STANDS (Re-apply renders, both wordings live); the cells' byte-pattern drifted — an order-sensitive regex is pattern-over-shape, the path-over-range law's cousin. **The cell amendment is P3's to make in its own sitting; this delivery touches none of P3's cells.** Reported per §0.2, never adapted around.

## Founder smoke — one glance on Paper
`/vendor/discover`, scrolled: the four ledger labels legible (oxblood, not pale gold) · the state card's italic sentence readable · the sub-line reads 「 As couples will see it, curated. 」. Dark stays your glance: the labels render brighter gold there — the same lawful delta family you accepted on screen 3.
