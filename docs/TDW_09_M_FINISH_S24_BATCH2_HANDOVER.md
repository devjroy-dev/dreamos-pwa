# M-FINISH · S2/4 · §4-4 BATCH ② — TEAM · CONTRACTS · TDS, AND THE OFFSET THAT NEVER TRAVELLED

**BASE (R-38.15): `735ed4c` = `origin/worklist`, re-derived at the moment of cutting.**
Sibling `dream-os` `1f8bab9` = `origin/main`, clean, `node_modules` present in both.
**Railway/Vercel green: NOT CLAIMED** — nothing here ran against a deploy.

---

## §0 · ENVIRONMENT

```
bash tools/preflight.sh worklist
```

```
pwa worklist 735ed4c · dream-os main 1f8bab9 · both clean · node_modules present
```

**PREFLIGHT REFUSED FIRST, AND IT WAS RIGHT TO.** The sibling was one commit behind
(`1f8bab9`, a TDW_19 spec amendment). F-38.34's whole lesson is that a floor derived against
a stale sibling is a number about a different tree, and three benches read it. Reset,
re-derived, and only then was a number written down.

**Floor at base: 22, set byte-identical to `a22e391`.**

---

## §1 · F-38.59 · THE RULE WAS WRITTEN CAREFULLY AT ONE SITE AND NEVER TRAVELLED

Found in the survey, before a line of batch ② was written.

`SliceShell` derived this when the list family crossed, and wrote the arithmetic at its own
site: *the old shell's BottomNav is 82 tall, which is where the 82 came from; the worklist
shell's chrome below the scroll column is the dock (8 + 44 + 8, AiDock.tsx:82-83) plus the
nav seat (52, WorklistShell.tsx:188) = 112.5, and 120 is that plus one step of the 8-scale.*

```
bottom: inShell ? 'calc(120px + …)' : 'calc(82px + …)'
```

**`app/vendor/calendar/screen.tsx:702` was a bare `82`, with no `useInShell` import in the
file at all.** Calendar crossed at §4-2 in this seat's own sitting, and its Add button has
been sitting ON the ask field inside the shell ever since. **Contracts and TDS were bare
`82` too** — crossing them as written would have made it three.

**NOTHING CAUGHT IT.** The render arm measures text edges, container edges and tuples; no
cell measured a control's clearance. A rule that exists, is reasoned about at one site, and
has no assertion behind it gets applied exactly where somebody happened to be looking.

**Fourth instance of the class-walks-away shape on this arc, and the first that is a
HIT-TEST.** R-38.22 ruled the sheets fine because they are FULL-cover with live catchers. A
FAB at 82 is **partially** behind the dock: the top of a 46px button clears, the bottom does
not, and the thumb lands on the ask field. Partial coverage is the shape that stops a batch.

**Cured in this cut at all three sites**, no new number invented — the pair is inherited from
`SliceShell`, which owns the derivation.

### C39, and where it lives

Homed in `b40` beside C24/C25/C26 **on purpose**: that is where the crossing checklist lives
— the registry set, the shell mount, the census — so batch ③ and every crossing after it
inherits this cell for free rather than someone remembering to write it. **A cell filed next
to the thing it guards is the only kind that gets run by accident.**

The bodies it walks are DERIVED from the `app/w` routes' imports, so a crossing joins the
cell in the edit that creates its route. Its subject is narrow and stated at the site: a
fixed control with a bottom OFFSET. A full-cover `inset:0` scrim is R-38.22's ruled sheet
behaviour, and a sheet panel at `bottom:0` spans the full width under its own scrim — neither
is a partial-coverage hit-test, and flagging them would teach the reader to ignore the cell.

**Both-ways, run at this tip:**
- Revert calendar's line to the bare 82 → **RED**, naming the file and the expression.
- Revert TDS's (the new instance) → **RED**, naming it.
- Restore each → **GREEN**.
- The cell refuses rather than passing if it finds no crossed bodies at all.

---

## §2 · THE CROSSING — THREE ROOMS, ONE CUT

| room | body split | in-body chrome retired | census | outbound |
|---|---|---|---|---|
| team | `team-hub/screen.tsx` | `SectionLabel "Team Hub"` | 1 → 1 | **three declared doors** |
| contracts | `contracts/screen.tsx` | **the masthead row, whole** | 1 → 1 | none |
| tds | `tds/screen.tsx` | chevron + `TDS` word | 1 → 1 | none |

`INTERIM_VENDOR_ROOMS` **4 → 1**. One room remains — collab — and when it goes this list is
empty, which is the condition F-38.52's mode bridge names for its own retirement.

Census holds at 1 for all three: body and route were one file in each case, so the mount
moved WITHIN the crossing rather than out of it (calendar's §4-2 precedent). **Derived
through `strip()`, per F-38.24** — a raw grep reads 2 per file, because each doc block
mentions `<Header/>` while explaining why it left.

### Three rooms, three different chrome retirements, and the difference is the point

Portfolio and Couture kept their masthead rows at §4-3 because an ACTION rode on each.
**Contracts' row is a chevron and the word and nothing else, so it goes entirely, hairline
included** — F-38.18's kin, and a row kept for symmetry with its siblings would be chrome
kept for the look of the diff. **TDS keeps its row** because Export CSV rides on it, with a
spacer taking the label's `flex: 1`. Team's `SectionLabel` reads 「Team Hub」 while the shell
prints 「Team」 above it — two names for one room, stacked — so it retires in the shell and
stays on the fallback, where nothing else names the surface.

### `teamTitle` is 「Team」, not 「Team Hub」

The tile has read `Team` since the registry was written and the masthead takes the tile's
byte, so the room is named the same thing in the grid and at the top of what it opens.

### Team's href changes its ROUTE SEGMENT, not only its tree

`/vendor/team-hub` → `/w/team`. The fallback keeps its own spelling; only the tile's
destination is renamed, and **no other file spells either one** — derived by command, not
assumed.

---

## §3 · TEAM IS A ROOM OF DECLARED DOORS, AND IT WAS RULED BEFORE IT LANDED

Every one of its three rows leaves the shell: `/vendor/studio/team`,
`/vendor/studio/tasks`, `/vendor/studio/team-payments` — Studio surfaces with no registry
entry and no crossing chartered this block. A vendor who taps any row gets a second layout,
a second Splash and a second session resolve.

Storefront's Discover row is the class; this is **three-of-three rather than one-of-two**, so
it went to the chair at the survey and was ratified before the crossing landed rather than
explained after it. All three are named entries in `INTERIM_VENDOR_LINKS` **with their source
line**, under C-2's ruling that the set grows by named entry at a crossing and shrinks only
when the TARGET crosses.

**THE HREFS STAY LITERAL AND DO NOT ASK `roomHref`.** The address book answers for ROOMS;
pointing a non-room through it would return the rooms directory on every miss — a silent
wrong answer in place of an honest carried link.

They cross at their own block or they die with `app/vendor/layout.tsx` at Phase 7, and **this
ledger is what will say which**, because a set that must be edited to retire an entry cannot
be retired by forgetting.

Contracts, three tiles over, is the opposite case in the same batch: **zero `/vendor`
literals**, so it adds nothing to any interim set.

---

## §4 · THE GUTTERS

`studioShared.tsx`'s `Row` and `SectionLabel` are shared with the main-side Studio page, so
both became `var(--slice-inset, 24px)` — the fallback is the literal that was already there,
which makes it a MOVE and not a fork (D-2). Same for contracts' row list and TDS's FY pills
and entry rows.

**The fixed sheets' interiors were deliberately left literal.** A sheet positioned against
the viewport is not a child of the scroll column and takes no gutter from it; declaring one
there would be a variable that can never be set.

---

## §5 · FLOORS AT THE CUT (R-38.19)

**Floor by SET: base 22 → cut 23.** One added, zero removed: `tdw_f0774_vacuity_probe`, which
refuses a dirty tree by design. **`FLOOR = NAMED BASE + the delivery-dirt probe.**

**NO BENCH CASUALTIES THIS BATCH, AND THE REASON IS NOT THAT THE CROSSING WAS GENTLER.**
Two benches read these paths — `tdw09_p2_doors` and `tdw09_uivendor` — and **both were
already RED at base**, so their reads are unexercised rather than saved. F-38.43's prediction
did not fire because the instruments in its path were already dark, which is a different
fact from the crossing being clean and is written here so nobody reads it as the latter.

`npx tsc --noEmit` **exit 0** · `b40` **FLOOR GREEN, 39 cells**.

**Both-ways:** revert the three hrefs → `C24` REDs, *the registry carries 4 /vendor hrefs but
declares 1 interim rooms*. Restore → GREEN.

---

## §6 · WHAT THIS SEAT OWNS

- **Nothing here ran against a deploy.** `wl_audit` and `wl_render` both need one; the
  served-bytes half is the founder's run, and so are the captures.
- **F-38.59's cure changes what the founder feels, not only what he sees.** Calendar's Add
  button moves up 38px inside the shell. It has been unreachable-ish since §4-2 and he may
  have worked around it without reporting it — worth a deliberate press on his walk.
- **C39's subject is narrow by choice** and the boundary is stated at its site. A fixed
  control anchored `bottom:0`, or a full-cover scrim, is out of scope; if a future room ships
  a half-width bottom bar, that is a new shape and wants its own reasoning, not a widened
  regex.
- **Two benches in this batch's path are dark** (§5). They are not this sitting's to relight,
  but a crossing that lands under a dark bench has no floor evidence of its own.

## §7 · THE NEXT SITTING

**Batch ③ — collab alone**, and it closes `INTERIM_VENDOR_ROOMS`. It carries the
**pre-granted `wl_render.cjs` byte**: `['room-collab', '/vendor/collab']` is a hardcoded seam
frame commented *a room that has NOT crossed*, and collab crossing makes that comment false
(F-38.29's shape). **Derive S3's shape of that file first if they have edited it meanwhile.**

Collab also has a second route — `app/vendor/collab/[post_id]/responses/page.tsx`, 349 lines,
its own `<Header/>` in the census. Whether the responses surface crosses with its parent or
stays a carried sub-route is a question for the batch ③ survey, not an assumption for this
handover to make.

Open and unchanged: F-38.22 · F-38.23 · F-38.24 · F-38.30 = F-19.14 · F-38.31's Phase 4 half
· F-38.32 · F-38.38 + F-19.18 · F-38.47's design sitting · the two dirt-enumeration homes ·
`rooms.ts:1`'s "SEVENTEEN ROOMS" header, corrected at the Khata edit.
