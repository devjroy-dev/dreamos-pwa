CE-42 · 4a PACKET 3b · R9-J1 THE INTRODUCTIONS SCREEN (dreamos-pwa, base e439c799) · [CE-42 LE-E3]

1 · WHAT SHIPPED. `app/vendor/(shell)/introductions/page.tsx` — the three dormant
    doors' first and only caller. `lib/worklist/introductions.ts` — the chair's
    fifteen bytes of 2026-09-10 in one home, the chip map, the row meta and the
    house date. The hub's TENTH row (`{ key: 'introductions', label:
    'Introductions' }`, last, R-42.8), `INTRODUCTIONS_HREF` as the sixth
    not-a-room constant, the two addresses on the API map, and ONE line in
    `ROOM_HREFS`. `b69` is new in both halves — a structural bench and a proof
    that IMPORTS the copy home rather than copying it. `b40` C31 and `b42` C3 are
    amended BY LABEL. F-42.95: the ratified frame and its two shots are banked at
    `docs/mocks/j1-introductions-mock.html`, which had never been committed.

2 · WHAT IT DOES ON THE GLASS. `/vendor/rooms` → Business Solutions → the tenth
    row now reads `Open` → `/vendor/introductions`. With nothing sent, `No
    introductions yet.` sits ABOVE the form under the `Sent` eyebrow and never
    instead of it. Three fields → `Review the message` stages at the door and the
    preview renders `body_filled`, the arm's own build of the filed template body
    — this side holds no copy of that sentence. `Back` is a text link ABOVE a
    full-width `Send to <name>` clamped to one line (frame correction 1; the
    frame drew them two-up and the name wrapped inside the button at 374). Every
    refusal on the glass is the estate's own byte off `body.error`: the vetoed
    ask, INTRO_ALREADY_SENT, `cap.reason()`. The screen owns none of them and
    does not even check for an empty field — posting a blank returns a
    founder-vetoed ask, and a local `if (!phone)` would replace three of those
    with silence.

3 · WHAT IS NOT PROVEN. THE WALK. No cell here reaches Railway, Supabase or Meta;
    `b69` reads the shipped source and drives the pure helpers, and that a
    handset RECEIVES the page button — and that a row moves Sent → Delivered — is
    the founder's card and nothing else. `b69` at a clean d22bdf2c REFUSES (exit
    3, its subject absent); it cannot be green at the uncured tree, but that is a
    refusal and not a red, said out loud rather than counted as one. The two
    amendments ARE proven both ways: at that same clean tree the amended `b42`
    reds `ten rows → 9` with the ordered join, and the amended `b40` reds
    `INTRODUCTIONS_HREF is not declared`, FLOOR RED 3.

4 · FOUR FINDINGS, RE-LABELLED BY THE CHAIR (c-42.22 — .96/.97/.98 were spent
    in other windows), AND ONE E-NUMBER. F-42.110 — `See my
    work` is `TEMPLATES.introduction.button.text` and the 201 does not carry it,
    so the preview needs a copy; declared as `BUTTON_LABEL`, pinned to one home,
    cure is one field on the door (seat E's). F-42.111 — `not_sent`/`none` have no
    vetoed byte, so those rows are not drawn under a `Sent` eyebrow; after a 503
    the staged row is on the plane and invisible on the glass and the 409 that
    refuses a retry names a row she cannot see. F-42.112 — the frame draws `8
    Sept`, which is the byte the S2 sheet ruled against; the house `Sep` is used
    and that is a FIFTH frame correction. F-42.113 — `tools/advisor_frame_emit.mjs`
    reads `prefixFor` at :48 and hardcodes the prefix at :51, so every frame
    spells the nine ROLE tokens `--atelier-*`, which resolve to nothing in the
    app; FILED, NOT CURED, and `b69` C8 stops it being copied in later. e-1, mine:
    the first cut of C6 asked whether the form was within 400 characters of the
    empty branch and reddened on correct code — a distance is not a nesting; and
    two mutations were VACUOUS on their first spelling, found by running them.

5 · THE HASH. Base e439c799863280692b8388b7e02e60ae9e914b9f, re-derived by
    `git fetch -q origin && git rev-parse origin/main` at the cut, not carried.
    RE-PINNED from d22bdf2c after D3 s1 and NOTHING WAS REBUILT: all five modified
    paths verified blob-identical across the two tips (support/page.tsx d65248e5,
    solutions/copy.ts d791346a, solutions/routes.ts e5c74880, b40 1b6d406b, b42
    b7c313fa) and all nine new paths verified absent at the tip. D3 s1's four are
    disjoint from the fourteen. The benches were RE-RUN at e439c799, not carried,
    and b20_a4 75/75 rides in the floor line below because two of D3 s1's paths
    sit inside b40 C50/C102's walk and this packet must be able to say it did not
    move them.
    b69 71/71 · b69 --mutate 91/91 (ten mutations, each RED, tree restored
    byte-for-byte) · b69 proof 42/42 · b20_a3 203/203 · b20_a4 75/75 ·
    tsc --noEmit exit 0.
    ⚠ NO BUILD NUMBER IS CLAIMED HERE. `npm run build` was RUN at this cut and
    failed, and it failed for a reason that is not this packet's: `app/layout.tsx`
    resolves four families through `next/font` and this seat's container cannot
    reach fonts.googleapis.com, so the compile dies on the font fetch before it
    reaches a single file the packet touches. Four errors, all in layout.tsx, all
    the same class, none elsewhere. THE GATE IS THE FOUNDER'S RUN and it is
    unforged here; `tsc --noEmit` at exit 0 covers every type in the tree
    including this packet's, which is the strongest thing this seat can honestly
    put behind it. DECLARED, INHERITED, NOT CURED (F-42.94): b40 FLOOR RED
    2 (C50, C102) and b42 175/176 (C7) are red at e439c799 before this packet
    touches anything — re-derived at the new tip rather than carried from the old
    one, because two of D3 s1's paths are inside C50/C102's walk — and are red
    after it by the same two and one cells. b42's
    count does not move — two cells were renamed, none added.
