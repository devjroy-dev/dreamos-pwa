# TDW_09 · THE BRIDE ATELIER — BUILD-ALL HANDOVER
**Repo:** `dreamos-pwa` · **base:** `2e09207` (re-derived fetch-first at delivery; origin/main unmoved)
**Seat:** Opus 5-LE, twenty-fifth chair's atelier arc · 2026-08-07
**Gate 1:** APPROVED — founder verbatim 「 approve 」 against `tdw09_atelier_language_gate1.html`
**APPROVED-COPY-CARRIES-ITS-HASH:** approval given against the Gate 1 package as delivered, authored at tip `2e09207`; the approved file ships in this ZIP at `docs/mocks/tdw09_atelier_language.html`, content-identical to the approved artifact.

---

## 1 · THE FLOOR, BARE

| gate | reading |
|---|---|
| `tsc --noEmit` | **exit 0, zero lines** |
| `tdw09_p2c` (sealed floor) | **52 / 52** — byte-stable, unchanged from charter |
| `tdw09_frost_parity` (new) | **57 / 57** at the cured tree |
| same bench at pre-arc `2e09207` | **41 / 57 — sixteen RED** |
| mutation ledger | **13 / 13 bite** |
| files changed | 15 modified, 2 new · +588 / −511 |

---

## 2 · WHAT SHIPPED

**The language, across every surface class.** The masthead recomposed to four runs from nine (Fork 3 arm A — the numeral dominant); the rail retyped in place (Fork 2 arm A); Moments at full-bleed 4:5 plates (Fork 1 arm A); list rooms, sheets and empty states on the rungs and the one gutter.

**The T-1 bride pass.** 155 body sites to the 16px floor; 192 engraved sites normalised to the two engraved rungs; 155 engraved runs to the one tracking. Display band collapsed by face.

**Type sizes: 33 declared → 7 literal rungs + the token-referenced numeral.** Final literal set: `9 · 11 · 16 · 19 · 22 · 46 · 52`, plus `18` and `20` at the nine named glyph sites (§5).

**F-09.159** — `MODES.E1A.soft` `0.65α → 0.75α`, byte-equal to `V2_WINE_NIGHT.inkSoft`. Cured at the token, so `CanvasShell.tsx` needs **zero bytes** — the seven standalone journey routes inherit it. Equality pinned by cell 6.4.

**F-09.160** — `layout.tsx` context default pinned `'E1A'` / `MODES['E1A']` / `'E1'`, labelled in-comment as the ruling's fifth seat.

**F-09.158** — the two extant 「 tab 」 lines cured. **See §4 — the relay named three; only two exist.**

**Tokens minted** in `lib/frost/tokens.ts`: `FT` (eight rungs), `FS` (spacing, gutter, hair, track), `FI` (imagery). Token-level, beside `V2_WINE_NIGHT` — no structural JSX rearrangement inside rooms, so TDW_13's extraction inherits them intact.

---

## 3 · MOCK-FIDELITY — MATCH OR REPORT, PER SURFACE

| class | verdict |
|---|---|
| 1 · masthead + rail | **matches** |
| 2 · dense room (Moments) | **matches** |
| 3 · list room (Vendors) | **matches** on type, gutter, money register. **DELTA:** the mock draws bookings and enquiries as one visual rhythm with the WhatsApp door only on enquiries — shipped as drawn, but the booked rows' balance column keeps its existing right-alignment rather than the mock's fixed column width, because a fixed width would clip `Rs 8,20,000` at the floor. Named, not silent. |
| 4 · sheet | **matches** |
| 5 · empty state | **matches** |
| type rungs | **DELTA — reported at the token.** The mock's token table said seven rungs and folded all Italianno into `type/room`; the mock **draws** the empty-state head at 52px Italianno and its note defends that. The drawing is the veto carrier, so `head: 52` ships as an eighth rung. Every "33 → 7" sentence is corrected to **33 → 8** (7 literal + the numeral token). |

---

## 4 · REPORT-NEVER-ADAPT · §0.2 · A RE-ISSUED RULING THAT COULD NOT EXECUTE AS WORDED

Relay #4 §2 re-issued **F-09.158 — "the three 「 tab 」 lines FOUNDER-VETOED 「 all ok 」, build them."**

**There are two, not three.** The read-first filed three; the Gate 1 package **corrected that on its face** and printed the correction inside the copy table the founder approved:

- `sanctuary:412` — *"…Add vendors in the Vendors tab."* → **cured**
- `journey/people:42` — *"…Invite someone from the Circle tab."* → **cured**
- `sanctuary:1155` — reads *"No one yet. Invite someone from Circle."* — **no "tab" in it.** My read-first error, disclosed at Gate 1, shown in the approved table as UNCHANGED.

The founder's 「 all ok 」 was given against the corrected table. Two lines built; the third **not invented**. Cell 5.15 asserts zero 「 tab 」 references survive anywhere in the bride tree — it passes, which is the real proof the class is dead. If the chair intends a third site outside the census's fourteen empty states, name it and it rides a rider.

**Second reported delta — the T-1 count.** The ruling says *161 body sites*. The build moved **155**. The read-first counted line-scoped; the build classifies by the **enclosing style object**, and seven sites that look like body on their own line sit inside multi-line engraved objects (`onboarding:150`, `sanctuary:2530`, `sanctuary:3250`, `CanvasShell:50`, `EnquirySheet:105/:339/:426`), one going the other way — net 155. The more accurate method wins and the number is reported, not quietly reconciled.

**Third — the poetry line.** Relay #5 rules 「 the poetry pool AS DRAWN 」. The approved masthead **does not draw it**, which is arm (c) of the read-first's veto slot: retired from the masthead, pool untouched. `FROST_COPY.idlePool` ships byte-identical (cell 5.16) and is now reader-less. **This is a reading of a drawing, not a quoted ruling** — if the intent was arm (a) or (b), it is a two-line reversal and I would rather be told now.

---

## 5 · DECLARED GAPS AND EXEMPTIONS, ALL NAMED

1. **Nine glyph sites exempt from the rungs** (`sanctuary:359 · 466 · 504 · 768 · 813 · 838 · 1322 · 2895 · 3471`, at 18 and 20px). These are icon sizes — the sheet closers ✕ and the viewer dismiss. Icon sizing is not a type rung; forcing 20px ✕ to 11px shrinks a hit target. Exempt and named rather than silently mapped.
2. **The money fit is a founder-device witness.** Whether `Rs 8,20,000` at the 16px floor clears the right column at 390px is **not measured at this seat** — no browser here. F-09.58's ledger strip is the precedent. If it clips: `minWidth:0` on the row body and a measured fit. **Never a smaller figure, never compaction** (R-U24).
3. **F-09.164 — `FROST_COPY.idlePool` is reader-less** after arm (c), with `getDailyPoetry()` and the `poetry` state wired to nothing. Filed rather than deleted: removing a founder-written pool is not a redesign's call. Register .164 spent.
4. **`tools/` is NOT in the ZIP.** The three transformers (`t1pass.mjs`, `t1display.mjs`, `mutate.sh`) are one-shot build scaffolding with absolute container paths. The **bench ships** and runs from any working directory (Q-SP-5); the scaffolding does not. If the chair wants the transformers committed for re-derivability, say so and they ride a docs rider.
5. **Engraved sites sit below the 16px floor by ruling**, not by omission — the founder's standing 「 keep engraved 」. They are still normalised onto the two engraved rungs, exactly as the approved page's retires paragraph declares.

---

## 6 · THE BENCH, AND WHAT THE MUTATIONS CAUGHT

`scripts/tdw09_frost_parity.proof.mjs` — 57 cells, capability-not-shape throughout. No cell asserts a class name, a colour literal, a pixel value, or an import string. §1 routes · §2 roster · §3 controls · §4 the thirteen verbs · §5 copy · §6 the language · §M absent-subject conviction.

**Two bench defects the mutation ledger caught before delivery, disclosed by name:**

- **Cell 4.4 was vacuous.** The Discover deck saves to Muse **two ways** — the double-tap at `:1603` and the button at `:2001`. The cell matched *either*, so breaking one went green. It counts both now. The mutation found it; I did not.
- **Cell 3.1 was mapped to the wrong mutation.** Un-wiring the exit leaves the `<button>` tag standing, so the count cell cannot see it — that is cell 3.3's question. Split into M-4 (wiring → 3.3) and M-4b (deletion → 3.1). Two questions, two cells, two mutations.

**Both-ways, stated plainly:** the sixteen §5/§6 cells prove RED at the pre-arc tree. The §1–§4 parity cells are green at *both* trees **by design** — parity means nothing moved — so their uncured tree is a mutated one. All thirteen mutations bite.

---

## 7 · GATE 2 — THE FOUNDER'S WHOLE-APP WALK CARD

Room by room against the census. Paste back anything that reads wrong. **You perform; I read the evidence.**

**Open the app at `/frost`.**

| # | step | what should be true |
|---|---|---|
| 1 | The first screen | One big copper number. Your name below it. One line of prose. One teal line. Nothing else competing. |
| 2 | Scroll nothing — just read the rail | Eleven rooms, same order as always: Discover, Circle, Muse, My People, Pages, Moments, The Journey, Expenses, Vendors, Meridian, Settings. Dream Ai below them, centred. |
| 3 | The Circle row | Still has its teal candle dot. |
| 4 | Discover → | Opens. Swipe the deck. Swipe the end card. Tap the blind toggle. Tap the heart — heart animation, toast, buzz. |
| 5 | Discover → tap the name bar | Vendor panel opens. **Drag it down** — it dismisses. |
| 6 | Discover → filter | Sheet opens, chips select, Apply and Clear both work. |
| 7 | Circle | Feed reads. Invite flow: tap +, pick contacts or type a name, role chips, send. |
| 8 | Muse | Board reads. Tap a save. Enquire / Share / Remove all present. Add sheet: camera roll AND paste-a-URL. The ✦ Surprise Me link still there. |
| 9 | My People | Rows read. WhatsApp and call buttons on each. Remove works. |
| 10 | Pages | Mood picker, write, save. |
| 11 | **Moments** | **The photographs are now full width.** Date and day label sit on the image. Tap a photo → full screen. Tap a caption → edit, save, cancel. Add from camera roll works. |
| 12 | The Journey | Timeline reads, grouped by date. |
| 13 | Expenses | Three tabs. Add an expense. Tap a receipt → full screen. Delete confirm. Pay against a booking. |
| 14 | Vendors | Booked and Enquired groups. **Check every money figure is whole — no clipping, no "…".** Tap a booking → Pay / Edit / Delete / Close. WhatsApp door on enquiries. |
| 15 | Meridian | Opens (premium). Concierge button. Chat sends. |
| 16 | Settings | Rows read. **The WhatsApp row still opens WhatsApp.** |
| 17 | Dream Ai | Opens. Prompt chips on an empty thread. Type and send. Clear works. |
| 18 | **Every room** | The ‹ chevron closes it, and swipe-down closes it. |
| 19 | Read anything | **Nothing is too small to read.** That is the whole point of the type pass. |
| 20 | The empty screens | If any room is empty, it should invite you to act, not just say nothing's here. |

**Only your device can witness:** the contact picker (step 7), haptics (step 4), the money fit (step 14), and whether the type actually reads at arm's length (step 19).

---

## 8 · REGISTER

`.164` spent (idlePool reader-less). `.165–.170` free. `.156–.163` spent as CE-209 records them.

---

*Nothing in this delivery was carried from memory. Every number is re-derivable at `2e09207` + this ZIP.*
