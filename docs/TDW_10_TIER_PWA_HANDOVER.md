# TDW_10 · THE TIER & MONEY SITTING — ZIP B (dreamos-pwa) · EXECUTOR HANDOVER

**Built at:** dreamos-pwa `2452eb5` (P2C ZIP 1; re-derived at origin, fetch-first, checkout-first at the moment of building).
**Paired with:** dream-os `2077214` — ZIP A, **already pushed and live**, database migrated and constrained.
**Seat:** Opus LE, successor seat. **Radius:** dreamos-pwa only.
**Rulings built under:** relay #2 §1–§5 · relay #3 · relay #5 · the RESUME CHARTER's ruled state.

> ## ⚠ THIS ZIP CARRIES NO APPLY BLOCKS, BY RULING
> ZIP B is **SEQUENCED BEHIND the Phase C sweep successor's delivery**. The sweep had **not** landed at origin when this was cut (`2452eb5` was still the tip). Under the conditional-withheld rule, a runnable apply block whose condition has not arrived is the E-1 mishap's exact shape — anything runnable left in a transcript will be run. **The file travels; its blocks issue from the chair when the sweep is at origin.**
>
> ### THE APPLY BLOCK WILL NEED A DISCLOSED `rm` EXTENSION
> §7's fixed chain **cannot delete**: `cp -r deploy/*` only adds and overwrites. M1 retires three pages, and their deletion therefore cannot travel inside the ZIP. Whoever issues the blocks must prepend, as a **CE-disclosed extension named in the block** (the apply-verbatim law permits exactly this and requires it be named):
>
> ```
> rm -rf app/admin/money app/admin/revenue app/admin/subscriptions
> ```
>
> **A ZIP-B apply block without that line leaves the three retired pages alive on disk**, and `tdw10_p1_shell` will redden at its disk-denominator cell — correctly, because the retirement would be half-done. That red is the designed alarm, not a defect.

---

## 1 · WHAT SHIPPED

| File | State | What |
|---|---|---|
| `app/admin/_components/adminNav.ts` | MOD | `RETIRED` disposition added to the union + its doctrine; three **tombstone rows** for F-10.76 with the control inventory recorded in-file. |
| `app/admin/_components/Bridge.tsx` | MOD | **C1 + C2**, founder-approved 「 yes 」. `Trials` → `Basic`; `Active on the trial tier` → `On the basic tier`. |
| `app/admin/makers/page.tsx` | MOD | The tier dropdown → the four ruled words. |
| `app/admin/config/page.tsx` | MOD | The cap-key editor offers the `_basic` keys 0115 actually seeded. |
| `app/admin/control-room/page.tsx` | MOD | **F-10.83's rider** — Rs 499 → canon Rs 999; the expired trial narration replaced; four unwired controls removed and accounted. |
| `lib/vendor/types/vendor.ts` | MOD | **F-10.81's union** + M2's two typed fields. |
| `hooks/vendor/useSettings.ts` | MOD | M2's data path, declared **and mapped**. |
| `app/vendor/settings/page.tsx` | MOD | **M2's subscription surface** — founder-vetoed strings verbatim; F-10.77's flip-reason cell; the payment path on the stored Subscription Link; F-10.69's UPI line; the `id="tier"` anchor. |
| `scripts/tdw10_tier.proof.mjs` | NEW | 98 cells. |
| `scripts/tdw10_p2_retint.proof.mjs` | MOD | **The 3 → 4 labelled amendment.** |
| `scripts/tdw10_p1_shell.proof.mjs` | MOD | Registry-invariant labelled amendment, 53 → 57. |
| *(deletions)* | RM | `app/admin/money/` · `app/admin/revenue/` · `app/admin/subscriptions/` — **not in the ZIP; see the `rm` above.** |

---

## 2 · THE PROOF

- **`tdw10_tier` 98/98 GREEN** cured · **33/98 at pristine `2452eb5`** — **65 cure cells RED, per-cell**, on an independently cloned pristine tree.
- **`tsc --noEmit` exit 0** on a cleared `.next` cache (§6's requirement after any file deletion).
- **FLOOR at CE-206's counts:** `tdw10_p2_bridge` 44/44 · `tdw07_p2_profile` 48/48 · `tdw07_p3_portfolio` 119/119 · `tdw_f0774_stripper` 33/35 (attributed, F-10.49) · `tdw10_p3_deck` 191/193 (attributed, F-10.62).
- **`tdw10_p2_retint` 76/76** — the fence lifts.
- **`tdw10_p1_shell` 57/57** — was 53/53, amended.

---

## 3 · THE RETINT AMENDMENT — CE-205'S CARRIER, THIRD ATTEMPT, LANDED

CE-204 ratified the 3 → 4 figure-site amendment and named the walk-hotfix delivery its carrier. That delivery landed at `8c1351a` **without performing it**. CE-205 re-carriered it here with 「 a carrier that does not know it is carrying is no carrier 」.

**Performed, labelled, and re-derived at this seat rather than carried:** four sites at `2452eb5` — masthead 40px, queue 26px tabular, and the two 34px lines (featured fee + the A4 rider's subscription line). The property the cell exists for never wavered: **every** site carries `lining-nums` and **exactly one** takes `tabular-nums` held at four exactly as at three, which is why only the literal had drifted and the guarded behaviour was never in doubt.

The count literal is **deliberately kept** rather than relaxed to `> 0`, and the amendment says so in-file: a literal is the most brittle possible expression of "all of them", and that brittleness is the feature — the next figure site added to this masthead also stops and asks.

---

## 4 · THE SHELL BENCH CAUGHT MY RETIREMENT. IT WAS RIGHT TO.

`tdw10_p1_shell` went 53/53 → **50/53** the moment the three pages left the disk. All three reds were real and all three were mine:

- *every /admin row in the table is a route that exists on disk* — **falsified by design.** A tombstone is precisely a row whose route is deliberately absent; the registry exists to answer "where did it go", and it cannot answer that about a page it has forgotten.
- *the disk carries exactly 37 non-login admin routes* — now 34.
- *18 LIVE + 1 RETIRES + 18 PHANTOM = 37* — three PHANTOM rows became RETIRED.

**Widening the first cell to `true` would have been surrender.** The amended invariant is **stricter than the one it replaced**: every row is either a live route on disk **or** carries the RETIRED disposition, and `retiredPaths` is derived from the registry text rather than hard-coded, so a genuine dangling reference still reddens and the next retirement needs no edit here. Three cells added beside it — the tombstones are exactly F-10.76's three; no tombstoned route survives on disk; and `table − disk === retired`, so the gap between the two numbers **is** the retirement, asserted rather than papered over.

---

## 5 · WHAT THIS DELIVERY DELIBERATELY DID NOT DO

- **`app/admin/layout.tsx` byte-untouched by name** — the standing wall, asserted by a cell.
- **The Bridge revenue block byte-untouched** — this sitting moved a label, never a number.
- **The other seven `/api/v3` phantoms keep their rows and their pages.** Three routes died because the founder ruled on them **by name**; F-10.84 (the whole v3 namespace is unmounted — 22 call sites, 10 pages) stays FILED and homed to F-07.95's sitting. Six scope-creep cells assert the survivors, because a sitting that quietly widened its own ruling would be legislating.
- **M2's strings are NO LONGER held** — the founder vetoed the short batch 2026-08-07 (「 yes 」), then ruled 「 drop the date 」. Every rendered byte is asserted VERBATIM by a cell, so a later edit that rewords founder-approved copy reddens rather than passing review on someone's taste.
- **The `_trial` and `_bench` config keys are not deleted** — only no longer offered in the editor.

**Residue named, no re-cut:** `lib/vendor/format.ts:14` carries a historical comment mentioning `admin/revenue`, now a retired path. Prose, not a pointer; it rides the next natural delivery.

---

## 6 · THE DATE THAT DOES NOT EXIST (§0.2, reported not adapted)

The first-draft flip-reason line read *Moved to Basic on {date}*. **No flip timestamp exists anywhere in this estate:** 0114 gave `billing_status` no companion stamp, `tierFlip.js` writes none, and `vendors.updated_at` moves on any profile save — rendering it would have printed the day she edited her bio as the day her plan changed. The true date lives only in `billing_events.created_at`, a different table `/me` does not read.

Reported before a byte with two arms — drop the date, or a second dream-os ZIP teaching `/me` to look it up. **Founder ruled: 「 drop the date 」.** A bench cell now asserts the absence, so no future edit can quietly reintroduce an invented timestamp.

## 7 · NEXT

M2 is COMPLETE. The remaining vendor-money work is the founder issuing per-vendor Subscription Links from the Razorpay dashboard and writing them to `vendors.razorpay_subscription_link` — a zero-placeholder founder-run UPDATE, authored when he has a link to write. Until then every vendor reads *Dev will send you a payment link.*, which is true.

**Register: F-10.78–.85 spent · .86 (the inverted PWA cap ladder) · .87 (the eight inert WA knobs) · .88 (the orphan `bench` keys) remain this seat's, filed and unbuilt.**
