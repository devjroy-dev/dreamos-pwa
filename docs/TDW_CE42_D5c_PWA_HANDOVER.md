# CE-42 · SEAT D2 · D5c (pwa) — the founder's two lines, and a refusal that stays put

**Cut at** `435101d1a57610fdfc246635a7109cb839ddc7c7`. `b20_a3` **203/203** (base 190), `tsc --noEmit` clean.

**`/e/<token>` — the founder's bytes.** *"Your enquiry details are with us"* over *"Sign in to **thedreamwedding.in**"*, the domain a real link to `https://thedreamwedding.in`. The page shows a summary and by roadmap §7 must never show her phone, name, exact date or figure, so *details* is the true word: the rest exists, and it is with us. The link is the vendor-join door this file's header has owed since D2; the front door offers Join and Sign in side by side (F-41.1's rider), so *"sign in"* does not send a stranger somewhere she has no key for. The `found:false` and no-id bytes are untouched. The anchor's href and text are escaped like every other interpolation.

**F-42.74 — the intake's refusal sits still.** The founder typed a vendor's number here, pressed *File* three times, and read nothing: three red 400s in a console he had open by chance, and the reason in a Railway log. This is the longest sheet on the queue — eleven budget fields below the fold — so its three-second toast fires where he is not looking, every time. The reason now renders above the button and stays until he **changes something**; not a timer, because a refusal that clears itself while the form still holds the thing it refused is a refusal that lies. The toast stays as the backstop.

**⚠ THIS HALF STANDS ALONE, WITH ONE NAMED DEPENDENCY.** Both bytes are pwa-only and apply in either order — the `/e/` copy needs nothing from dream-os, and F-42.74's frame renders whatever sentence the door sends. But the sentence F-42.74 was built to show is **F-42.73's**, and until the dream-os half of D5c is pushed and deployed the vendor-number case still answers with `COUPLE_FAILED` — *"Could not create the account for this number. Try again."* — which the frame will hold still and in front of the founder, which is an improvement on a toast he could not see but is still the wrong words. **Applied alone this is correct and incomplete; the wording arrives when the door does.**

**One wrapper, not seven call sites:** every intake field clears the refusal through `edit`, so the twelfth field somebody adds cannot forget. A cell counts fields against wrapped fields and reds if they diverge. **The sentence is the door's** — F-42.73's names a handle this page cannot know and R-42.7's names the field; the page rewrites neither.

**Caught at the cut, worth naming:** the first patch wrapped the **outsider sheet's** WhatsApp field instead of the intake's — the two are byte-identical call sites in different components — and `tsc` caught it as an out-of-scope `edit`. A cell now asserts the wrapper never leaks outside `TypedIntake`. **Two D5a cells amended by label:** both pinned the hit's heading and second line, which the founder re-ruled tonight; the labels and the never-promise-details meaning are what they were minted for and both still bind.
