# CE-41 · SEAT D · D2 (dreamos-pwa) — the room, the words, the enquiry address · HANDOVER

**Cut at** dreamos-pwa `1d57dd9478265c9efe0d7988283ac8d14e4b0e60` (seat E's E2 (v)), **re-pinned to `d3a1df1cdfb080ac8208ed0825345766a143e6c1`** — the brand family.

**Collision derivation (R-40.82).** `git diff --stat 1d57dd9..d3a1df1` is **14 files, 0 insertions, 0 deletions** — binary assets only, every path under `public/brand/`. Nothing outside that directory moved, and none of this packet's twelve files were touched. Clean fast-forward, no carry.

## 1 · F-41.98 / R-41.107 — the room is the mount's assertion

`streamChat` (`lib/vendor/api/vendor.ts:396`) builds the body in **one place**, and `room` rides exactly as `ai_primer` already does: **conditional, one home, absent when unset** — which is G2 §1's *"absent, empty, misspelled → no assertion, fail-closed and silent."* Nothing client-side validates the value; a second opinion beside the door's is how two things end up deciding one fact.

It rides an **options bag, not an eighth bare positional** — `onBeat` is already optional and two bare optionals in a row is how a caller passes the wrong one. `useChat` is the only caller (derived), so the shape could be the right one rather than the compatible one.

The chain is **WorklistShell → AiDock → AskSheet → useChat → streamChat**, and the assertion is **the mount**: `/vendor/advisor` passes `room="advisor"` on its own shell instance; every other page's instance passes nothing. That is how *"the shared sheet sends nothing on any page, `/vendor/advisor` included"* and *"the Advisor page sends the field"* are both true. **No page byte moved** — the founder ratified that frame at 06:12.

**Three live meanings of one word, now separated in comment:** `AskSheet`'s `mode` is a **theme** (`'dark' | 'light'`), the engine door's `mode` is **accepted and ignored** (G2's M31 reds anything reading it), and `Header.tsx:338`'s `room="Discover"` is **neither**. They sit one letter of carelessness apart.

## 2 · F-41.124 — an unknown code never borrows another's sentence

The map was always keyed on the exact code; what was missing was a fallback, so a `131008` fell through and the row showed the `131049` sentence sitting above it. **A borrowed sentence is worse than a blank one** — it told the founder Meta's *marketing limit* blocked a send Meta had actually refused for a *missing button parameter*: a different problem with a different fix. The generic line is deliberately dull and names no mechanism, because naming the wrong mechanism is the defect.

## 3 · F-41.84 — S2-8 finally ships

`Closing tells her nothing.` The clause chain I struck under R-41.98, the chair ratified, and I then failed to ship in D0. Closed.

## 4 · R-41.119 — the prefix branch, and what `/e/[id]` honestly is

`/r/[code]` branches on an **explicit `ENQUIRY_PREFIX` constant**, never on shape — a guess at "what a review code looks like" would route on a coincidence, and the day one matched, a vendor would land on someone's enquiry. Every other code falls through **untouched**. A bare prefix with no id does not redirect. The bend to R-40.15 is recorded with its retirement: at the next Meta edit window the base moves to `/e/` and this branch goes with it.

**`/e/[id]` does not show the enquiry, and that is declared, not hidden.** There is **no public read for an assistance item anywhere in dream-os** — derived at `417c9f8`. Building that door is a dream-os packet this seat was not chartered for, and a client fetch against a route that does not exist would have shipped a page that 404s on its own data while looking finished. It follows this repo's own precedent at `/r/[code]`: *a sentence beats a 404 and nothing more is built.* It answers 200, carries **no phone**, and fetches nothing.

**Owed:** `GET /api/v2/public/enquiry/:id` — summary only (category · city · month · budget band), **never the couple's number**, because roadmap §7's refusal is that her number never leaves with an outsider and a public page is the last place it could be allowed to.

## 5 · What the walk will show, and what it will not

The button now resolves: `/r/enq-<id>` → 302 → `/e/<id>` → *"Your enquiry is with us — reply on the WhatsApp message and we will send you the full details."* **That is the honest end of the chain until the public door lands**, and it is a large improvement on the review family's fallback the founder saw this morning.

## 6 · Two things I got wrong, both caught before the cut

**The absence cell was too broad** — it matched any `room="` and convicted `Header.tsx`'s unrelated `room="Discover"`. Narrowed to the chat chain, which is the assertion R-41.107 actually makes.

**The first both-ways run compared cured against cured** and reported GREEN/GREEN. A both-ways proof showing the same result twice is not a proof, it is a copy error. Re-run against a fresh clone: **RED 107/119, twelve cells convict.**

## 7 · Carried

F-41.100 (the fan-out cap displayed and unenforced) and F-41.101 (v2 has no Switchboard label; the retired v1 holds the human name) — **both unruled, neither touched here.** R-41.122's D3 work: `0156`, the two-limb refusal, the paste-box frame. `found_vendor` `pending` → `approved`. `b61`'s re-cut cell awaiting a number.

**Range F-41.102–F-41.107 unspent.**
