# TDW_09 · PACKAGE 2 · PHASE C — ZIP 1 · THE FLOOR MADE HONEST · HANDOVER

**Base:** `dreamos-pwa @ 8c1351a`. **Under:** the twenty-fifth chair · P2C relay #1 (§2's shapes, F-09.93/.94/.95 allocated) · P2C relay #2 (§1 bank granted, §2 F-09.96/.97 ratified, §3 this ZIP's contents ruled).
**Scope:** two bench files. **Zero product bytes.** No `app/`, no `components/`, no `lib/` — this delivery cannot change a rendered pixel, by construction.
**Floor at delivery (`8c1351a`, run bare per F-09.92):** tsc **0** · **tdw07_p3_portfolio 119/119** · **tdw07_p2_profile 48/48** · doors 86/86 · p2b 29/29 · p2r1 13/13 · arbiter 16/16 · canon 24/24 · p4b_body 133/133 · home 67/67 · hotfix 38/38 · f0784_panel 34/34 · **retint 75/76 — ATTRIBUTED ELSEWHERE** (the billing session's A4 rider made a 3-figure cell read 4; its labelled amendment rides CE-204; not fenced here, not touched here).

> **THE SENTENCE THIS DELIVERY EXISTS TO PUT IN THE RECORD, and it belongs in Package 2's closing ledger:**
> **A sitting's floor list is the benches its DIFF can redden — never the benches its charter names.**
> All four findings below were invisible for exactly one reason: nobody ran the bench their change could break.

---

## 1 · WHY THIS ZIP IS TWO FILES AND NOT SEVEN LIMBS

Phase C's charter carries five build limbs plus a closing ledger. This session banks at a **reported seam** instead: the ruled cures are landed and proven; the adoption sweep is untouched. The ground, stated and chair-accepted: Package 2's floor was **not** what its charter claimed. Authoring a closing ledger over a false "all floors green", or a thin sweep proven against a floor that was already lying, reproduces the exact failure F-09.93 and F-09.94 exist to record. The floor is the precondition for everything behind it, so the floor ships alone and first.

**A restatement, because origin moved twice during this sitting.** Read-first was authored at `c539ba8`; the chair's relay #1 landed at `7e39385` (the billing A4 rider); this ZIP is cut at **`8c1351a`** (the walk hotfix micro). Every count in this document is re-derived at `8c1351a`, not carried. The hotfix touched `app/vendor/storefront/page.tsx` (+76) — a file this delivery's `§3.1` cell pins — so the pin was re-derived rather than assumed: still **1** under the stripper, cell still GREEN.

---

## 2 · WHAT SHIPPED

### `scripts/tdw07_p3_portfolio.proof.mjs` — CRASHED → **GREEN 119/119**

| part | change | warrant |
|---|---|---|
| (i) | **The absent-subject shim.** `raw()` records a miss and returns a named sentinel instead of throwing; new `§10` convicts each miss **by name**; the verdict is fail-closed while `MISSING` is non-empty. `§10.1` DRIVES the shim against a probe path — the refusal path is walked, not merely held (F-07.99's call-site clause, applied to a reader). | F-09.93(i) |
| (ii) | **`§3.1b` re-aimed, LABELLED.** The mode leaf dissolved with the mode (P2A fork 8.4); its route buckets went byte-equivalent into `roomClassForPath` inside `app/vendor/layout.tsx` (P2A row 4). F-07.30's one-classifier property **survives at a new home**. STRENGTHENED: the retired leaf's *absence* is now asserted too, so a resurrection reddens instead of silently restoring the duplication F-07.30 ended. Symbol test is `\b`-anchored. | F-09.93(ii) |
| (iii) | **`§3.1` amended, LABELLED.** The Portfolio row moved More→Storefront under R-X27(a) at `8715a69`. Fork 3(b) is INTACT — no edge was lost, one edge changed which screen carries it. `more` pinned at 0, `storefront` at 1, `submit` pinned at 0 with F-09.97 named in-cell. | F-09.93(iii) |

**The declared residual, stated in-file rather than hidden:** the sentinel fails every POSITIVE cell honestly, but a NEGATIVE cell over a sentinel would pass vacuously. That is why the refusal is convicted separately and fail-closed. The two cells that read the deleted leaf are re-aimed in this same delivery, so the window has no live occupant — it is guarded, not merely empty.

### `scripts/tdw07_p2_profile.proof.mjs` — 43/48 → **GREEN 48/48**

Five cells re-aimed by LABELLED amendment following the meter to its P2B homes (`lib/vendor/profileMeter.ts`, `components/vendor/ProfileMeter.tsx`) — the exact `p4b §6.8` shape P2B itself used, asserting the SAME properties at the new address.

**The rule applied throughout, and the reason it matters:** where a predicate was a NEGATIVE, it is now asserted over **BOTH** files. A negative re-aimed to one file is a cell that stopped watching the other one. Three cells (`§7.2`, `§7.4`, `§7.5`) were widened this way. That is a strengthening, and it is the whole reason to re-aim by hand rather than sweep.

---

## 3 · FINDINGS — durable entries (the chair bands them; this document does not touch FINDINGS_LOG)

**F-09.93 — P2A'S FALSE CALLER-ZERO AND THE BENCH IT KILLED.**
`8715a69` deleted `lib/vendor/vendorModeForPath.ts` under a handover row claiming **caller-zero**. The claim was false: `scripts/tdw07_p3_portfolio.proof.mjs:132` read that file. The caller-zero derivation swept `lib/` and `app/` and **never `scripts/`**. A sealed 07-era bench regressed from a reporting instrument to a hard `ENOENT` throw — every cell after the throw unrun and unrunnable, which is strictly worse than a red (F-09.30's refuse-never-crash class). The same sitting's own disclosure #5 cured this identical wound in `p4b_body`; the wound in `p3_portfolio` went unseen because that bench was on no floor list. **Cured, three parts, above.**

**F-09.94 — P2B'S METER MOVE REDDENED A SEALED BENCH AND ITS FLOOR LIST OMITTED IT.**
`f732110` moved the profile meter's model and arc. The move was honest — "moved, never rewritten" holds, every property survives. What P2B did not do was run this bench. Four cells (`§0.1 · §7.2 · §7.4 · §7.5`) went red on a delivery that changed nothing they assert. **Cured by labelled amendment, both-ways proven.**

**F-09.96 — ONE GOLD ASSERTED A LITERAL THE CANON HAD RETIRED.** *(elder to Package 2)*
`§4.3` has been red since **`8066072`** (TDW_09 S1, F-09.28) — the sweep that put nineteen theme-blind literals onto six semantic roles on the ground that no single hex clears AA on both Espresso and Editorial Paper, so a literal *cannot* be theme-correct, only a role can. `#C9A84C` was one of them. The cell was asserting the presence of a byte the canon had lawfully retired — P1's colour-values-are-columns law read backwards. Red for the whole of S1→P2C with nobody told. **Cured:** the property re-aimed to `var(--role-metal)` at `components/vendor/ProfileMeter.tsx:18`, pinned at exactly one sighting, with the literal's absence asserted across all three files.

**F-09.97 — THE SUBMIT EDGE DIED AT F-10.53 AND NO BENCH SAID SO.** *(elder to Package 2)*
`app/vendor/discover/submit/page.tsx` has held **zero** `/vendor/portfolio` references since **`8a1fee1`** (TDW_10 P3 rider 3), where F-10.53 retired the samples step that carried the link. `§3.1` has been red since that sitting. **Pinned at its derived 0 with the finding named in-cell**, so restoring that edge — if the estate ever wants it — is a ruling, not a rediscovery.

**F-09.95 — THE STRIPPER TRAP'S UNCOVERED SPECIES: `/*` INSIDE A STRING, BITING THE INSTRUMENT.** *(filed; cure HELD — dream-os byte, pwa-only phase)*
See §5 below: the arithmetic the successor needs.

---

## 4 · THE MUTATION LEDGER (production source, `cmp`-restored byte-identical)

Cured bench copies were **banked before any revert**; no `git checkout` restored my own work (RIDER4 §0.2's confessed habit, inherited).

| W | mutation, at the NEW home | cell | result |
|---|---|---|---|
| W-A | `app/vendor/layout.tsx` **DELETED** | §10.2 | 117/119 — **named refusal, no throw** |
| W-B | mode leaf `vendorModeForPath.ts` resurrected | §3.1b | 118/119 |
| W-C | `roomClassForPath` renamed | §3.1b | 118/119 |
| W-D | Portfolio edge restored to More | §3.1 | 118/119 |
| W-5 | a second `var(--role-metal)` on the arc | §4.3 | 47/48 |
| W-8 | `pending: o.pending` dropped from the model | §7.2 | 47/48 |
| W-9 | rate term re-demands a max bound | §7.4 | 47/48 |
| W-10 | canary line broken at `profileMeter.ts` | §0.1 | 47/48 |
| W-11 | `photos: 0.270` weight nudged | §7.5 | 47/48 |

**W-A is the cure's whole point, proven in the direction it was written for:** the bench that used to die on a missing file now names it and keeps counting.

---

## 5 · F-09.95 — THE RECONCILIATION ARITHMETIC (the successor's census amendment inherits this)

The kickoff's pre-drafted diagnosis — *"its per-file counting vs per-site"* — is **RETIRED, superseded** (chair relay #1 §2). Both sides count per-SITE. The true reconciliation, derived:

```
  pressed roster (grep ∧ file lacks pressedStyle|onPointerDown)   29 sites / 7 files
+ 3   sites the instrument counts in CARRIER files the roster excludes by predicate
        app/vendor/page.tsx · app/vendor/portfolio/page.tsx · components/vendor/BottomNav.tsx
− 2   sites the instrument is BLIND to (the string-trap, below)
        sanctuary :3608 · :3615
──────
= 30  instrument headline `tap_suppress=30`                                    ✔ reproduces
```

**The mechanism.** `tools/tdw09_stage2.py:51` — `STRIP_BLOCK = /\*.*?\*/` with DOTALL — is naive and **string-blind**. Two JSX attributes in `app/(frost)/frost/canvas/sanctuary/page.tsx` open a phantom block comment:

| site | `accept="image/*"` at | swallows | lines lost |
|---|---|---|---|
| 1 | `:2328` | 2328–2404 | **76** |
| 2 | `:3547` | 3547–3616 | **69** |

**145 lines of one file are invisible to every headline that instrument emits** — `below44`, `interactive`, `height_decls`, `pressed`, not only `tap_suppress`.

**The aggravation, and it is what makes this a finding rather than a quirk:** this species is **already known and already cured on the pwa side**. F-07.74 retired eleven copies of the naive rule across ten proofs onto `scripts/lib/stripComments.mjs`, and both benches in this very ZIP carry its canary (`§0.X` / `§0.Y`) naming `accept="image/*"` by name. The dream-os Python instrument never learned the lesson its sibling repo paid for.

**Standing for the successor, per relay #1 §2:** `§5`'s census close **may not rest on the blind instrument alone**. Use the arithmetic above; the roster's own grep — which *does* see `:3608`/`:3615` — is the settling witness for the swallowed ranges; the ledger states the instrument's blindness **by number**. The instrument cure is a dream-os byte and rides the next dream-os sitting, chair-carried. Editing it from a pwa-only phase would have been the STOP-and-fork it was correctly reported as.

---

## 6 · DISCLOSURES — owned by name, none quietly patched

1. **The storefront pin was a raw-grep count.** I first pinned `storefront = 2` off a grep that counted the file's own explanatory *comment* as a navigation edge. The cell counts through the stripper; the true value is **1**. My own cell convicted me — the independent-method law working in the direction it was written for. Corrected with the reason in-comment.
2. **W-9 did not bite on first draft.** My `§7.4` re-aim used a bare substring, so a mutation *appending* `&& o.rateMax !== ''` left the bench GREEN — the cell could not tell a min-only predicate from a min-and-max one, **which is precisely the law it exists to defend**. Strengthened to pin `!/rateMax/`.
3. **W-C did not bite on first draft.** Same defect, same hour, sibling bench: `/function roomClassForPath/` matched `roomClassForPathX`. Anchored with `\b`.
4. **My read-first's `§3(b)` was wrong and my own bisect falsified it.** I claimed P2A "broke a sealed 07-era ruling." `§3.1` was **already red at `84848e8`** (submit's edge, F-09.97). P2A did not redden a green cell; it added a **second** mismatch to an already-red one. The chair's relay #1 carried my premise forward as fact and has corrected its own record in-band.
5. **My read-first's `§3(c)` said four red cells against a count of five**, and my guess at which one was elder would have been wrong: the elder is `§4.3` (F-09.96), not `§7.4`.

**Two of my three mutation misses were caught only because I RAN the mutations rather than reporting them as run.** A non-biting mutation is a cell that was never proven.

---

## 7 · WHAT THE SUCCESSOR PICKS UP

**Chartered (relay #2 §4):** a fresh session on the same P2C kickoff with this handover as the chair-adopted addendum. Its five remaining limbs: `§1`'s 23 adoptions · `§2`'s `touchBox44` onto the chat chips · `§3`'s mirrors at the ruled R-X24 numbers · `§4`'s ink-on-brass role mint · `§5`'s census close, plus `tdw09_p2c.proof.mjs` and **Package 2's closing ledger**. Range **`.98–.110`**.

**Its baseline floor** — the attributed-red roster shrinks by two: `tdw07_p3_portfolio` **119/119** and `tdw07_p2_profile` **48/48** are now GREEN and stay on every floor list.

**Rosters, re-derived at tip and confirmed EXACT against the kickoff (zero drift, per-file counts identical):**

| sites | file | disposition |
|---|---|---|
| 20 | `app/(frost)/frost/canvas/sanctuary/page.tsx` | ADOPT — per-file `PressBtn` local wrapper |
| 1 | `app/vendor/collab/page.tsx` | ADOPT |
| 1 | `components/vendor/VictorModeChip.tsx` | ADOPT |
| 1 | `components/vendor/CalendarBlockSheet.tsx` | ADOPT |
| 4 + 1 + 1 | `app/demo/vendor/[handle]/page.tsx` · `.../collab/page.tsx` · `components/demo/ModePill.tsx` | **DEMO-HELD, F-09.89** |

**23 adopted + 6 demo-held = 29.** `files_with_pressed` reads **12** by the instrument's own `PRESSED_SIGNALS` predicate and **7** by the roster's carrier test (`pressedStyle|onPointerDown`) — **different predicates, both correct at their own definition**; the kickoff prints them as if commensurable, and the successor should not.

**Two ladder drifts, ruled at relay #1 §3, restated so they are not rediscovered:** `TDW_09_S5_FLOW_AND_GRAMMAR.md` lives in **dream-os** `docs/specs/`, not the pwa. **There is no P2-R1 handover document** — `5c88e8a` touched zero `docs/`; R1's rulings reconstruct into the closing ledger from commit message + bench + code, provenance labelled at that grade. No retroactive handover is authored.

**Corpse duty (P-1(a)) at this seam:** all three primitives (`pressedStyle`, `touchBox44`, `rowBaseline`/`rowGlyphSlot`) still hold live consumers; this ZIP touches no product byte and so cannot have created a corpse. Asserted, not assumed — and the successor re-asserts it at ITS delivery tip, because that is where the sweep lands.

---

## 8 · FOUNDER SMOKE CARD

**There is none, and that is the correct answer, declared rather than omitted.** This delivery ships two `scripts/` files. It changes no route, no component, no style, no copy, and no rendered pixel — there is no thumb-path to walk, and inventing one would be discovery, which the witness-path law forbids. The evidence is the verify line in the apply block, which you run and paste back.
