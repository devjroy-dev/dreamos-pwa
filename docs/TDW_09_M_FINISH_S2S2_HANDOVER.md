# M-FINISH · SITTING 2, SESSION 2 — THE SIX ITEMS

**BASE (R-38.15): `7142cbf` = `origin/worklist`, re-derived at the moment of cutting.**
dream-os `b65b27a` = `origin/main`, sibling clone present. **Railway/Vercel green: NOT
CLAIMED** — nothing here ran against a deploy, and the render arm needs one.

**Packet:** the S2/2 kickoff arrived as a text relay, so its hash is **recorded, not
re-derived** — byte-exactness of a pasted document cannot be established.
Founder's line: `fb83289a2c8b7b7f5b846804101f07f11eeb334644c8e8da2d8df6dcc238bfbd`.

---

## §0 · ENVIRONMENT — THE PRECONDITION, AND THE ONE THAT WAS NOT IN THE LAST HANDOVER

```
pwa worklist 7142cbf · dream-os main b65b27a
npm ci in BOTH repos before anything. The pwa's is not optional:
tools/wl_render.cjs requires @sparticuz/chromium out of node_modules.
```

**RUN `npm ci` BEFORE THE FLOOR, OR THE FLOOR LIES BY EIGHT** — S2 §0's lesson, unchanged,
and it now has a third sighting in the pair below. **AND KEEP THE `dream-os` SIBLING BESIDE
THE CLONE**: three floor benches (`tdw09_p2b_vocab`, `tdw13_d6_parity_matrix`,
`tdw15_p1_events`) refuse without it, which is how a worktree at `b251600` reads 25 REDs
where the named base is 22.

---

## §1 · F-38.27 · THE ZIP-BOUNCE CURE BROKE A FLOOR BENCH AND SHIPPED

**This is the finding of the sitting and it was found at §0, by running the floor rather
than quoting it.**

`tdw37_leadgate_b_slot` read **36/37** at `7142cbf`. The RED cell was
`the slot routes at Billing, the CE-210 home`, which asserted `/href="\/vendor\/billing"/`
in `SliceShell.tsx`. `f542795`'s own R-38.1 cure — the remedy for the S2 ZIP bounce —
replaced that literal with `roomHref('billing')`. The cure and the bench were correct
about different things and nobody ran them together.

**HOW IT GOT PAST S2.** §5's `FLOOR = NAMED BASE, no delta` was derived at `b251600`,
before the bounce cure existed. §9's remedy re-derived the cells and never re-ran the
floor. The handover's floor line was true when it was written and false when it shipped,
which is CE-22 broken in the one dimension R-38.16 had not yet covered.

**THE PAIR, DERIVED RATHER THAN REMEMBERED** — and both halves are stated because the
naive comparison gets it wrong:

| tree | REDs | note |
|---|---|---|
| worktree at `b251600`, **no sibling** | 25 | three benches refuse for want of `dream-os` |
| `b251600`, sibling present | **22** | = S2's named base |
| `7142cbf`, sibling present | **23** | delta of one: `tdw37_leadgate_b_slot` |

**THE CURE IS A LABELLED, COUNT-PRESERVED, IN-PLACE AMENDMENT.** The cell's subject never
changed — the slot must route at Billing. What changed is that Billing's address is the
registry's to answer, so asserting a SPELLING would pin the bench to a string the estate
has deliberately stopped writing. It asserts the destination in **two halves**, because
either alone passes on a defect: the CTA asks `roomHref('billing')`, **and** the registry
still has a billing href for it to return. A registry that lost that row would red here
while the call site still looked right.

**NON-VACUITY, BY MUTATING PRODUCTION SOURCE:** restoring `href="/vendor/billing"` in
`SliceShell.tsx` reddens `tdw37_leadgate_b_slot` alone at 36/37, naming that one cell;
restoring the file returns 37/37.

`scripts/` was outside §2's grant. Named under STOP, ratified at relay #1, and the grant
widened for that one file. **R-38.19 followed**: every ZIP's verify block runs the pwa
floor with the sibling present, and `FLOOR = NAMED BASE` is re-derived at the cut, never
quoted from §0.

---

## §2 · THE FLOOR AT THE CUT (R-38.19, re-derived here)

**23 RED on the dirty tree.** The count is the same as the base's and **the set is not**,
which is exactly why the rule asks for a diff rather than a number:

| bench | at `7142cbf` clean | at this cut |
|---|---|---|
| `tdw37_leadgate_b_slot` | RED | **GREEN** — F-38.27 cured |
| `tdw_f0774_vacuity_probe` | green | **RED** — dirty-tree only |

The vacuity probe reds on any dirty tree exactly as ZIP 14 ⑦ documented and as S1 §7 and S2
§5 both witnessed at the founder's terminal. **It goes green the moment the commit lands.**
So: **`FLOOR = NAMED BASE (22)` at the commit**, and nothing else moved.

---

## §3 · ITEM 1 · THE ARM

### (a) The re-seed retires

`settle()` retried once, re-seeding the session in between, and printed a scrupulous line
about not naming a cause it had not derived. It was still **an instrument repairing its
subject mid-measurement and then reporting green**. The only thing that clears that session
is the product deciding to (`_base.ts:103-124`), so a surface that needed a re-seed to
settle is a surface the vendor was also thrown out of — and the retry hid that behind a
PASS.

It now takes **the names of the cells that depend on it** and records
`surface did not settle; re-seed refused` against each. The alternative — return false and
trust every call site to remember — is how a surface that never mounted becomes a cell that
was never printed, and four call sites were already hand-writing their own version of that
line. `reseat()` is deleted. The `mode` argument went with it; nothing else needed it.

`evaluateOnNewDocument` **stays**: it is the fixture install before every document, not a
recovery from a failure.

### (b) C-R9 follows the room, and C-R9b keeps the fallback

C-R9 hit-tests `.wl-coin` at **`/w/leads`** now. Leads crossed at S2 §4-1; the room the
vendor opens from the tile is the shell's, with the shell's coin and the shell's drawer. A
cell that went on hit-testing only the fallback would have kept saying yes about a surface
she had stopped visiting.

**C-R9b** keeps `[data-tour="profile-coin"]` on `/vendor/list/leads`, where F-38.13 lived.
Dropping it with the move would have retired a cell by relocating it — coverage lost in a
step that looks like coverage kept, which is the shape the S2 ZIP bounce convicted twice.
**Its retirement is written in the cell**: Phase 7, with `app/vendor/layout.tsx` and its
Header. A retirement date that lives only in prose is a retirement nobody performs.

### (c) The fixture is real, or the arm says so and reds

`tools/wl_mint_token.sh` — **sourced only**, and the refusal explains itself: an executed
export dies with the subshell and the arm would run SYNTHETIC while you believed otherwise.
`read -rs` for the PIN, checked against the server's own `PIN_RE` before it spends one of
five attempts against the lockout on your account. Body on stdin via `--data @-`. Response
parsed by `node`, not by a regex over JSON. Refuses any phone but `9888294440`, with an
optional argument that exists only so that passing a different number is a refusal rather
than a silent success. **Not argv, not a file in the tree, not the shell history.**

The arm reads `WL_RENDER_TOKEN` from the environment and asks the real `GET /me` with it
**before a browser is launched**, so a run declares what it is before it can spend a minute
of yours pretending to be something else. The API host is read out of
`lib/vendor/api/_base.ts` rather than retyped — one home, the same correction C-R3 took for
the room count.

**`tdw_vendor_handle` seeding is DELETED.** C-R12's whole subject was the handle cache, and
the arm had been writing `DEVROY` into it and then asserting a card built from it. That is
a mirror, not a measurement. C-R12 now **warms the cache by navigating** — the vendor's own
second load, which is the state F-38.21's cure is about — and asserts `WIRE.handle` in the
next load's first paint. C-R10 asserts `initialsOf(WIRE.name)` instead of the literal `DR`.

**There is no `/me` stub anywhere in the file** and the prohibition is the point: a stub
would let every cell go green with no server behind it.

**⚠ THE PROVENANCE OF `fixture: REAL (9888294440)`, PRINTED RATHER THAN IMPLIED.** `GET /me`
carries no phone field (`dream-os src/api/vendor/me.js:68-92`). The number is true because
the mint script refuses to mint for any other, not because the arm read it back. The line
ships exactly as ruled with that sentence underneath it.

### C-R15 · the session survives the wire

**The thing four cures in that file were built to hide, promoted to a cell.** F-38.8
established the 401 → failed refresh → `clearAndRedirect` chain; the re-seed, the
per-document seeding and the load-bearing cell order are all machinery for continuing to
measure a shell the product had already thrown the fixture out of, and **none of them
asserted that it had not**. C-R15 waits on the response to the shell's own `/me` rather
than a clock (D-38.1), then asks whether the session is still there and whether the browser
is still under `/w`.

### ⚠ ZIP 2 · TWO CELLS THE FOUNDER'S RUN CONVICTED, AND ONE FREE PROOF

The first cut was applied to the working tree and run against a Vercel deployment still
built from `7142cbf`. **That accident produced the RED half of both-ways that no seat here
could run**: the arm reddened at the uncured tree on exactly the cure assertions — C-R14 on
the retired card titles, C-R17 on `numeral:true, t0:1`, C-R18 refusing a surface with no
FAB, and **C-R16 reading exactly 4**, the number this handover's §4 predicted for the
uncured tree from source before any of it ran. Nine of eleven REDs were the instrument
working.

**The other two were mine.**

- **C-R12 would have reddened the CURED tree too.** It asserted `WIRE.handle` in the card's
  TEXT, and item 3 arm (a) withholds the address row until `/v/` answers — the wa.me link
  the card carries meanwhile lives in a JS closure, not in the DOM. **I wrote the cell
  against the card R-38.17 describes and shipped the card the withholding rule permits.**
  Same class as C13's prefix matcher: two halves of my own delivery disagreeing, with
  nothing but a run to say so. It asserts the CARD now, by its register title — which is
  still authenticated, because the card renders only when a handle exists and the handle
  comes from the wire and nowhere else since the seeded cache was deleted. The stronger
  claim is **withheld with the row it would read**, same date, stated at the site.
- **C-R15's light-mode FAIL was a race.** `waitForResponse` is single-shot; C-R10 leaves an
  in-flight `/me` that the next navigation aborts, so the cell passed in dark and reported
  「the shell never asked GET /me」 in light, on one tree in one run. **A cell whose verdict
  depends on which mode ran first is not a cell.** It listens across the navigation and
  waits for quiet now — C-R16's shape, which came back identical in both modes.

### The both-ways, and who can run it

**SYNTHETIC → the four authenticated cells RED (C-R10, C-R12, C-R15, C-R16), chrome cells
GREEN, verdict RED.** Verified in this container as far as it goes: with no token and no
reachable deploy the arm prints the SYNTHETIC banner, the refusal line, both
`the shell never seated` FAILs and exits RED. **The green half needs a deploy and your PIN
and is therefore yours** — this seat has neither, and says so rather than implying a run.

**Cells: 20 per mode, 40 total.** §5 asks for 32+.

---

## §4 · ITEM 2 · F-38.26

`vendorMe()` in `hooks/vendor/useVendorHandle.ts`: a module-scope in-flight promise, keyed
on the access token, **one site**. `useVendorHandle`, `useVendorInitials` and
`app/w/layout.tsx` all await the same request.

**A PROMISE, NOT A CACHE, AND THE DISTINCTION IS THE DESIGN.** A cache stores an answer and
then owns the question of when it is stale. This stores the REQUEST. Module scope is the
right lifetime because it is the session's lifetime: client navigation keeps it, a real page
load resets it, and a page load is the one moment the answer could have changed underneath
us anyway. **Keyed on the token** — a different token is a different vendor, and answering
her with the previous session's identity is the worst thing this file could produce.
**A rejection clears itself and rethrows**: caching a transport failure would blank the
medallion and the link card for the rest of the session.

**C-R16 walks IN-APP and the choice is load-bearing.** A `p.goto` between rooms resets
module scope, so a `goto`-based walk would green on a broken tree by measuring three
separate sessions. It clicks the Leads tile and then the Rooms seat — both anchors, both
client navigation — and **attaches its listener before the first navigation**, because a
listener attached after the shell mounts misses the layout's read and would report two
where the vendor paid three.

**THE COUNT: "six" IS NOT RE-DERIVABLE AND THE CELL DOES NOT ASSERT IT.** Three `/me` sites
lie on a shell path (`app/w/layout.tsx:57`, `useVendorHandle`, `useVendorInitials`); a
Rooms → Leads → Rooms walk should produce **four** — the layout once, the medallion on
every `WorklistShell` remount. The cell reports what it observes and asserts **one**.

**F-38.28, and it is an S2-EXTERNAL TOUCH.** The memo means a handle or name changed in
Settings inside the same session would need a reload to show. Scope was widened by ruling
for **two lines only** in `components/vendor/SettingsScreen.tsx` — `forgetVendorMe()` in
the success arms of `saveMe()` and `saveHandle()`. Nothing else in that file moved.
`WorklistShell`'s sign-out clears it too: structural guarantee plus explicit one, on the
single path where being stale is unrecoverable rather than untidy.

---

## §5 · ITEM 3 · THE COPY RECUT, AND THE BYTE THE CHAIR AMENDED

### F-38.31, and c-38.14

R-38.17's first cut put 「Nothing needs you yet.」 on the masthead unconditionally. This seat
filed against the ruling: **that sentence asserts an absence nothing has checked** — the
exact claim `todayCountCaption` and `todayNotLive` were written to refuse, in a file that
argues the point at length. The chair amended his own byte rather than defending it.

**AND THE OBJECTION CONVICTS THE NUMERAL TOO.** A `0` no instrument produced is the
identical claim in digits. Curing the sentence while keeping the digit would have been a
cosmetic fix to an honesty defect, and it would have passed every cell about the sentence.
So the numeral is **gated, not defaulted**: `feed.responded && feed.openItems !== null`.
`feed.openItems ?? 0` is what C34 exists to catch.

`lib/worklist/feed.ts` is the one home for the fact. **It is a reader, not a constant**, and
the reason is written at the site: a `const FEED_RESPONDED = false` would have made the
surface's conditional dead in one direction — a branch no cell can reach and no reader
trusts. Both arms are live code today. The Phase 4 step is stated in the file: replace the
body, keep the shape, `responded` true only on a 200 that carried a count, fail closed in
the same direction `app/w/layout.tsx`'s guard fails.

### Card 3 · WITHHELD BY RULE — F-38.30 = F-19.14

**`/v/` is a 404 today.** `app/v` does not exist in this tree. The P0-B seat derived the
same fact independently and filed it as **F-19.14**: no per-vendor public URL exists
anywhere in the estate, `middleware.ts` rewrites on demo hosts only with no wildcard handle
map, and `tdw_referral_invite` is APPROVED at Meta pointing at `/v/`. **One finding, two
filers, cross-referenced in both handovers** (chair's ruling, relay #2). It is owed at
**TDW_19 P0-B step 4**.

Never-404 binds, and shipping the address would have replaced a working affordance with a
dead one. Arm (a) ruled: the address row ships **fully commented, with its uncomment step
dated and exact** — when P0-B step 4 lands `/v/<code>`, delete the comment block and the two
markers, point `SHARE_URL` at `'https://' + COPY.cardLinkAddressBase + handle`, nothing else
in the file moves. `cardLinkAddressBase` lives in `copy.ts` from the moment it was vetoed;
the CSS rule ships too, so the uncomment is one comment block and not a comment block plus a
stylesheet edit nobody wrote down.

### The rest of the recut

- Masthead: date · **gated numeral** · status at **t1** · rule. `todayTitle` retires — a
  page title over a masthead that already names the day is two lines where the surface
  needed one, and neither said what state Today was in.
- `todayEmpty` / `todayEmptyAction` retire. An empty state with its own action, above three
  cards that are each an action, pointing at a nav seat that is on screen at all times.
- Eyebrow `Get started`. Card 1 `Enquiries` — a noun; the old title claimed hours and the
  body repeated it. Card 2 `Ask TDW`, **no body**, eyebrow `Try` over the chips. Card 3
  `Your link`, no body, `Copy` · `Share` as two verbs where one button used to guess which
  the vendor meant.
- `AskSheet` gains `TDW replies on WhatsApp.` — the vendor types in-app and the answer
  arrives on her phone, and a surface that does not say so is wearing the costume in the
  other direction.
- **The sheet's title was an inline literal and that was a copy-law breach**, not a tidy. It
  reads `COPY.dockAria` now: the sheet's title and the label of the control that opens it
  are one statement about what this is, not two that can disagree.

### 「DreamAi」 banned, R-37.78 tombstoned

R-37.70 permitted the name in prose and forbade it only in labels. **That exemption had to
go rather than be narrowed again**: it was a rule about WHERE a name may appear, and every
surface that wanted the name could argue it was writing prose — `cardDeskBody` and
`cardAskBody` both did. A ban with a register-shaped exception is a ban that loses one
sentence at a time.

R-37.78's naming grammar retires **because its middle clause did**. With the NAME banned
there is no term left for the grammar to place, and a two-term grammar over one term is not
a grammar. The tombstone is written rather than the block deleted, because a rule that
vanishes gets reinvented by the next reader who notices there is no rule.

---

## §6 · ITEM 4 · THE ADD CONTROL

**It is an entrance, not a second home for create.** Five legs hand off to
`components/vendor/AddSheet` with the same `slice` prop the five list rooms pass; Note opens
the composer `NotesBody` already owns; Calendar goes to the calendar room. Nothing here
duplicates a form, a validator or a vetoed byte — asserted, not asserted-about (C35 reddens
on a create call inside the file).

**THE THIRD CASE IS NAMED.** Calendar resolves through `roomHref`, which today returns the
**declared interim** `/vendor/calendar` — it is not a `/w/` route and not an AddSheet leg,
and it becomes a `/w/` route in the same edit that crosses the room, with nothing here to
remember. The cell asserts all three kinds.

**SCOPE IS A MOUNT, NOT A PATHNAME TEST.** A control that decides for itself where it exists
carries a second copy of the ruling inside it. C35 reddens on a `usePathname` in the file and
on a mount outside Rooms; C-R18 measures the absence on Today, on glass.

**THE PRESS IS GEOMETRIC, AND THAT IS A RULING RATHER THAN A SHORTCUT.** F-38.14's 1.5:1 is
a ratio between a row's pressed fill and the ground it sits on. It does not transfer to a
solid accent disc floating over arbitrary content: there is no fixed neighbour, and any
darker accent chosen for it would be **a colour nobody measured** — precisely what the Slice
Door's retired opacity was. So the press is size and depth, 56 → 52.6 with the shadow pulled
in. **No new colour token was invented for a pressed state**, and that refusal is the point.

**THE OFFSET IS ARITHMETIC AND ARITHMETIC IS NOT EVIDENCE.** The FAB's bottom is a `calc`
over two chrome heights it does not own; if the dock gains a row the rule stays true and the
control lands on top of it. **C-R18 measures the painted gap** in both modes, and the
derivation in the comment is there to be followed, not to be trusted.

---

## §7 · ITEM 5 · `?add=1`

One effect, on mount, calling the existing `setAddOpen(true)`. It adds no second way to open
the composer; a parameter that opened its own copy would be two homes for one surface. C36
asserts the empty dependency array — re-running would re-open the sheet behind a vendor who
had just dismissed it.

**⚠ DEVIATION FROM THE RULED SHAPE, DISCLOSED, RATIFY-OR-REVERT.** The ruling says one
`useEffect` over `useSearchParams`. This reads `window.location.search`, and the reason is
the estate's own precedent on this exact parameter class:
`app/vendor/calendar/page.tsx:280-282` reads its `?block=` leg the same way and states why —
「no useSearchParams: keeps the page free of a Suspense boundary」. Under Next 16 the hook in
a client component forces a Suspense boundary at every caller or the prerendered route
deopts, and **this component has three callers, two of them outside this seat's contention
grant** (`app/vendor/list/[slice]/notes.tsx`, `app/vendor/studio/notes/page.tsx`). Choosing
the hook meant editing two out-of-scope files to add boundaries, to read one integer. **The
revert is five lines.**

---

## §8 · INSTRUMENTS

### `b40` — 31 cells → 36, amendment labelled

**C32** no persona name in any shell STRING, `DreamAi` included, and the grammar recorded as
retired · **C33** the ask affordance's three keys cannot drift, and the sheet reads the
register instead of inlining it · **C34** the numeral and the true-empty line are gated on a
reading · **C35** the Add control's frozen order, seven real legs, accent token, Rooms-only
mount · **C36** `?add=1` opens the composer that already exists.

**C13 AMENDED, AND IT CARRIED TWO DEFECTS OF ITS OWN.**

- **F-38.29** — the S1 amendment stated *in words* that R-38.6 cuts the body ceiling from
  three sentences to ONE, and the assertion underneath went on reading `count > 3`. **A cell
  whose comment reads like the ruling and whose code does not perform it is worse than no
  cell**: it is a paragraph nobody re-derived, agreeing with itself. Cured at the site, with
  R-38.17's clause folded in — one sentence, fourteen words.
- **THE MATCHER WAS ANCHORED ON A PREFIX.** `cardAskChips` is a prefix of
  `cardAskChipsEyebrow`, so the moment R-38.17 added the eyebrow key beside the list, the
  chip matcher ran from the eyebrow to the first bracket and counted its byte as a sixth
  chip. **It reddened a correct tree** — the same class as the audit matcher that read
  double-quoted attributes only, found the same way, cured the same way: match what you mean.

### `wl_audit` — the R-38.6 set grows, and one entry carries an asterisk

Nine retired bytes added. **`Nothing needs you yet.` is on the retired list and it is not
wrong copy** — it is TRUE copy shipped one phase early. The list is what stops it arriving
by a later tidy, and **it comes off in the same edit that wires `feed.ts` to a real read**.
The byte lives in `copy.ts` the whole time.

`DreamAi` joins the persona cell. And the **t0 cell's claim is narrowed by label**: it
asserts where the rung is DECLARED, not that it paints, because after c-38.14 those are two
different facts — the rule correctly ships on `/w/today` while no element consumes it.
C-R17 owns the painted claim. Saying so is the whole of D-38.1.

### `wl_render` — 15 cells per mode → 20

New: C-R9b, C-R15, C-R16, C-R17, C-R18. Moved: C-R9. Rewritten: C-R10, C-R12. C-R14 now
reads the expected card title **from the register** rather than holding its own copy of a
vetoed string — a cell with a retyped byte goes red on a correct tree the day the byte is
re-vetoed, which teaches the next seat to loosen it.

### Compile · `npx tsc --noEmit` — exit 0

**And it caught the backtick trap for the sixth time this arc.** A comment inside a CSS
template literal, written around the selectors it was retiring. `WorklistShell.tsx` carries
the mechanical rule at the top of its own literal; `app/w/today/page.tsx` and
`components/worklist/AddFab.tsx` now carry it too. Selectors in those blocks are written in
words, not in code marks.

`next build` was **not** run in this container: four `next/font/google` fetches are outside
the egress allowlist, as S2 §5 documented. **The founder's own build is the evidence for the
fonts and for the route graph.**

---

## §9 · WHAT THIS SEAT OWNS

- **The render arm's green half is unproven.** No deploy is reachable and the token needs
  your PIN. The SYNTHETIC direction was witnessed here; the REAL direction was not.
- **The four prior arm FAILs are re-labelled INSTRUMENT.** C-R6's hollow green, C-R7's
  NEVER MOUNTED, C-R8's crash, C-R12's planted handle — every one of them was the
  instrument failing, not the tree, and the founder's glass frames were the witness that
  the surfaces were fine. They are not defects in the shell and must not be read as such by
  whoever counts this arc's findings.
- **The captures are specified, not taken.** As at S1 and S2, the founder's frames are the
  real-session evidence.
- **`wl_audit` was not run against a domain**; it exits 2 without one, as designed.
- **Item 5's shape is a disclosed deviation** (§7) and item 4's press is a ruled refusal
  (§6). Both are argued at their sites and both are mechanical to revert.
- **The FAB's `calc` offset is derived arithmetic.** C-R18 is what makes it evidence; if
  that cell is ever loosened, the number goes back to being a guess.

## §10 · THE NEXT SITTING

§4-2's eight rooms remain, in `INTERIM_VENDOR_ROOMS` order: **calendar, storefront,
portfolio, couture, team, contracts, tds, collab**. Calendar first is now doubly indicated
— the Add control's first leg and `AddSheet.tsx:486` both point at it, and both re-point in
the same edit that crosses it.

Then Khata (§4-3), Settings on the rungs and `Sign out` confirm (§4-4), F-38.3 (§4-5), the
retirement sweep (§4-6).

Carried forward: `rooms.ts:1` still reads "THE SEVENTEEN ROOMS" against
`ROOM_COUNT_EXPECTED = 18` — corrected to **NINETEEN at the Khata edit**, one correction at
one site. **F-38.22** (thirty colour literals), **F-38.23** (six header words, three homes),
**F-38.24** (the census), **F-38.30 = F-19.14** (`/v/` 404, blocks card 3's address) and
**F-38.31**'s Phase 4 half (the true-empty byte, on the retired list until the feed answers)
are open. C27 remains a guard, not a cure cell.

**THE 「THE MAKER」 NOTE, BESIDE R-37.79.** R-37.79 is one identity everywhere, and the
identity the shell paints is initials off a name. `dream-os src/api/vendor/me.js:72` returns
`user?.name`, while `pin-login` writes `business_name || users.name` into the session — so
the medallion can seed from a BUSINESS name and be corrected to a PERSON's on the wire read,
changing under the vendor for reasons neither byte explains. C-R10 asserts the seed matches
the wire's answer and would red on exactly that divergence, which is how it will be found
rather than argued about. **The word 「Maker」 is the estate's own noun for a vendor**
(`auth.js`: 「This number is not a Maker account.」) and appears in no shell byte; if the
identity question is ever reopened, that is the register it should be reopened in, and not
by inventing a fourth spelling of a name.
