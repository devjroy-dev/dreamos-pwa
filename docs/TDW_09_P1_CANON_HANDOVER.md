# TDW_09 · PACKAGE 1 · THE CANON FOUNDATIONS — EXECUTOR HANDOVER

**Session:** LE under the twenty-fourth chair, relays #1 + the WhatsApp-green chair note.
**Repo:** `dreamos-pwa` ONLY — **dream-os ZERO-BYTE** (no reason found; none forked).
**Base:** `8a1fee1`, re-fetched at delivery (origin unmoved both repos: pwa `8a1fee1` · dream-os `0c08dfe`, CE-201 at its tip). P3's F-10.59 rider had not landed; disjointness at the actual tips is trivial — this delivery's radius (`app/globals.css` · `lib/vendor/controls.ts` · two scripts · docs) intersects neither P3's admin radius nor its named F-10.59 radius (`app/vendor/discover/**` + `src/lib/vendor/discover.js`).
**Range:** F-09.82–.99 issued; **allocated .82–.84** (chair-confirmed at relay #1); next free **F-09.85**.
**Rulings executed:** CENTRAL FORK = (b) · P-1 = (a) with the retirement-duty rider · P-2 = NO (ratified law-grade) · B-4 re-scope ratified · mints .82/.83/.84 confirmed · 「 green it 」 (founder word, the Block-08 shelf item closes).

---

## 1 · WHAT SHIPPED — FOUR LIMBS, ZERO VISUAL CHANGE

### B-1 · The both-homes completion sweep (`app/globals.css`)
Four cures, all moving GLOBALS to the owner, never the owner:
- **F-09.77 CURED** — light `--atelier-card-border` `0.20 → 0.18`. Ground correction carried: the token grew live readers (demo-lane rows) since MICRO-2; the 「 no radius reader 」 note dies with the divergence.
- **F-09.82 CURED** (minted + cured in-sitting) — light `--atelier-header-bg` `0.94 → 0.96`, the same class, found by the same comparator.
- **R-M5 COMPLETED** — `--atelier-page-bg` + `--atelier-section-bg` declared in BOTH homes at owner bytes; they were published post-mount and read live (`app/demo/vendor/[handle]/layout.tsx:246` reads `page-bg` unfallbacked) while declared in neither — the first frame rendered inherited/transparent where the owner holds a value.

Post-mount: zero delta by construction (ThemeContext overwrites). Pre-mount frames align to the owner — the F-09.35 class's whole point.

### B-2 · The three interaction primitives (`lib/vendor/controls.ts`)
`pressedStyle` (scale .98/80ms, reduced-motion keeps the opacity arm — reduced motion is not a request for zero feedback) · `TOUCH_FLOOR`/`touchBox44` (transparent padding + compensating negative margin; no-op at-or-above 44) · `rowBaseline`/`rowGlyphSlot` (R-X24's shape). **Consumer-zero, staged, with PACKAGE 2 NAMED IN-FILE AS CARRIER** per P-1(a) + CE-201's named-carrier law; the in-file comment distinguishes staged-API-with-named-carrier from wire-or-delete's zero-caller-check class and states the retirement duty (a dropped primitive is a named line in P2's delivery, never a silent orphan).

### B-3 · The census as a committed instrument (`scripts/tdw09_vendor_census.mjs` + `.json`)
Code-only (comments stripped), per-file rows, the counting method stated in its own header — **the instrument owns every number**: HEX **167 occurrences · 31 distinct**; BRASS **269 occurrences · 39 distinct alphas** (the read-first's ~290 was a raw-grep floor including comments; the stripped instrument settles 269); **77 files** carry literals. **The lawful-invariant roster ships BY LAW:** the pin trio (R-M6/THE-LANDING-IS-THE-LAW) · `Splash.tsx` (R-M4(c)) · **`#25D366` RULED-INVARIANT BY FOUNDER WORD 「 green it 」** with the F-06.85 mechanism (WhatsApp recognition — no theming pass ever re-tints it) and every site enumerated by file:line in the JSON.

### B-4 · The F-09.78 gradient watch (`scripts/tdw09_p1_canon.proof.mjs` §4)
No value moves (a metal darkening would repaint every metal site against the zero-visual bar, with zero live damage to cure — arithmetic re-derived: 4.26:1 on the lower stop, 4.66 elsewhere). The watch walks every file **compositing** the sheetTop→sheetBot gradient and convicts on the first metal-ink pairing — F-09.77's watch shape, which proved itself when .77's reader arrived.

---

## 2 · PROOF

**`scripts/tdw09_p1_canon.proof.mjs` — 24/24 at the cured tree.** Both-ways, each cure file reverted ALONE at `8a1fee1`:

| reverted alone | result |
|---|---|
| `app/globals.css` | **19/24** — §1.1 (the comparator convicting all four divergences itself) + §2.1–2.4 RED |
| `lib/vendor/controls.ts` | **18/24** — §3.1–3.6 RED (§3.7 correctly green both tips: consumer-zero is a guard, true at both) |

**tsc exit 0.** **The floor whole at the cured tree, every count from the bench's own line, exit 0:** `home 67/67 · landing 98/98 · type 16/16 · surface 51/51 · roles 130/130 · money 18/18 · palette 18/18 · theme_retire 16/16 · p3_landing 89/89 · console 55/55 · factory 45/45 · invite_spent 14/14 · prospects_console 54/54 · shell 53/53 · bridge 44/44 · retint 76/76 · deck 159/159 · p4b_probe 35/35 · p4b_slice1 30/30`. dream-os floor witnessed at seating (`0c08dfe`, zero bytes shipped): `micro 23/23` · known-reds exactly four as attributed (`meter 28/29` F-06.41 · `f0555 22/23` F-07.11 · `f0772 158/159` §12.14 · `p4b_body 75/76` §5.26).

**Acceptance:** ① mechanical half GREEN (§6: publication list exactly 25, owners untouched at the cured tokens) — **the live half is the founder's one-screen-per-theme glance, on the smoke card below**; ② **25/25 owner-equal both homes** — the comparator is now a permanent cell and F-09.77's class is dead as a class; ③ three primitives property-asserted, carrier named (§3); ④ both-ways + floor + tsc above.

---

## 3 · THE S1 §3 SUPERSESSIONS, RECORDED BY NAME (the central-fork ruling's rider)
So no future session "restores" a value that never lived: S1_TOKEN_CANON's proposed `ink-muted #6E6B67` was **superseded by R-U18's alpha ladders** (dark `rgba(240,230,210,0.58)` / light `rgba(26,15,8,0.70)`, ratios in-comment at the owner); its proposed Porcelain accent `#5E1A24` **never existed in the tree** — LIGHT's accent is `#7A3828` (Addendum-A-era, walk-approved); its `app/tokens.css` + `lib/design/tokens.ts` homes are **superseded by the ruled layer 2** = `lib/vendor/theme.ts` (owner) + `app/globals.css` (both pre-mount homes) + `ThemeContext` (publisher). The spec survives as intent; the tree is the canon.

---

## 4 · COPY INVENTORY
Zero strings added, changed, or removed. Zero interactive controls touched (the primitives are unconsumed exports). No persona name in chrome; money register expected-zero, confirmed; W-1 clean — no soul/lens/prompt/engine file in radius.

---

## 5 · FOUNDER SMOKE CARD — acceptance ①'s live half (one glance per theme)
Fixture: any vendor account; no state to prepare — the bar is that NOTHING changed.

| # | do this | look at | paste back |
|---|---|---|---|
| 1 | Open the vendor app on the Espresso theme, land on the home | The screen you already approved — unchanged in every pixel you can name | one word |
| 2 | Drawer → Display → Light; glance the same screen on Editorial Paper | Same — unchanged | one word |

Anything that looks different from yesterday is a defect in this delivery — say so and the ZIP bounces.

---

## 6 · DISCLOSURES
**D-1 — the §4 walker's false conviction, cured by its own red.** The first gradient-watch cut matched name co-occurrence and convicted `ThemeContext.tsx` — the publisher lists both stop names without compositing anything. Corrected to composition-matching (`linear-gradient` over both stops); the exclusion is earned in-comment by the red that taught it.
**D-2 — the read-first's brass count was a raw floor.** ~290 came from an un-stripped grep; the committed instrument settles **269/39** and owns the number per the roster law. The distinct-alpha count (39) and file counts stood.

---

## 7 · WHAT PACKAGE 2 INHERITS (the carrier's manifest)
- **The three primitives + their filed adopters:** `pressedStyle` → the 29 suppression sites first (F-09.21) · `touchBox44` → the vendor chat chips first (F-09.22) · `rowBaseline` → F-09.16's mirror sites per F-3(b), acceptance = R-X24's re-shot numbers. A dropped primitive is a named retirement line.
- **F-09.83** (ink-on-brass fragmented: `#1A120E`×58 · `#111`×22 · `#111111`×5 · `#0C0A09`×10) — the role token and its sub-perceptual normalization are P2's, lawful under the filed finding.
- **F-09.84** (the brass-alpha hairlines, 269/39/77 per the instrument) — its cure is its OWN ruled pass, not P2's by default; queued.
- **The adoption law (P-2's ratification):** every literal→token move classifies its site's theme context first — a DARK-valued literal adopted onto a themed token is the pinned-ground species in reverse.
- **The census instrument** re-runs at P2's read-first; its numbers move only by labelled amendment.
- `VendorProfileView.tsx` remains byte-untouched until P3 seals.

**Open watches after this sitting:** F-09.78 (armed at §4, zero live pairs) · F-09.79 (untouched, the two-member pin stands — P1 re-themed nothing). **F-09.77 CLOSES** (cured; the class-killer comparator stands in its place).

Sequencing beyond this sitting is the founder's.
