# TDW_09 · PACKAGE 2 · PHASE C — ZIP 2 · THE INK, THE CHIPS, THE MIRRORS · HANDOVER

**Base:** `dreamos-pwa @ 2452eb5` (ZIP-1). **Under:** the twenty-fifth chair · sweep relays #3, #4, #5.
**Scope:** five limbs CLOSED — ① L4 ink whole · ② L2 chips · ③ L3 mirrors · ⑥ F-09.100's coin · ⑩ the founder SQL and walk account. **④⑤⑦⑧⑨ pass to the successor.**
**Gate at delivery:** `npx tsc --noEmit` → **0**. Every bench in `scripts/` run, not the charter's list.

> **THE SENTENCE THIS DELIVERY PUTS IN THE RECORD:**
> **A count is not a proof. `20 style spreads / 20 handler spreads` greens on a build where one press lights ten siblings.**
> ④'s acceptance bar was struck for exactly this reason (relay #5), and it was struck before a byte of ④ was written.

---

## 1 · THE FLOOR, RUN BARE (F-09.92), EVERY BENCH — NOT THE CHARTER'S LIST

ZIP-1's law applied: *a sitting's floor list is the benches its DIFF can redden.* This diff touches 23 files, so all 43 benches ran.

| bench | verdict |
|---|---|
| **tdw09_landing** | **GREEN 103/103** — was 98/98; grew by five cells, see §4 |
| **tdw09_roles** | **GREEN 131/131** — reddened by this diff, cured, see F-09.104 |
| tdw06_m3_report_chip · tdw09_p1_canon 24/24 · tdw09_p2_doors 86/86 · tdw07_p4b_body 133/133 · tdw07_p3_portfolio 119/119 · tdw07_p2_profile 48/48 · tdw09_surface 51/51 · tdw09_palette 18/18 · tdw09_theme_retire 16/16 · tdw09_type 16/16 · tdw09_home · tdw08_* · tdw07_* · tdw_auth_crossover 46/46 · tdw_f0770_authority 104/104 | GREEN |
| tdw10_p3_deck **191/193** · tdw_f0774_stripper **33/35** · tdw10_p2_retint **75/76** | **RED — ATTRIBUTED ELSEWHERE**, the CE-206 floor, byte-stable, untouched here |

---

## 2 · LIMB ① — THE INK, AND WHY IT WAS NEVER ONE CLASS

The charter carried it as *"the 34 in-radius `#1a120e` adoptions → 0."* Two derivations changed the limb before a byte was written.

**F-09.101 — the needle was the wrong case.** The charter spelled it lowercase. The tree carries `#1A120E` **63 times** and `#1a120e` **once**. A case-sensitive grep authored off the charter's own byte sees 1, and the `→ 0` cell goes **vacuously green on a sweep that moved nothing**. Cured with `-i` and a canary cell.

**The roster reconciles exactly** — 34 in-radius / 25 demo-held / 4 canon + 1 mint = 64 across tracked files. The charter was right on the totals.

**F-09.102 — but they are two classes, and the sweep as chartered would have shipped a defect.**

```
26 sightings  FAB-GOVERNED    .atelier-fab, globals.css:973-981, sets with !important:
                              background → oxblood · color → #F5F2EE
                              A stylesheet !important beats a non-important inline
                              style. The literal is ALREADY overridden on Editorial
                              Paper and matches the class on Espresso. Adoption is
                              runtime-byte-equivalent; the value is removing a literal
                              that lies about what governs its pixel.
 1 sighting   calendar:514 isSel — the selection pill is a NON-theming cream literal.
                              13.30:1 / 15.70:1 derived. A true flat-role site.
 1 sighting   calendar:514 isToday — F-09.100's ruled cure. See §3.
 4 sightings  UNGOVERNED ON A THEMING GROUND  ◄ F-09.102
```

**F-09.102's arithmetic, computed at this seat:**

```
--role-metal      DARK #C9A84C   LIGHT #826A27   (F-09.28 darkened it for AA-as-text)
--role-critical   DARK #E07B5C   LIGHT #BA4723

#1A120E on #C9A84C = 8.08:1      #1A120E on #826A27 = 3.55:1   ◄ fails the 4.5 body bar
#1A120E on #E07B5C = 6.30:1      #1A120E on #BA4723 = 3.53:1   ◄ same failure
#F5F2EE on #826A27 = 4.66:1      #F5F2EE on #BA4723 = 4.69:1   ◄ the cure, both clear
#F5F2EE on #C9A84C = 2.05:1      ◄ THE GUARD: the cure is CATASTROPHIC on Espresso
```

F-09.28 darkened `metal` so brass-as-**text** would clear AA on paper. Nothing re-derived the ink sitting **on** brass-as-**ground**. Four specimens: `portfolio:981` (HERO badge), `InputBar:87` + `:124` (send glyph and its SVG stroke), `DetailSheet:139` (Confirm-delete, on `--role-critical` via `SliceRow.tsx:20`).

**Why this stopped the sweep rather than riding it:** adopting these four to a deliberately non-theming `INK_DEEP` would freeze 3.55:1 into a *named role* — **making a sub-bar value look derived**, and falsifying the mint's own warrant, which argues the constant is correct *because its ground does not theme*.

**`InputBar:87` could not take the role at all.** It is an SVG `stroke` **presentation attribute**, and CSS `var()` does not resolve there. Sweeping it would have made the icon vanish — a defect no type check catches. It takes the JS ternary off `T.isLight`, which that file already holds.

**The mint.** `INK_DEEP` in `lib/vendor/theme.ts` — a **constant, not a ThemeTokens field**, because brass is `#C9A84C` in both themes. An ink whose ground does not theme must not theme either; a per-theme `INK_DEEP` would be two names for one value and the next sitting would "fix" the divergence by inventing one. Carries the F-06.85 mechanism note (*if brass moves, this value is re-derived*) and CITATION-NEEDS-A-CELL on the donor.

`--role-ink-deep` is pinned **identically** in both `globals.css` blocks, and the identity is declared as F-09.35 satisfied-**by-sameness** so it does not read as an oversight. `--role-ink-on-metal` is the conditional role and **must never flatten** — the 2.05:1 guard above is why, written into the declaration.

**Naming, flagged not widened:** `--role-ink-on-metal` also serves a `--role-critical` ground. The chair's ratified name under-describes its roster. Not renamed silently.

**All 34 sightings accounted, residual 0.**

---

## 3 · LIMB ⑥ — F-09.100, AND THE DONOR THAT WAS ALREADY IN THE FILE

The finding's arithmetic **reproduces exactly** at this seat: 2.13:1 on the light coin's 70% stop `#7A3828`, against 6.47:1 in Espresso and 7.76:1 for the cream. Derived, never carried.

**Strengthened:** measured across all three gradient stops, not the one the finding measured. `#F5F2EE` reads 5.31 / 7.76 / 10.81. **The worst stop clears the body bar**, so the cure holds across the whole coin.

**The-product-is-the-spec, literally.** `html.theme-light .atelier-today-coin` already declares `color: #F5F2EE`. The numeral is the **button's** text — a different element — so it never inherited that cure. And two other surfaces already knew the rule: `OnboardingOverlay:216` and `TipsCarousel:231` both read `isLight ? '#F5F2EE' : '#1A120E'`. **The estate knew ink-on-brass must flip to cream on Editorial Paper, in three places. The coin didn't get the memo.**

**DECLARED DEVIATION, ratified at relay #4:** `calendar/page.tsx` has **no theme read at all**. The ruled cure could not be a ternary without seating a hook in a sealed screen, so it is a CSS role — `--role-today-coin-ink`, `#1A120E` dark / `#F5F2EE` light. Dark byte-unchanged. Derivation preserved in-comment at both homes.

---

## 4 · LIMB ③ — A RETIREMENT, NOT AN ADOPTION, AND IT REDDENED ITS OWN BENCH

O-1's landing rewrite hand-rolled R-X24's entire cure shape while `rowBaseline()`/`rowGlyphSlot()` sat caller-zero — **F-07.52's class live again**. L3 is the duplicate being retired onto its own primitive: 2 rows, 2 country buttons, `FlagSlot`.

**Running the bench my diff could redden took `tdw09_landing` from 98/98 to 95/98.** Three cells asserted the **local literal shape**, not the property. Cured by **labelled amendment** on ZIP-1's own F-09.93(ii) precedent, and **strengthened** the same way:

| cell | what changed |
|---|---|
| `§9.1` | re-aimed at `...rowBaseline(), borderBottom` |
| `§9.1b` **new** | the retired literal's **absence** asserted — a resurrection reddens |
| `§9.1c` **new** | the primitive is **imported**, not redefined locally |
| `§9.4` | re-aimed at `...rowGlyphSlot(20)` |
| `§9.4b` **new** | the hand-rolled slot literal's absence asserted |
| `§9.4c` **new** | `alignSelf:'center'` pinned **at its owner** — the declared delta, asserted not narrated |
| `§9.4d` **new** | `ROW_LINE_HEIGHT` **agrees with** the primitive's `1.5` — two constants, two files, CITATION-NEEDS-A-CELL |
| `§9.5` | title was made false by the adoption; R-O5's actual bar unchanged and still asserted |
| `§M.10` | its anchor **was** the retired block, so it became unapplicable the moment L3 landed. Re-aimed; still drives the row back to hand-rolled centre |

**The declared delta:** `rowGlyphSlot` carries `alignSelf:'center'`; O-1's copy did not. That clause is the reason a glyph slot exists, so it prevails **and it moves the flag**. The founder's **specimen ② baseline shot** is the acceptance. `flex:'none'` supersedes `flexShrink:0` (strictly stronger). `fontSize`/`lineHeight`/`translateY` are the **glyph's optics**, not the slot's geometry, and stay local by design.

**Label collision, UNRESOLVED and not silently fixed.** The charter rules the `SHOT ①` label cured to `②` citing F-09.98. The file already carries a `SHOT ②` at `:879`. Applied literally, that yields **two shots labelled ②**. Not applied. The renumber is the chair's.

---

## 5 · LIMB ② — THREE CHIPS, NOT TWO

`touchBox44(32)` on the clarify chip, `touchBox44(30)` on the M-3 report chip, **keyed** pressed state on both. Heights `32` and `30` are **UNMOVED** — the spread sits beside the height, never replacing it, and the primitive's compensating negative margin makes the visible chip byte-identical.

**Keyed, not boolean:** the clarify chips render from a `.map` and the report chip is per-message. One shared boolean would light every chip at once. One keyed press state per file, no new shared API — the same law ④'s hook implements, at a surface that needs only the state.

**F-09.103 — `ChatThread.tsx` holds THREE sub-44 interactive chips, not two.** The 3.0-C2 proactive-suggestions chips at `:241` are `<button>`, `height: 30`, routing through the **same `onChipTap`** as the clarify chips, and were on no roster. Filed, cure held pending ruling, **adopted at relay #5 on the founder's word.** Adopting two and declaring the floor held would have left the bench asserting a floor the file does not have — the F-09.94 species, one sitting later.

---

## 6 · F-09.104 — THE MINT BROKE AN INSTRUMENT'S PROXY

`scripts/tdw09_roles.proof.mjs:149` used *"imports from `theme.ts`"* as its proxy for *"consumes a theme."* Sound for its whole life. **`INK_DEEP` is the first deliberately theme-INVARIANT export that module has ever carried**, so adopting it made `AtelierForm.tsx` appear to **leave** F-09.79's accidentally-invariant set on a delivery that changed nothing about its invariance.

**Bisected against the clean baseline before claiming it: 131/131 at `2452eb5`, 130/131 under the diff. The red was mine.**

Cured by labelled amendment narrowing the proxy to what it always meant. **Proven not to have weakened it:** adding a real consumer alongside `INK_DEEP` still reddens the cell — GREEN cured → RED on a real consumer → GREEN restored.

---

## 7 · WHAT THE SUCCESSOR PICKS UP — ④⑤⑦⑧⑨

### ④ THE SANCTUARY HOOK — RULED SHAPE, RULED KEYS, RULED ACCEPTANCE

**The ratified ground reproduces exactly at tip:** the 20 suppression sites are **13 `<div>` / 6 `<button>` / 1 `<a>`**, across **exactly 9 owning components** — `SettingsRoom` 4 · `DiscoverRoom` 4 · `SanctuaryPage` 2 · `PagesRoom` 2 · `MuseRoom` 2 · `MeridianConciergeBtn` 2 · `CircleRoom` 2 · `PeopleRoom` 1 · `EventsRoom` 1. A keyed hook, not a polymorphic component.

**THE KEYING RULE (relay #5).** Ten sites are standalone and take static per-site keys. **Ten render inside `.map()` callbacks** and must compose the loop discriminator already on the element:

```
 872 key={mode}       1122 key={m.id}       2283 key={f.value}    2285 key={f.value}
2463 key={ev.id}      2840 key={p.id}       3157 key={entry.id}   3184 key={mood.key}
4464 key={slice.key}  (+1 to re-verify: 4480 may sit OUTSIDE its neighbouring map)
```

**A map site with a bare static press key is a defect by definition.** One press would light every sibling.

**THE ACCEPTANCE BAR IS AMENDED — the spread census is STRUCK.** `20 style spreads / 20 handler spreads` **greens on the broken build**. Replaced by: a structural cell asserting every map-site's press key references its loop discriminator, plus a **negative** cell convicting any map-site carrying a bare literal key. The bench proves the key composition; *the sibling doesn't light* is witnessed at the founder's C④ **FEEL** walk and named **PROVABLE-EQUIVALENT** on the card.

**④ RESUMES AS TWENTY HAND-VERIFIED EDITS. No scripted sweep touches `sanctuary/page.tsx`** — view each site, edit each site. See §8.

The hook's header **must not quote the roster needle in prose** (the predecessor's disclosure #2 — de-fang by construction). Import `pressedStyle` from `@/lib/vendor/controls` rather than hand-rolling it: the frost lane already crosses that boundary for `formatRs`, and hand-rolling a copy of an existing primitive is the exact F-07.52 class §4 just retired.

**F-09.95 corroborated independently:** sites 16 and 17 land at `:3608` and `:3615` — precisely the two the dream-os instrument is blind to behind the `accept="image/*"` string trap. A raw structural read sees them. The interim's reconciliation arithmetic holds.

### ⑤ THE THREE SINGLES · ⑦ `tdw09_p2c.proof.mjs` · ⑧ L5 CENSUS · ⑨ THE CLOSING LEDGER

⑦ must carry, per adoption **class**: ink · pressed-keyed · pressed-boolean · touch · baseline — plus the donor cell on `brass`, the fenced canon-home cell, F-09.100's arithmetic cell, and F-09.102's **both-arm** guard (a flattening of `--role-ink-on-metal` is a catastrophic dark-theme regression). ⑧ carries the **147-spanned / 143-fully-destroyed** labelled amendment against ZIP-1 §5's 145. ⑨ owes Package 2's whole story, the five chair defects, the predecessor's five disclosures, and the seven below.

---

## 8 · DISCLOSURES — SEVEN, OWNED BY NAME, NONE QUIETLY PATCHED

1. **My founder SQL used a bare literal** `u.phone = '9888294440'`. The column stores E.164. It returned `Success. No rows returned` — an **uninformative zero**, the exact silent-zero class I had convicted one screen earlier in F-09.101. Cured to `LIKE`, and split so a zero is diagnosable.
2. **I returned `users.id` under the header `user_id`.** Every link table keys on `couple_id → couples.id`, a different column. Pasting those values into a `couple_id` filter returns zero and reads as "no pairing."
3. **My pairing query carried a VACUOUS ARM.** `conversations_owner_xor` forbids `vendor_id` and `couple_id` both non-null; my arm joined on one and required the other. **It could never return a row.** Surfaced only because an independent counter sat beside it (2 vs 0). Shipped alone, I would have reported "no conversational pairing" as derived fact. It would have been false.
4. **Method:** I first attributed that CHECK to `waitlist_signups` by nearest-preceding header. Constraints are grouped in their own section. The schema doc's own §3 warns a constraint read without its predicate is misread; **positional attribution is the same class.** Settled by constraint name.
5. **My sweep's roster was self-blinding.** I cured four sites by hand, then built the bulk sweep's file list from a grep for the literal — so the two files I had already cured were skipped for imports and used `INK_DEEP` unbound. Three `TS2304`s. **A roster derived from the thing you are removing goes stale the moment you start removing it.** Caught by the tsc gate.
6. **I put the crash class back into a bench.** My first `§9.4c` called `read(path.join(ROOT, …))`; `read()` already prepends `ROOT`. The bench **threw ENOENT** — every cell after it unrun. That is F-09.93's refuse-never-crash class, strictly worse than a red, reintroduced one ZIP after it was cured in a sibling bench.
7. **THE SPECIES, NAMED.** Three scripted derivations failed this session, all one class — **a roster derived by text pattern rather than by structure**: (5) the stale file list; (6) the double-prepended path; and the element walker that broke on `=>` inside attributes, returning 14 of 20 sites as end-of-file. **Each was caught by an instrument. None by me.** This is the ground on which ④ was ruled to twenty hand-verified edits.

**And the near-miss that is the reason ④'s bar moved:** my first map-detection pattern found **three** map sites. The looser pattern found **ten**. Had I built on the first number, seven sites would have shipped sharing a press key — green on every count-based cell, broken in the hand.

---

## 9 · LIMB ⑩ — THE WALK ACCOUNT, DERIVED BY TWO AGREEING ROUTES

Estate: `users.id ec4232ae-…` · `+919888294440` · **Make up by Dev Roy Mint**.

**Walk account: `couple_id 9f1f84d5-e688-4d4f-9e44-9f5da6315e52` · login `+919625759924` · Dev Test 23 · Jaipur.**

- route A — `couple_enquiries.couple_id` → the estate's `vendor_id`
- route B — `conversations.counterparty_user_id` → `users` → `couples` (`couple_thread`, state `new`)

Counters beside the query: estate 1 · enquiries 1 · conversations 2. The second conversation is `vendor_self` — the founder's own thread, correctly excluded.

**Watch, unfiled:** the xor means any reader pairing a vendor thread to a couple via `conversations.couple_id` is dead by construction. No live reader derived, so no number spent.

---

## 10 · WHAT IS NOT IN THIS ZIP

`app/admin/**` · `settings/page.tsx` · the six demo organs · **dream-os: zero bytes** (F-09.95's instrument cure remains a STOP-and-fork). `sanctuary/page.tsx` is **untouched** — the failed walker wrote nothing; verified by `git diff --name-only`. Copy: **ZERO** vendor-facing or model-voiced bytes.

Findings **.101 · .102 · .103 · .104** ride this handover for durable banding at CE-207. **`.105–.110` remain.**
