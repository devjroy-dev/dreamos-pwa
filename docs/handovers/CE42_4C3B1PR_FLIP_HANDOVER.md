CE-42 · 4c-3b-1p-r · R7 G5.3 THE INFLUENCER EXCHANGE — THE FLIP (dreamos-pwa, base d644a3c6) · [CE-42 SEAT R7]

1 · SHIPPED. `EXCHANGE_PREVIEW = false`. The doors landed at dream-os `50781af` (4c-3b-1s, `b78` 22/22 with 21 both-ways mutations) and answered the contract the pwa half offered; the shapes were compared field by field before this line moved. Every act on the exchange is now live: send, withdraw, mark completed, accept, decline. The post tiles are DROPPED from the reach card (F-42.208) and their two CSS rungs went with them. `withdrawn: 'Withdrawn'` carries its veto date (sheet №6, 2026-09-10) where its AWAITING-VETO warning used to sit.

2 · THE CONSTANT STAYS, AND SO DO ITS BRANCHES. The flip is one line, not a deletion: the three preview branches and the fixture mappers are intact and unreachable. If a door misbehaves on the walk, the way back is `true` — one line, no re-cut. `b76` C3 pins that the branches survived, and would go red on a rider that had swept them out with the flag.

3 · WHAT THE REACH CARD SHOWS NOW. Audience by city · age · gender, engagement, and the verified state. Nothing else. R6's `/posts/cards` renders HER cards from HER wedding pages — a creator's recent Instagram posts are a different read that does not exist at any tip, so drawing tiles from a fixture past the flip would have been the room's only lie. The vetoed byte `Recent posts` is PARKED in `lib/worklist/exchange.ts` with F-42.208 named beside it, for 4c-3b-2 to land with the demographics reader.

4 · PROVEN / NOT. `b76` **15 GREEN, 0 RED**. C2, C3, C8 AMENDED BY LABEL — each said something that was right for a flag that was true, and each states its replacement at the cell. C15 new. **Four mutations against production source, each proven RED and reverted**: the flag left true · the flip deleting a preview branch · the sixth byte's veto record stripped · the tiles put back. `b74` 12/12, `b75` 11/11, `b76`-sunday 19/19. `tsc --noEmit` clean. Floor `b40`: C50 + C102, the base's own two; this cut adds none.

   ⚠ ONE HOLLOW GREEN CAUGHT BY THE BOTH-WAYS RUN. C8's first amendment asserted the sixth byte's veto with an alternation whose second arm (`VETOED 2026-09-10 (sheet`) matched bytes 1–5's marks — stripping the sixth's own record left the cell GREEN. It now reads the four lines directly above `states:`. That is the third cell in this arc to pass on a match it was never aiming at; the pattern is worth a chair's eye.

   NOT PROVEN: `npm run build`. Fails here on `next/font` — four `Failed to fetch … from Google Fonts` — and fails IDENTICALLY at the untouched base (4 either way). `fonts.googleapis.com` is not on this sandbox's allowlist. The PWA gate is owed on the founder's machine, as it was for 4c-3b-1p.

5 · ONE THING I DID NOT CHANGE, AND IT WANTS AN EYE. The Referrals & partners row still reads **「Influencer exchange · Coming」** — the hub's `preview` chip, pinned by `b40` C31 and `b76` C6. The room now takes real requests, so that chip is about to be false on a live surface. I have not touched it: it is a neighbour's assertion and the packet did not name it, and flipping it quietly would turn C6 red in someone else's bench. It is a one-word edit whenever the chair wants it — and R6's own flip at 4b-3b landed "zero chrome change (R-42.14)", which may well be the intended precedent here too.

6 · NEXT — THE WALK IS NOW REAL END TO END.
   (a) `0166` statement 4 (the three indexes) — the one SQL block still owed, ships with this rider.
   (b) Vercel green on `dreamos-pwa`, Railway green on `dream-os` (`50781af`).
   (c) The founder's second test vendor, category `content_creator`, on a clean number.
   (d) Her Settings → the exchange row appears (drawn only for a creator), OFF by default; turn it on, reload, it holds. DEV440's Settings shows no such row.
   (e) DEV440 → Referrals & partners → Influencer exchange: she is in the list, badge **Pending** (no snapshot exists until 4c-3b-2 — expected, and what the banner says).
   (f) Tap her → Send request → craft, 2 reels, dates → Send. Her seat opens on the INBOX, not the browse list; the row reads Sent with Accept · Decline.
   (g) Accept → DEV440's row reads Accepted with Mark completed → tap → Completed.
   Then 4c-3b-2, chartered as the R6/R7 pair: R6 writes the demographics reader in `igOAuth.js` (their file), this seat reads it into the snapshot and the verified badge.
