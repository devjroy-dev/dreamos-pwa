# CE-42 · SEAT SHELL-2 · R-42.17 — TYPOGRAPHIC HIERARCHY IN THE SUB-ROOMS, + R-42.16's LABEL RE-RULED (dreamos-pwa, base f239601d1cfcdc6c8b5f451f7bdf3979cdc49d26; sibling dream-os ef1abe069db1237ee92d075505b3a30b3ac214d8, full clone)

1 · SHIPPED. `/vendor/dates` and `/vendor/number` — the only two of the hub's eleven rows that are preview screens; the other nine are real rooms with their own chrome, untouched — gain the room eyebrow `.sol-kicker` (t5 uppercase .08em ink-dim, `CHIPS.coming`, C2 carried), one `h1.sol-title` at t1 reading `roomLabel(key)` — the seat's byte, seat unchanged (A1) — and `.sol-subhead` (`COPY.canHead`, T2 vetoed, one home in `lib/solutions/copy.ts`, built onto R-42.16's bytes); `.sol-can` margin 16px 0 0 → 0 (D). Three elements, not the four D names: D7 struck. `.sol-eyebrow` (B1), every control and every other byte unchanged; `SolutionsPieces.tsx`'s false "t1 owned by WorklistShell" line corrected (my F-42.212). FOLDED IN: ROOM_ROWS' eleventh label `Collabs & barter` → `Hire, collab & barter` (founder-ruled); key, position, href, count unchanged.
2 · MOCK. `docs/mocks/solutions-hierarchy-mock.html` re-cut FROM THE TREE, never as first drawn (c-42.52/.53): `scopeCss`/`typeCss` verbatim, WorklistShell's own header rules (seat = the room, Beta mark), SolutionsStyles verbatim plus one scoped before-panel line (b5b7dfc2's list margin), no D7, Chalk label `#3A3F42`; `b77` §6 holds it rule for rule.
3 · PROVEN at f239601d. New `b77` 51/51 · the same bench in a clean f239601d worktree 0/51 (every cell RED uncured) · `--mutate` 16/16 RED, control GREEN, tree restored. FIRED, THEN AMENDED BY NAME AT THEIR OWN SITES — other seats' cells: SHELL's `b73` §5 sheet count 11+2 → 12+3 and T2 in its shipped map (69/69 → 70/70; R-42.16's :227–232 edit untouched beneath it) · `bs_audit` C18 (R-42.17, reader set derived) and C24 → 41/41 by the register row · R7's `b42` C3 ordered join on the new label (fired: 177/179 unamended; amended: 178/179, count eleven, the red is C7's two `weddingPages.ts` bytes as at base). Did NOT fire, derived: `b69` pins count and Introductions' index only (71/71); `b73`:232 pins counts only. Unmoved: `b59` 20/20, `b76` 19/19 and 17/17, `b40` red lines byte-identical to base (C50, C102), `bs_audit` C36 as base, `tsc` 0, FLOOR = NAMED BASE, no delta.
4 · OPEN. F-42.216: §8's first cell cannot be met under D (all three roles 14px/400; lede and aside line share ink-soft) — `b77` §3 carries the PROPOSED reading and prints the literal measurement; unruled. F-42.217: `.sol-chip`/`.sol-statlabel` .06em and `.wl-beta` .12em sit outside theme.ts:65's reading. F-42.218: F-42.211 and F-42.212 are each minted twice — this seat's read-first by allocation, and R-42.16's handover (b42 C7 red; no pin on hub labels). F-42.219: the row now reads `Hire, collab & barter` and opens a room whose seat reads `Collab` (`lib/worklist/copy.ts:784`, `rooms.ts:181`). NOT PROVEN: `npm run build` (the founder's gate) and the walk.
5 · CARD (DEV440, deploy = pushed tip): Business Solutions → the eighth row reads **Hire, collab & barter**, still Open → back → **Open dates & rates**: small grey COMING, serif title, lede, small grey WHAT THIS WILL DO, three lines, **Suggest rates**, a rule, *Date checks already work on your page.*, **Storefront** — each visibly its own thing, seat still OPEN DATES & RATES · BETA → back → **Your own number**: the same, without the rule and the row. NEXT: R8/R9 replace these screens at the same addresses and retire the eyebrow with `PREVIEW_KEYS` in one edit.
