# CE-42 · SEAT D3 · `/plan` SITTING 2 · RIDER 2 · F-42.153 + FORK 2 AMENDED

**Cut at dreamos-pwa `09280bb60217f9bc2f28b0b4d3b329f29d735484`, derived fetch-first at the moment of writing. Three files, pwa only.**

1. **F-42.153, limb one — the date never becomes a `Date` again.** The first cut validated by ROUND-TRIP: `new Date(s + 'T00:00:00')` compared against `toISOString()`, which parses **local** and serialises **UTC**. East of Greenwich those are different days — on an Indian device `2027-02-14` came back `2027-02-13`, the equality failed, and **every dated link silently arrived with an empty date field**. This is `b70 §1.5`'s lesson facing the other way: that one renders the wrong day for a reader west of Greenwich, this one discards a real day for one east of it. **The cure is not a UTC parse.** A `Date` here has no work to do at all — the wire wants `YYYY-MM-DD`, the input wants `YYYY-MM-DD`, and the only question is whether three numbers name a real day. `isCalendarDay` answers it with arithmetic, and arithmetic has no zone.
2. **F-42.153, limb two, and it is the worse half — the cell was timezone-blind.** This container is UTC, where a local parse and a UTC serialisation agree, so a validator that discards every dated link on the founder's device read **GREEN** here. `TZ` is fixed when a process starts, so one process cannot be the witness: **§7 now transpiles the shipped slice and drives it in three child processes — `UTC`, `Asia/Kolkata` and `Pacific/Kiritimati` (UTC+14) — and requires byte-identical output.** The mutation restores the round-trip and reds on the zone disagreement, not on a spelling. **The defect was visible in the founder's first walk** — City filled, date blank, in the same screenshot — and this seat read past it.
3. **FORK 2 AMENDED at the walk: the tap, not the timer.** The 1500 ms beat shipped, and the founder — walking it deliberately, watching for it — could not report having seen the screen at all. That is the evidence, and it matters more here than anywhere on the estate: a stranger from a public link has no app, no account she knows about and no inbox, so **that screen is the only acknowledgement her request landed**. It stays until she taps the founder-vetoed `Continue`, which runs the identical push the timer used to, off the same recorded href. **No timer survives anywhere on the page**, and a cell says so rather than checking a number.
4. **The sheet's `sentAction` is OPTIONAL BY DESIGN and the bride lane passes none.** She is already inside the app and the sent state is where she stops, so a Continue there would be a button to nowhere; her sent screen is byte-unchanged, and a cell asserts the bride caller never names the prop. Two sealed cells **AMENDED BY LABEL (R-41.121), never re-pinned**: §9's order cell kept its guarantee — the filing lands after the session is written and before she is moved on — and lost only its last term, because the move left the function; it now also asserts `router.push` is **absent** from `onVerify` rather than merely later in it. §9's failed-file cell follows the same meaning to the confirmation it must never reach.
5. **Floor at the cut.** **`b20_a5` 121/121 GREEN**, and **GREEN under `TZ=Asia/Kolkata`** — the zone that found the defect. Still both-ways: **REFUSES exit 1** at `09280bb6` where the two files are absent. **Fourteen mutations RED** with a no-op control, two of them new. `b20_a3` 204/204, `b20_a4` 75/75, `b70`/`b71` FLOOR GREEN, `tsc --noEmit` clean. **INHERITED, NOT MINE:** `b40` and `b42` — red SET derived at base and at this cut and compared, not counted: identical. **Declared dirt:** `npm run build` cannot run here (`fonts.googleapis.com` outside the allowed domains; four `next/font` failures at base and at the cut alike, no file in this packet named) — NAMED BASE, no delta. **Witnessed on production at `09280bb6` before this cut:** the row landed `origin='public'` with a `couple_id`, city Jaipur — the sitting's first acceptance cell — and Fork C(a) is confirmed on the glass at `/frost/canvas/onboarding`. **Still unwalked:** the frost sheet opening already-sent in its own shell, and F-42.143's absence of horizontal scroll at 374.
