# repo: dreamos-pwa @ 7476b1a · TDW_14 · D-3d — THE DRESS MICRO

**Seat:** LE · **Founder-caught on his own screen, 2026-08-14, immediately after D-3c deployed.**
**Styling only. NO BYTE MOVES.** ① and ⑨ are frozen and unchanged; only their rendering does.
**pwa only. Zero DDL. W-1 shut.**

---

## 1 · TWO NOTES, AND THE TOKEN FILE ANSWERS BOTH

> 「 reduce the size of no polls yet and make ask the circle a little more prominent. But reduce the sign. its too big. the poll cant be the Hero of the circle page. the ask the circle has nothing to show its a cta and not just text 」

`lib/frost/tokens.ts` does not merely list sizes — **each rung declares its own purpose**, and read against them both notes are convictions rather than preferences.

## 2 · ⑨ — I OVERSHOT, AND BY A NAMED AMOUNT

D-3c moved ⑨ from `engravedSm` (9px mono — a footnote) to **Italianno 46**. That is `FT.greeting`, and the token file declares it **"the masthead greeting. ONE per screen."**

Two faults in one line:

1. **The masthead already spends that rung.** A second use is not a style choice, it is the language being broken.
2. **It made the empty state a HERO on a page whose subject is the circle**, not polls. The founder's sentence — *"the poll cant be the Hero of the circle page"* — is the rule, not a taste.

**⑨ now sits at `FT.body`**, declared *"ALL body prose. THE FLOOR."* — which is exactly what the activity feed's own empty **sentence** uses. The Italianno line above that sentence is **that surface's head, because the feed is that surface's subject.** I had copied the head and should have copied the sentence.

**The byte is unchanged and still one line.**

## 3 · ① — AN ACTION ON THE CAPTION RUNG

It shipped at `engravedSm` (9) with **no border and no padding** — the rung the language calls *"the one permitted sub-floor rung"*, which this estate spends on timestamps and captions. **Nothing about it said it could be pressed**, which is the founder's whole note.

**`FT.engraved` (11) is declared "JetBrains Mono · ACTIONS, PRIMARY LABELS."** ① is an action. It now sits on the rung named for actions, wearing **this file's own pressable chrome** — a hairline in the accent at `FI.chrome`, the identical shape every poll option and every sheet field already wears. **A CTA appears without inventing a vocabulary.**

## 4 · PROOF — 104/104, and the cell now has a CEILING

**§10.7 was re-aimed and given both bounds**, because this byte has now been wrong in **both directions inside one sitting** — too small to be seen, then too large to belong. It names `FT.body` rather than a number, so the rung moving moves the cell with it.

**§10.7b is new and specific:** ⑨ may not borrow a one-per-screen rung. §8.M15 dresses it back at 46 and it reds.

**§10.9 is new:** ① must carry the action rung *and* the chrome. §8.M16 strips the border and it reds.

**16 mutations** (14 + 2), all process-boundary, sha256-restored. **`FLOOR = NAMED BASE, no delta`**, sibling-full on a clean tree. `tsc` clean. **Control census unmoved at 161** — ① was already a button; only its dress changed.

## 5 · WHAT THIS MICRO DOES NOT TOUCH

**The create door's first live use was GREEN**, on the founder's own screen, before this micro: a poll asked from the sheet, rendered on both surfaces, both participants voting, `2 of 2`. That path is unchanged here.

**The #418 hydration errors are F-13.11's**, pre-existing, unrelated, and untouched.

**Sequencing beyond this micro is the founder's.**
