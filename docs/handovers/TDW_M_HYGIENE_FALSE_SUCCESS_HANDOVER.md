# M-HYGIENE · THE FALSE SUCCESS DIES — LE HANDOVER

**Base:** `dreamos-pwa` @ `28df2b0` on `main`, re-derived fetch-first at this seat
(`git rev-parse HEAD` → `28df2b04018ecbfeeeeeb965918a64466637108b`; `git status`
clean; tip's own subject is *M-LEADGATE-RECUT Seat B′ ZIP 2*).
**Relay guard: CLEAR** — no build for this scope exists beyond the tip.
**Ruling:** R-37.43 §8.3, on R-37.22's shape.
**Delta:** 3 files (1 MOD, 2 NEW). Zero SQL. Zero migrations. Zero production writes.

---

## THE DELTA

| File | State | What |
|---|---|---|
| `components/vendor/slices/SliceShell.tsx` | MOD | the expenses right-swipe suppressed; the incident written in-comment |
| `scripts/tdw37_hygiene_false_success.proof.mjs` | NEW | 20 cells, both-ways, six production mutations |
| `docs/handovers/TDW_M_HYGIENE_FALSE_SUCCESS_HANDOVER.md` | NEW | this file |

---

## THE CURE

`SliceShell.tsx:586` at the base tip:

    right: { label: 'Repeat', onTrigger: () => showToast('Repeat-last lands with the AddSheet rebuild (A4).', 'success') },

A control whose entire body was a `'success'` toast over zero writes — ungated,
every tier, on the shell twenty-two paying vendors use. §4's house law admits no
exception for small acts: the UI confirms only what a tool result or an API
response proved.

Cured to `right: undefined`, with the incident and both refused arms recorded in
place. **The suppression is mechanical, not cosmetic:** `SwipeRow` clamps
`next = 0` when a side is absent, so the row does not translate under the thumb,
no label reveals, and there is no handler to fire. `Delete` on the left is
byte-unmoved. **No new copy byte ships** — there is nothing to veto, because
there is no surface.

---

## PROOF

- **`tdw37_hygiene_false_success`: GREEN 20/20.**
- **Both-ways, six mutations on PRODUCTION source, every one restored and
  re-greened.** The ledger:

  | # | Mutation | Reds |
  |---|---|---|
  | M1 | the diseased block restored verbatim | 6 — incl. the count tripwire and the §4 canary |
  | M2 | substitution instead of suppression (`Repeat later`) | 2 — the refused arm has teeth |
  | M3 | `SwipeRow`'s right clamp neutered | 1 — *the row would still translate 140px under the thumb* |
  | M4 | a sibling verb moved (leads `Booked` → `Book`) | 1 — and the canary correctly stayed silent |
  | M5 | a NEW false success added to the swipe table | 1 — *the swipe table now carries 2 success sites* |
  | M6 | the cure comment stops recording the diseased byte | 1 — §1.3's absence cell declared vacuous |

  M3 initially reddened by **extraction refusal** rather than by behaviour. That
  is a weaker conviction than it looks, so the clamp regex was loosened to pin
  the line's *shape* and not its *contents*; it now reds on the value. Recorded
  rather than quietly fixed.

  M4 initially also reddened the §4 canary, because the canary shared a subject
  with a live cell. Re-pointed at `swipeSidesFor`'s own signature — a byte no
  other cell asserts on — so that when it fires it says one thing only: *the
  instrument is broken.*

- **Floor before:** 7 reds at untouched `28df2b0`, `FLOOR = NAMED BASE, no delta`.
  **Floor after:** identical — `FLOOR = NAMED BASE, no delta`, exit 0, the new
  bench joined the enumeration and is green. Derived local-commit → floor →
  unwind, never pushed.
  *Desk note, cost me a bounce:* the floor's own header warns that a `--depth 1`
  clone spuriously reds `tdw15_p2_envelopes` and `tdw15_p3_pulse`. Both clear on
  `git fetch --unshallow`. They are not base and were not entered as base.

- **Type proxy:** `npx tsc --noEmit` → **exit 0**. `next build` is unreachable in
  my container — `fonts.googleapis.com` is off my allowlist and the build dies on
  four `next/font` fetches in `app/layout.tsx`. I captured the full log: the only
  compile failures are those twenty font fetches; **zero type errors, zero module
  errors, `SliceShell.tsx` never named**. Per CE-225's law, **the chair holds the
  `next build` instrument.** Declared, not claimed.

---

## READ-FIRST RETURNS

**(i) Both bytes re-verified at the tip.** `SliceShell.tsx:586` verbatim as the
kickoff states it. `portfolio/page.tsx` — the `?igprobe=1` ladder confirmed gated
at `:349` (`igProbe` set only from the query param) and rendered at `:1207`.

**(ii) THE CENSUS — every other swipe verb on every slice.** One grep-and-read
pass over the complete swipe table: `SliceShell.tsx` (leads · invoices · expenses
· events) and `BinderCard.tsx` (clients). **Nine sides, plus the four cured.**

| Slice | Right | Left | Class |
|---|---|---|---|
| leads | `Booked` → `patchLeadState` | `Call` / `Mark lost` / **suppressed on redacted** | real write · R-37.22 |
| invoices | `Mark paid` → `recordPayment` | `Cancel` → confirm | real write |
| expenses | **suppressed (this cure)** | `Delete` → confirm | — |
| events | `Done` → `updateEvent` | `Cancel` → confirm | real write |
| clients | `Ask in chat` → `router.push` | `Call` / absent | real navigation |

**No sibling of the disease.** The expenses right side was the only swipe verb in
the estate reporting success over no write.

**ONE adjacent site, NAMED NOT CURED —** `SliceShell.tsx:577`:

    if (owed <= 0) { showToast('Already settled.', 'success'); return; }

A `'success'` register with zero writes behind it, reachable from the invoices
right swipe. It is **not** the same class: it reports *existing state* rather
than claiming an act occurred, and it early-returns before touching anything.
I did not cure it — that is a register question and a register question belongs
to the chair. **Both facts are pinned in §3 of the bench**, and the *count* is
pinned with them, so a genuinely new false-success announces itself instead of
blending into a crowd of one.

**One class reported, out of the shell boundary:** `app/demo/vendor/**` is
saturated with `'success'` toasts over no writes (*"Sign up to manage your
data"*, *"available in your full studio after signup"*). That is the demo twin —
a deliberate simulacrum, Block 08's, not the live vendor shell. Named so a later
sweep does not think it found something new; **not touched.**

**(iii) COPY INVENTORY — zero added, one removed.** No new vendor-facing byte
ships. The removed string was never founder-vetoed copy; it was a stub's excuse.

**(iv) ATTESTATION** — §7 chain, verbatim, and where each link stands:

> *ZIPs ONLY. Always.* — one ZIP, `deploy/`-prefixed, below.
> *The founder never copy-pastes code, never edits files, never fills a
> placeholder. Zero `<YOUR_X_HERE>` anywhere, ever.* — held; every block below is
> self-contained.
> *Never deliver unverified patches (§6 gates apply to the ZIP's contents before
> it ships).* — bench 20/20, floor no-delta, tsc 0, all on the delivered tree.
> *End every session with a handover: what shipped, what's proven, what drifted
> from the spec, what the next sitting picks up.* — this file.
> **`TDW_00_MASTERPLAN.md` status table and `docs/SCHEMA.md`** — untouched by
> ruling: this is a pwa hygiene cut, no schema moved and no block status changed.
> Declared rather than silently skipped.

---

## THE RIDER — REFUSED, AND THE GROUND IS NOT COST

The kickoff offers the `?igprobe=1` ladder as an optional in-passing strip **to
the one working arm**. I am leaving it, and the reason is not that it costs more
than minutes.

**There is no verdict to strip to.** The record at `c1d35cb` says of F-07.22:
*the four-shape probe ships dark behind `?igprobe=1`; **the walk DEFERRED
founder-accepted***. The measurement the ladder exists to take was never taken.
Picking "the working arm" would mean inventing the answer the instrument was
built to produce — the exact deviceless-physics move the file's own standing
header (`NO-DEVICE-IN-THE-LOOP`, earned by three inversions) forbids.

**And the seam is closed above it.** R-1 at pwa `2609cc8` carries a **standing
founder ruling**: *Safari is the supported web context until native ships (Block
11) — no further bytes on this seam*, taken on a live walk where a plain tap
completed consent in Safari end-to-end. A strip is bytes on that seam.

**The cost is also real, for the record:** `tdw07_p4b_probe.proof.mjs` is green
at **35/35** and binds the four arms across nine sites. Removing three arms moves
that bench — that is a RETIRE-WITH-THE-READER retirement with counts to disclose,
not a passing tidy.

**Recommendation, not a ruling:** the ladder's retirement is a small sitting of
its own, and its right predicate is R-1's ruling (*the seam is closed, so the
diagnostic is spent*) — not a walk result nobody has.

---

## LE DISCLOSURES

1. **I wrote `right: undefined` explicitly rather than omitting the key.** An
   omitted key reads as an oversight to the next seat; an explicit `undefined`
   reads as a ruling, and it is what R-37.22 wrote one gesture over. `tsconfig`
   has `strict: false` and no `exactOptionalPropertyTypes`, so the optional
   property takes it cleanly — checked before choosing, not after.
2. **The cure comment reproduces the diseased line verbatim.** That is
   deliberate — the incident should be legible where it happened — and it is
   exactly why the bench's §4 canary exists: an absence cell reading stripped
   source is worthless if the stripper stopped stripping.
3. **`Already settled.` (§(ii) above) is left standing on purpose.** I can cure
   it in a one-line follow-up if you rule the register — but I will not re-grade
   a live vendor-facing byte inside a sitting chartered on a different one.
4. **The verify block cannot run the full floor.** `tdw_f0774_vacuity_probe`
   writes to production source and restores it, so it exits 1 on any dirty tree
   and says so plainly. Between the apply and the git line your tree is dirty by
   definition. **The full floor runs in block 3, after the commit, on a clean
   tree** — the MICRO-WA-DIAL precedent, and where it is meaningful.
5. **My memory of this estate said there were no real vendors, only three test
   accounts.** The committed record disagrees — CE-225's tier census (basic 17 ·
   prestige 4 · signature 1) and CE-226's *seventeen real basic vendors*. I
   weighted this cut as live-estate work on the record's evidence, not on my
   recollection, and I am flagging the drift so it is not carried forward by
   anyone reading over my shoulder.

---

## FOUNDER WALK — one gesture

1. Open the vendor app. Go to **Expenses**.
2. **Swipe an expense row to the RIGHT.** The row does not move. No label
   appears. No toast fires. Nothing happens — that absence is the whole cell.
3. **Swipe the same row to the LEFT.** `DELETE` reveals in ember, and crossing
   the threshold opens the standing confirm. Back out of it.
4. Go to **Invoices**, swipe a row RIGHT. `MARK PAID` still reveals and still
   fires — the cut did not reach the slices beside it.
5. One screenshot of an expense row mid-right-swipe (it will look exactly like a
   row not being swiped; that *is* the screenshot).

What only your device can witness: that the row is genuinely inert under a real
thumb rather than merely handler-less in source. The bench proves the clamp
returns `0`; it cannot prove your glass agrees.
