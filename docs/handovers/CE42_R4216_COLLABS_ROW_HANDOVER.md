CE-42 · R-42.16 · R7 THE ELEVENTH ROW — COLLABS & BARTER (dreamos-pwa, base b5b7dfc2) · [CE-42 SEAT R7]

1 · SHIPPED. `ROOM_ROWS` gains `{ key: 'collabs', label: 'Collabs & barter' }`, IMMEDIATELY AFTER `referrals` — the two peer rows sit together — and `Introductions` STAYS LAST, which is R-42.8's ruling and is untouched. One vetoed byte. `ROOM_HREFS` gains `collabs: roomHref('collab')`. `PREVIEW_KEYS` is unchanged, so the chip reads **Open**. Eleven rows, ten KEPT, one NEW. No frame owed — the hub is unchanged to the eye but for one row in an existing list.

2 · THE ADDRESS IS DERIVED, NOT CHOSEN. `collab` IS a registry room: `lib/worklist/rooms.ts:181`, `{ id: 'collab', label: 'Collab', band: 'business', href: '/vendor/collab', pinnable: true }`. So its address comes from `roomHref` for exactly the reason `contracts` does (`support/page.tsx:78`, whose comment states the rule): `lib/solutions/routes.ts` is the home for rooms the registry does NOT own, and a `COLLAB_HREF` for one it does would be a second home for one address. NO CONSTANT WAS ADDED. The `Record<RoomKey, string>` stays total, so the new key was a `tsc` error until it had a destination — the guard that cannot be skipped by a caller who forgets.

3 · THREE OTHER SEATS' CELLS AMENDED BY NAME, NONE LOOSENED. Each carries its reason at its own site (R-41.121):
   · `b42` C3 — the PAIR, `labels.length === 10` and the exact ordered join, both moved to eleven with the label in position.
   · `b69`:263 — `ROOM_ROWS carries ten rows` → eleven, and `Introductions` LAST at index 10 rather than 9. What that cell guards is UNCHANGED, which is why it was amended rather than relaxed: R-42.8 ruled Introductions last, and it still is — now with one more row above it, not beside it.
   · `b73`:227 — two Coming / eight Open → two Coming / NINE Open, driven through the real row.
   That all three fired is the guarantee working: an eleventh row could not be added quietly, which is what those cells are for.

4 · PROVEN / NOT. `b42` **178/179** (see §5), `b69` **71/71**, `b73` **69/69**, `b74` 12/12, `b75` 11/11, `b76` 17/17, `b59` 20/20. Floor `b40`: C50 + C102, the base's own two; this cut adds none. `tsc --noEmit` clean. **Four mutations against production source, each proven RED and reverted**: the row dropped · the row placed last after Introductions · Introductions removed from last · the row shipped inside `PREVIEW_KEYS` so its chip would lie.
   NOT PROVEN: `npm run build` — four `Failed to fetch … from Google Fonts`, identical at the untouched base; `fonts.googleapis.com` is not on this sandbox's allowlist. Owed on the founder's machine.

5 · FILED.
   **F-42.211 — `b42` C7 has been RED AT BASE since R6's 4b-1/4b-2, and it is not this micro's.** The cell is `every room string appears verbatim in the ratified mock, bar the two ruled past it`; the two bounces are `The cards couldn't be made. Try again.` and `Make the reel` — Posts-room bytes absent from the ratified mock. Attributed by `git stash -u` at `b5b7dfc2` before this cut and named here so this micro's red is ATTRIBUTED AND NOT INHERITED SILENTLY. Uncured: the cure is R6's next Posts touch, and the mock re-emit that would clear it is not this micro's.
   **c-42.51 (the chair's, recorded at his word).** The packet ruled `Collabs & barter` excluded BY NAME from `b42` C7. That instruction rested on a false premise and was withdrawn: C7 reads `WPCOPY` — the wedding-pages room's copy home — not `lib/solutions/copy.ts`, so it never sees a hub label. It did not fire on this byte, and `Introductions` was never carved out of it either; R-42.8's exclusion-by-name lives in C3's comment, not C7's list. **NO CARVE-OUT WAS ADDED.** Adding one would have planted a permanently unreachable entry in a list whose own both-ways pin (`stale`, :485) exists to stop exactly that, and would have told the next reader this byte had been mock-checked and waived when nothing checks it at all.
   **F-42.212 — NO CELL PINS HUB ROW LABELS AGAINST A RATIFIED FRAME.** Surfaced by the above. The hub's eleven labels are founder-vetoed copy with no mock pin behind them; `b42` C7's discipline covers one room and stops there. Not chartered now — it wants `W5-hub` re-emitted first, and it belongs with whoever next does that.

6 · NEXT. Nothing owed by this seat. 4c-3b-2 remains unchartered, awaiting the walk and the R6/R7 pair.
