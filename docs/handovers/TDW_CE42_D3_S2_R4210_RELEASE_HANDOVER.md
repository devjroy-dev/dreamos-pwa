# CE-42 · SEAT D3 s2 · RELEASE OF E3's HELD PACKET · R-42.10 · THE `/plan` ENTRY

**Cut at dreamos-pwa `f538d98410cc339aaac1699023c34dce585e5582`, derived fetch-first at the moment of writing. Four files, pwa only. The code is seat E3's; this cut, and every byte of the release, is D3 s2's.**

1. **THE RE-PIN IS DERIVED, NOT ACCEPTED.** The chair's claim was that `app/(landing)/page.tsx` and `scripts/b20_a4` are blob-identical across `893c3136 → f538d984`; re-derived here by `git rev-parse <tip>:<path>` and both hold — `a166c847…` and `f17b26aa…` at each tip. E3's other two paths are absent at both. **The landing file is the one file in the estate where a botched apply locks out vendors and couples alike**, so its identity was checked by hash rather than by the chair's sentence — trust evidence over narrative, including a chair's.
2. **F-42.156 — A HELD PACKET'S BENCH NUMBER IS A CLAIM WITH A SHELF LIFE.** E3 derived rung `b70` by `ls` across both repos and it was free at that moment; two R8 packets took **70 and 71** while this one sat held. The rung is a property of the tree at the cut, not of the packet. Renumbered **`b70` → `b72`** by re-deriving `ls` at `f538d984`, and the bench's own prose and its result line follow the number rather than disagreeing with their filename. **Nothing else about the bench changed.**
3. **E3's RECORD IS PRESERVED AND SO IS ITS ERROR LEDGER.** Their handover ships with **every byte after the preamble untouched** — including §4's `e-1` and `e-2` and §5's floor, which is a record of *their* cut and not a claim about this one. Only the `HELD` preamble is replaced: a withheld block whose condition has been decided is no longer withheld but dead ink that reads as a plan, which is **F-42.55**'s own lesson, and this packet's condition arrived when `/plan` was walked green.
4. **WHAT THE PACKET IS, VERIFIED RATHER THAN RELAYED.** One anchor on the entry screen — *Not ready to sign up? Tell us what you need →* — at `/plan`, a real `href` and not a router push, because nothing on that screen holds a session and a plain link is what a crawler follows. **Parity by construction:** `ENTRY_LINE` / `ENTRY_LINE_VERB` are hoisted once and named by both lines, so the two can differ only in their words; parity asserted by a cell can drift for a day, parity by a shared identifier cannot drift at all. **The census moves on ANCHORS alone** — derived here independently of the bench, by counting the raw file: `button 17/17 · input 4 · a 3 · BackBtn 4 · GoldBtn 3 = 31`. `b20_a4` is **AMENDED BY LABEL 30 → 31, never loosened**, with a second cell pinning the delta to anchors; method A is untouched **by construction**, because a link is not a button — which is exactly why the ruling said text link and not third door.
5. **Floor at THIS cut, re-derived and never carried.** **`b72` 26/26 GREEN, `--mutate` 42/42** (eight mutations, tree restored byte-for-byte) · **`b20_a4` 76/76** (was 75/75 at base; the delta is E3's two cells) · `b20_a5` **121/121**, and **121/121 under `TZ=Asia/Kolkata`** · `b20_a3` 204/204 · `b70`/`b71` FLOOR GREEN · `tsc --noEmit` clean. **DECLARED, INHERITED, NOT CURED:** `tdw09_landing` 99/100 (§2.3), `b40` (C50/C102 red, C80/C81/C84 refused for the absent sibling) and `b42` C7 — all three **SET-compared** at `f538d984` and at this cut, not counted: **identical**, so nothing in this packet moved them. **Declared dirt:** `npm run build` cannot run in this container (`fonts.googleapis.com` outside the allowed domains; four `next/font` failures at base and at the cut alike, no file in this packet named) — NAMED BASE, no delta. **The build gate and the live witness are the founder's.** `.157`–`.160` unspent.
