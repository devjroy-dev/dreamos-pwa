CE-42 · F-42.205 MICRO · the per-role note, shoots only (dreamos-pwa, base 92416015c13be560d13b64e432f9bbeb80ea7b28 — re-pinned from 6d6823a8 after R6 4b-2, no shared path) · [CE-42 SEAT R7]

1 · SHIPPED. `components/vendor/CollabPostForm.tsx`: the per-role note input renders only when `kind === 'shoot'` (ruled (a)), with the vetoed placeholder `Who you need — e.g. Model, 22–30` held in one constant; its accessible name stays the craft's label. `scripts/b75_4c1_shoot_board_pwa_bench.js` gains C11.

2 · ON THE GLASS. Collab's `+ Post`: a chosen craft shows its label alone — the box the founder asked about ("the box beside Designer is to write the purpose?") is gone there, because no Collab surface ever read what it wrote (F-40.109's class; the only reader is ShootsBlock). `Post a shoot`: the box stays and now says what it is for.

3 · PROVEN / NOT. b75 11 GREEN; C11 RED with the guard removed from production source, GREEN restored. `tsc --noEmit` clean. Floor: see the relay. NOT proven: `npm run build` (the founder's gate), the device.

4 · OPEN. None new. F-42.190 (the first-look line's fixed 12 hours) stands, as does F-42.189.

5 · NEXT. The 4c-1 walk continues on the card as handed; this micro changes Step 2 only by adding the placeholder to the box Swati types `Lead shooter` into.
