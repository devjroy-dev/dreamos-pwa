CE-42 · 4c-3b-1p · R7 G5.3 THE INFLUENCER EXCHANGE — THE INBOX AND THE WIRING (dreamos-pwa, base a9b5e0cd) · [CE-42 SEAT R7]

1 · SHIPPED. The creator's seat and one flag. `EXCHANGE_PREVIEW` (`lib/worklist/exchange.ts`) is the only branch between the fixture and the doors, on `SUNDAY_PREVIEW`'s idiom: true → every row is a mock, no door is called, every act toasts `COPY.launchingSoon`; false → `lib/vendor/api/exchange.ts` answers and the acts are real. SHIPPED FLAG-ON. `ExchangeRoom` is the role gate — one read (fixture, or `GET /exchange` live) decides which glass mounts, and NOTHING draws until it has answered. `InboxScreen` is Y1/Y2: Accept · Decline on `sent` only, the sender's note rendered, the row settling on the door's own echo. The opt-in row (Y3) sits on Settings, transcribed property-for-property from `PeerDiscoverySwitch`, drawn ONLY for `category === 'content_creator'`, defaulting CLOSED. `useSettings` gains `category` + `exchange_discoverable` from its ONE `/me` read (no second fetch). The sender's screen keeps every 4c-3a control and now sources them through the flag.

2 · THE DOOR SHAPES ARE A CONTRACT OFFERED, NOT A READ. dream-os has no exchange doors at `c5de470`; the pwa lands first by the chair's split. So `lib/vendor/api/exchange.ts` states the paths and payloads 4c-3b-1s must implement — or the rider that flips the flag corrects this file in the same cut. What must not happen is a flip against a shape nobody compared. Ten paths, four transitions posted as VERBS with empty bodies (the room can never name a state on the wire).

3 · PROVEN / NOT. `b76` **14 GREEN, 0 RED**. C2, C3, C7 AMENDED BY LABEL — the shell's "no network" and `onClick={soon}` assertions are false by charter now, not by drift, so the cells keep their numbers and assert the thing that replaced them. C4 extended by file list. C8–C14 new. **Nine mutations, each against production source, each proven RED and reverted** (guard deleted · act toasts then falls through · unvetoed byte on a fixture row · preview param overriding the door · creator handed the browse list · a posted body naming a state · the category test dropped · the glass reading a fixture-only field · the follower count entering the sort). `b75` 11/11, `b76`-sunday 18/18. `tsc --noEmit` clean. Floor `b40`: C50 + C102 red, byte-identical to the base's own two — this cut adds none.

   ⚠ ONE HOLLOW GREEN CAUGHT AND FIXED, RECORDED BECAUSE IT NEARLY SHIPPED. C2's first cut scanned for `/EXCHANGE_PREVIEW/` within 20 lines above a call and passed on the `useState(EXCHANGE_PREVIEW ? … )` seed four lines up — it stayed GREEN with the real guard deleted. The cell now requires the `if (EXCHANGE_PREVIEW)` statement form. The both-ways run is what found it; a cell that is never made to go red is a cell nobody has read.

   NOT PROVEN: `npm run build`. It fails in this workspace on `next/font` — four `Failed to fetch … from Google Fonts` errors — and fails **identically at the untouched base** (proven by `git stash -u` + rebuild: same 4). `fonts.googleapis.com` is not on this sandbox's network allowlist. The PWA gate is therefore OWED on a machine with network. NOT PROVEN: any device.

4 · OPEN.
   (a) **THE SIXTH BYTE IS NOT VETOED.** The shell drew four states; 0166 has five, because Withdraw now has somewhere to land. `withdrawn: 'Withdrawn'` is authored, marked AWAITING VETO in source, and CANNOT reach glass while the flag is true (no fixture row carries it — C8 asserts that). **4c-3b-1s must not flip the flag until the founder has vetoed or replaced it.**
   (b) **The reach card's post tiles have no door.** They are fixture-only; the section is omitted when a row carries none. 4c-3b-1s either serves them (R6's `/posts/cards`, 4b-1) or the tiles are dropped — a ruling, not a default.
   (c) The PATCH echo of `exchange_discoverable` is optional in the types until 1s ships it; the row keeps the tapped value on `undefined` rather than reading absence as NO.
   (d) `F-42.206` (workspace integrity, dream-os) STANDS UNANSWERED and is not this half's to close. 4c-3b-1s builds on that tree.

5 · NEXT. Founder's look at `/vendor/exchange` (sender) and `/vendor/exchange?as=creator` (the inbox) after deploy; Settings shows the opt-in row only on a `content_creator` account. Then 4c-3b-1s: `0166` is already run and green, so it needs `exchange.js`, the doors, and the rider that flips the flag — with (a) answered first.
