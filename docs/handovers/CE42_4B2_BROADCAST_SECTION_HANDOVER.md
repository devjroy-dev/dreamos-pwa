# CE-42 · 4b-2 · R6 · THE BROADCAST SECTION + THE PAISE ARM (dreamos-pwa) — HANDOVER · seat R6
1. **Shipped:** the Broadcast section of `app/vendor/(shell)/posts/page.tsx` on the vetoed frames — `{n} couples`, the list (her book's name, else the last four), both bodies filled by the door from the registry, the button label and page URL off the WIRE (no second spelling), the fee as `formatRs(fee_paise / 100)` with a no-break space so the figure never splits, the confirm sheet, `Sent to {n}. {m} not delivered.`, the referral's next date in full month; a gate that is off shows `Not switched on yet.` IN PLACE of Send; every refusal is a code mapped to a vetoed byte (never the door's text, F-42.193). Copy home `lib/worklist/posts.ts` (vetoed bytes + wire types); `API.postBroadcast` in `lib/solutions/routes.ts`. **F-42.170:** `formatRs` gains the paise arm in `lib/vendor/format.ts` — a figure with paise prints exactly two digits ("Rs 6.10", "Rs 1.02"); a whole-rupee figure goes down today's line, byte-unchanged. Bench `b74` 12/12 (C3 scoped to STATUS_FOR, C8 re-read, C9–C12 new; C12 drives the real `format.ts`, RED at the uncured formatter).
2. **On the glass:** Posts & ads → Broadcast lists DEV440's past couples with the fee; Send appears only when `template.tdw_couple_broadcast` is ON (the founder's Switchboard tap, after this lands); the referral card reads its gate the same way.
3. **Proven / not proven:** `tsc --noEmit` exit 0. The 16 pwa benches that read a touched path exit identically at untouched `6d6823a8` and here, except `b74` (red at the tip — 4b-1's C3 read the door file before 4b-2's second status map; green here). Every pwa bench that reads `formatRs` exits identically with and without the arm. NOT proven: the walk — a broadcast to the test couple, the receipt, the fee on her glass; the pwa floor and `npm run build` are the founder's gates.
4. **Collision derivation (rebuilt onto `6d6823a8`):** of this packet's paths only `lib/solutions/routes.ts` moved since 4b-1 (`424c1fbf50` → `76aa6f14ca`: SHELL's seventh/eighth constants, R7's `COLLAB_API_PATH` + the collab doors). The one conflict — `API`'s tail — is rebuilt with R7's collab entries FIRST and `postBroadcast` after; nothing of theirs removed. `posts/page.tsx`, `format.ts`, `worklist/posts.ts`, `b74` are the same blobs at `85f6f3c1` and `6d6823a8`. `support/page.tsx` and `b40` moved (SHELL's total Record + PREVIEW_KEYS, R7's b40 amendments) but this packet does not touch them; `posts: POSTS_HREF` stands in SHELL's Record.
5. **Base:** dreamos-pwa `6d6823a8fa6785e968fa333384c43cadfab3d59e`; sibling dream-os `c5de470` (4b-2's arm). Next: the founder walks a broadcast to the test couple, then the gates; 4b-3 the Sunday brief (dark).
