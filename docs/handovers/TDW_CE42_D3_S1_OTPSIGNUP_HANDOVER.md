# CE-42 · SEAT D3 · `/plan` SITTING 1 · FORK B · `lib/auth/otpSignup.ts`

**Cut at dreamos-pwa `d22bdf2cd00a833d54fd3e01f30b5d742edef65f`, derived fetch-first at the moment of writing. pwa only; dream-os is zero-byte this sitting.**

1. `sendOtp` and `verifyOtp` left `app/(landing)/page.tsx` for `lib/auth/otpSignup.ts`, **both roles**, with `safeSetItem`, `mirrorSessionToCookie` and `SESSION_COOKIE_MAX_AGE` — the third traveller, named here because §4.1 of D2's note named only two. Shape is `useOtpSignup({...})` returning the pair (B-2, arm (b)); no React hook is called inside it, so the two closures keep exactly the per-render lifecycle they had as component consts.
2. **B-3, folded:** the three-line session write that stood identically in `verifyOtp` and in `handleSignIn` is now one exported `persistSession(isVendor, session)`. `handleSignIn` stays on the page and calls it — the one line this extraction writes into a function it does not move.
3. **F-42.61 CLOSED.** The moved comment names its callers by role — the join door's Send code, the two sign-in paths, Resend — and carries no line cite of any shape (B-4). Its four old cites had all rotted, and a cross-file line number could not have stayed true for one edit.
4. **The glass did not move.** `base:63..466` and `base:643..EOF` are byte-identical to the cut; the six diff hunks are confined to the imports, the helper block, the two functions and the fold. The census is **30, all KEPT** — 17 `<button>` opens / 17 closes, plus 4 `input`, 2 `a`, 4 `BackBtn`, 3 `GoldBtn`, two methods whose failure modes differ (e-8 not inherited).
5. **Floor at the cut:** `b20_a4` **75/75**, `b20_a3` **203/203**, `b20_a2` **193/193**, `tsc --noEmit` clean. `b20_a4` is both-ways: it **refuses (exit 1)** at the uncured tree, and with the new file present but the page un-rewired **twenty page-side cells red**. §8 mutates production source five ways with a no-op control — and mutates **comment-stripped** source, because this bench's first cut hit `role === 'Maker'` in the module header comment instead of in `sendOtp` and reported a mutable subject as unmutatable. Three cookie cells read `(cookies[0] || '')` rather than indexing bare: at the red rehearsal they **threw** instead of failing, and a stack trace is a worse thing to hand back than a named FAIL.

**Findings filed from the assigned range:** **F-42.100** — `GhostBtn` is defined in the landing page and instantiated zero times; KEPT under the extraction, uncured, for Block 09. **F-42.101** — `tsconfig.json` carries `"paths"` twice, `:3` and `:4`; the values agree today so nothing is wrong yet, and the day they disagree the second wins silently. Uncured.

**Declared dirt:** `npm run build` cannot be run in the seat container — `fonts.googleapis.com` is outside its allowed domains and `app/layout.tsx`'s four `next/font` fetches fail. Run at **base** it fails with the identical four errors and exit 1, so this is **NAMED BASE, no delta**, R-38.34's shape. Neither file in this packet is named anywhere in the failure. The build gate rides the founder's verify block.
