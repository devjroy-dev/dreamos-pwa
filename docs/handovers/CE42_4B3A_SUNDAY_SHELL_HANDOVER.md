# CE-42 · 4b-3a · R6 · THE SUNDAY BRIEF'S SHELL (dreamos-pwa) — HANDOVER · seat R6
1. **Shipped (R-42.14 shell-first):** `SundaySection` on `app/vendor/(shell)/posts/page.tsx` — the FINISHED chrome for all eleven ruled states (pending · connect · under100 · empty · live · arrows · share · error · stale · expired · notconnected), drawn from a brief; `lib/worklist/sunday.ts` — `SUNDAY_PREVIEW` (one flag, one home, one branch), `FIXTURE_BRIEF` in the accepted stored shape, the vetoed bytes, `weekLine` (full month, R-42.13), `arrow` (↑/↓/—; prev null → none), `bestPostLine`. Under PREVIEW the live glass shows **S5 live** with the fixture beneath the vetoed eyebrow `Preview · sample numbers` (caution ink); every CTA — Connect Instagram, Share this, Check again, the best-post row, Share, Download — toasts `COPY.launchingSoon`; no door is called. The eleven frames are banked in `docs/mocks/sunday-brief-mock.html` + 24 PNGs and are the spec.
2. **Control inventory (4b-3b changes zero chrome):** Connect Instagram (S2/S10/S11) · Share this (S5/S6) · Check again (S8/S9) · the best-post row · Share / Download on the share card (S7). All NEW.
3. **Proven / not proven:** `b76` 18/18 — one flag, no door, every CTA one byte, the bytes verbatim, and §2 RENDERS the real section (react-dom/server over the transpiled page) once per state asserting each state's bytes appear and no other state's leak; 9 production mutations each red by name. `b74` 12/12, tsc exit 0. NOT proven: the walk (the toast on each tap, S5 on DEV440's glass).
4. **4b-3b picks up:** flip `SUNDAY_PREVIEW` in the edit that wires the door; the door answers `{ state, brief }` in exactly these shapes; the share card renders through the cards arm; nothing on the glass moves except the data.
**Cut 2:** the founder's floor on cut 1 read one delta — `b59_seven_ink_census` on two hex literals in the S7 share-card stand-in (`#EDEEEF`, `#C9A84C`). Replaced with `--atelier-ink` / `--role-metal` (the stand-in now follows the arm; 4b-3b's real card is Cloudinary's Graphite pixels). Cut 1 retired unapplied at origin.
**Cut 3:** the founder's `npm run build` on cut 2 refused `SundaySection` as a named export on `page.tsx` (a Next.js page module may export only the page; `tsc --noEmit` does not run that check — the seat's sweep missed what the founder's gate caught, R-40.66). The section moved whole to `components/worklist/SundaySection.tsx`; `b74` C8 reads it there. Cut 2 retired.
5. **Base:** dreamos-pwa `34272f28d4a823ffeac0b7d437237882634c81cc`.
