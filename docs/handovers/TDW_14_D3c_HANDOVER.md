# repo: dreamos-pwa @ 8fbcddc · TDW_14 · D-3c — FOUR CURES, ONE DELIVERY

**Seat:** LE · **Rulings:** CE-33 D-3c scope · the create sheet ratified whole 2026-08-14 (「 all stand 」) · create is **bride-only** by founder word.
**Tip at build, fetch-first:** dreamos-pwa `8fbcddc`. **pwa only — dream-os untouched. Zero DDL. W-1 shut.**

---

## 1 · WHAT SHIPPED

| Path | State |
|---|---|
| `lib/circle/pollCopy.ts` | the nine create bytes join the one home — **twelve exports** |
| `components/frost/blooms/circle.tsx` | the create sheet · ① becomes the affordance · F-14.8 defused · F-14.9 rendered · ⑨ dressed |
| `app/coplanner/threads/page.tsx` | F-14.8 defused **here too** · F-14.9 rendered |
| `scripts/tdw14_d3b_polls.proof.mjs` | **82 → 98 cells**, 14 mutations |
| `scripts/tdw09_frost_parity.proof.mjs` | the control census moved **by charter** |

---

## 2 · THE SHEET COST ONE LINE LESS THAN IT LOOKED

**① `Ask the circle` was vetoed as *"the affordance that opens a poll."*** D-3b rendered it as a dead eyebrow because no affordance existed. D-3c makes it the button. **The byte never moved; it finally does the job it was approved for** — which is why the sheet needed no line for its opening control, and why `POLL_SHEET_HEAD = POLL_ASK` rather than a second name for one act.

**A gate I had to remove to make it work.** D-3b gated the whole block on `polls.length>0` — right while it was read-only. With ① tappable that gate would have **hidden the only way to ask a question at exactly the moment there are none**: ⑨ saying *"No polls yet."* beside no means of fixing it. The affordance now renders whenever the bloom has loaded, and ⑨ sits beneath it, so the sentence and the remedy are on screen together. **§9.5 reds if the gate returns.**

**⑫ binds the whole sheet: labels above, never placeholders inside.** The invite panel's `e.g. Mom, Priya, Anjali` is a different, separately-vetoed surface and is **not** precedent. §9.9 asserts it over the sheet's own slice.

**Three ratified expected-zeros, each asserted rather than merely honoured:** at four choices the control greys and says nothing (② already speaks the bound at the server; repeating it explains a wall she has *just hit* instead of one she is approaching) · submit is **gated, never refused with a client byte** · no helper text.

---

## 3 · F-14.8 LIVED ON BOTH SURFACES AND I DEFUSED ONE

The finding was raised against the bloom. **It was in the strip too**, and my first cut fixed only the bloom. **§10.1 caught the half-cure** — the cell asserts the absence of `toLocaleString(undefined` on *both* surfaces, and reddened.

Both now format `en-GB` with an explicit `Asia/Kolkata`: the same string on every machine. The estate is one wedding business in one time zone, and **a close time shown to the bride and to a member must be the same close time**. §8.M9 and §8.M10 restore the runtime locale to each surface separately, so a future half-cure reds the same way.

---

## 4 · F-14.9 — THE SCREEN LINE (b) DESCRIBED

D-3a served `linked_event`; D-3b rendered it nowhere. **The founder's ratified sheet line (b) — *"a member without the flag sees the event's name and date but not its vendor"* — was payload-true and screen-false**, and the walk is what caught it.

Both surfaces now render the event's **own name and date, label-free** — ⑫-class, her data, **no connective word of ours**, which is why this needed no byte on the sheet. **§8.M11 inserts one (`title for`) and §10.4 reds**, because a connective word is new copy and owes the veto.

**Neither surface reads `vendor_id`.** The gate stays the **server's**: a flagless member's payload omits the key entirely, so the withholding is **payload-level truth, not a CSS opinion** — the 08 blur standard. §10.5 reds if a surface starts reading it. ⑪ holds: nothing announces anything is missing.

---

## 5 · ⑨ — THE BYTE IS FROZEN, THE DRESS WAS THE DEFECT

It rendered at **JetBrains Mono 9px uppercase inkMute** — the treatment this estate reserves for timestamps and `loading…` — while the activity feed's own empty state, in the same file, speaks at **Italianno 46 in the accent**. **I dressed a statement as a footnote.**

Same byte, same character as its sibling. **It stays ONE line** — the sibling's second, explanatory line would be a **new byte** and does not ride a styling fix (§10.8).

---

## 6 · PROOF

**98/98** cured · **20 cells RED** at the pre-D-3c tree · **14 process-boundary mutations**, all restores sha256-verified.
**`FLOOR = NAMED BASE, no delta`**, sibling-full on a clean tree. `tsc --noEmit` clean.

**The control census moved by charter, 153 → 161**, counted from the source diff rather than estimated: **+4 buttons** (①-as-button, Add a choice, Ask, Cancel) · **+3 inputs** (question, choice 1, choice 2 — slots 3 and 4 are the *same* `<input>` rendered from `askOpts.map`, so a source census counts controls **declared**, not painted) · **+1 tapdiv** (the sheet's scrim, disabled mid-write so a mis-tap cannot orphan a poll). Anchor, textarea, select unmoved.

---

## 7 · MY CELLS OVER-REACHED, THREE TIMES IN ONE SITTING

**§9.8 and §9.9** scanned the whole bloom and convicted the **invite panel's helper line** and the **composer's placeholder** — both pre-existing, both separately vetoed, neither anything to do with polls. **§10.6** matched `overflow:'hidden'` — CSS, not a sentence anyone is shown. **§10.8** counted the import line as a render site.

**Same mechanism every time: an absence cell whose radius exceeds its claim.** It does not prove more; it convicts things its ruling never covered, and a bench that cries wolf gets muted. Each is now bounded to where its claim actually lives.

**Two more, caught by cells I had just written:**

- **§9.8a, the slice canary, earned its place on its first run.** I bounded the sheet's slice on `Activity feed` — which lives **inside a JSX comment** and is stripped before the search, so the slice came back empty and every bounded absence cell would have **passed vacuously over nothing**. An empty string contains no placeholder either.
- **§10.7 checked the font *family* when the claim is about *weight*.** §8.M14 shrinks 46px to 9px and recolours to inkMute while leaving `Italianno` in place, and sailed straight through. **A cell that checks the typeface has not checked the presence.**

---

## 8 · WHAT THE FOUNDER DOES

**Apply · verify · git.** No SQL, no dashboard acts, no env var, no file to open.

**Then the one thing only a handset closes:** open the bloom with **no polls** — ⑨ should read at real weight with **`ASK THE CIRCLE` tappable above it**. Tap it, ask a question with two choices, and watch it appear on both surfaces. **That is the create door's first live use**; every poll in the walk came from SQL.

## 9 · WHAT ROW 14 STILL OWES

- **No delete and no edit on a poll.** Once asked it stands until it closes. D-3a built no such door, the founder was offered one, and taking a question back is a **separate charter, not a byte**.
- **The vendor gate is still bench-only.** No event in production carries a `vendor_id`, so §5.1–§5.3 remain unwalked. Named at the walk, unchanged here.
- **D-4 delegation**, held on the `events.assigned_member_ids` ownership derivation.

**Sequencing beyond this delivery is the founder's.**
