# CE-42 · SEAT D3 · `/plan` SITTING 2 · RIDER · F-42.143 / F-42.144

**Cut at dreamos-pwa `13a4c28160b17227aa3ce1dced89578bb7054ad2`, derived fetch-first at the moment of writing. Three files, pwa only.**

1. **F-42.143 — the black screen was a sideways document, and the cure is one property.** `flex: 1` does not shrink a flex item below its **intrinsic** width, because `min-width` defaults to `auto`; an `<input>` with no `size` is about 170px, so six code boxes demanded roughly a thousand pixels inside a 374 column, the document grew wider than the viewport, and one swipe slid the cream page off the screen. No crash, no navigation — the address bar never left `/plan`. **The floor is BLANKET, on every input on the page and not only the two that need it today:** a rule conditional on the parent being a flex row has to be re-derived every time a field moves, and the field that moves is the one nobody re-checks. My error was checking the row against the ratified frame, where the boxes are `<span>`s — spans have no intrinsic width and inputs do.
2. **F-42.144 — and this one would have shipped silently.** Both `/plan` branches returned the same `<AssistanceSheet>` element at the same position, so React **reused the instance** across the flip to `sent` and every `useState` initialiser — including the two that read `initialSent` — was skipped. She would have watched the empty form redraw where the confirmation belongs, and nothing would have thrown. `key={screen}` is the cure; the prop's contract is now named at the component, where the next caller reads it. **The bench had a cell and it was the wrong cell:** §9 asserted `initialSent` is *passed*, which is presence and not behaviour — D-38.1's own doctrine, in a cell I wrote.
3. **The chair's question, answered by driving rather than by reading — §15, and the shape is IDENTICAL.** Both doors reach the same two functions in the same file, so the bodies can differ only through the deps; the bench now drives the real `otpSignup` module twice, once with `/plan`'s values (digits only) and once with the landing's (spaces as typed), and asserts **every URL and every body byte-identical**, plus that the two calls hand the mint the **same eleven dep keys**. `verify-otp` goes to the couple door with `+919625759924`, `417289`, `purpose: login`. **F-42.145 is not ours** — the 500 is `users_auth_user_id_key` at the identity link, dream-os, filed to J1-IN.
4. **What §13 IS, stated rather than implied (CE-115, provable-equivalent).** The chair asked for a cell measuring the document width at 374. **This container has no layout engine** — Chrome cannot be fetched here, the puppeteer download host being outside the allowed domains — so nothing in this bench measures a rendered page. It proves the **mechanism** end-to-end and the founder's card names the absence of horizontal scroll as a truth only his device witnesses. Two instrument defects were found and owned in the writing, both by mutation rather than by review: §13's first cut walked back to the nearest `<div style={{` and landed on the `+91` box instead of the row holding it, so the phone field passed **vacuously**; §15's key extractor read the value side, so `joinName: name` contributed `name` on one call and nothing on the other and would have reported two identical dep sets as different.
5. **Floor at the cut.** **`b20_a5` 107/107 GREEN** (was 90/90), still both-ways — **REFUSES exit 1** at `13a4c281` where the two new files are absent; **twelve mutations RED** with a no-op control, three of them new. `b20_a3` 204/204, `b20_a4` 75/75, `b70`/`b71` FLOOR GREEN, `tsc --noEmit` clean. **INHERITED, NOT MINE:** `b40` and `b42` — red SET derived at base and at this cut and **compared, not counted**: identical. **Declared dirt:** `npm run build` cannot run here (`fonts.googleapis.com` outside the allowed domains, four `next/font` failures at base and at the cut alike, no file in this packet named) — NAMED BASE, no delta; it was **green on the founder's gate at `13a4c281`** and `/plan` printed `○ (Static)`, which is Fork 3's ruling witnessed rather than argued. **Fork C(a) remains untested** — verify never succeeded, so the pin screen was never reached. **`.145` is spent to dream-os; this seat's range is exhausted.**
