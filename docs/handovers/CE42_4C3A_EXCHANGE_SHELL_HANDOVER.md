CE-42 · 4c-3a · R7 G5.3 THE INFLUENCER EXCHANGE — THE SHELL (dreamos-pwa, base 948176dcc440cd074ece8f02fa39fbbb364693ce) · [CE-42 SEAT R7]

1 · SHIPPED. `app/vendor/(shell)/exchange/page.tsx` — the exchange's whole glass behind PREVIEW (R-42.14): the static pending banner (S1), the browse list sorted by audience-city fit then engagement with the follower count as a fact never a key (S2(b)), the reach card by city · age · gender with fixture tiles and no follower identities (S3), the typed offer sheet — the eleven's labels + note, Post · Reel · Story + count, full-month dates, NO money field (S4(b), §7) — the vendor's requests with Withdraw on sent and Mark completed on accepted (S5, R-40.107), the badge (S6), both empties (S7); EVERY act toasts COPY.launchingSoon. `lib/worklist/exchange.ts` — the vetoed strings, with the chair's one byte ("their audience"). `lib/mocks/exchange.ts` — three invented influencers and four requests, handles suffixed `.tdw` (the two-word `@aanya.m` was LIVE on web-check; all three renamed). `EXCHANGE_HREF` in routes.ts on the not-a-room precedent; one `RoomRow` with the hub's Coming chip under Shoots in Referrals & partners. `b40` C31 amended by label for the tenth constant. The vetoed frames land in docs/mocks with the byte and the renames applied.

2 · ON THE GLASS. Referrals & partners gains one row, "Influencer exchange · Coming", under Shoots. It opens a screen a vendor can read end to end — who is on it, their audience, what a request looks like, where her requests would sit — and every tap says "Launching soon." The influencer's own seat is not drawn (ruling §5: an influencer is a vendor of category content_creator with an opt-in and her own IG connection; her inbox is 4c-3b's).

3 · PROVEN / NOT. `b76` 7 GREEN, C1 executing the shipped TS; seven production mutations each RED, reversed by edit. `b75` 11 GREEN. `b40`: C50 + C102 red exactly as at the base (its two standing reds), C31 green with the amendment. `tsc --noEmit` clean. Floor: the relay's line. NOT proven: `npm run build`, any device.

4 · OPEN. F-42.177 stays open for the stylist and coordinator kinds; the exchange does not wait on it. 4c-3b (storage by allocation, the match arms, reach verification through igOAuth, the dark gate on perm.instagram_business_manage_insights) after R6 4b-3b lands. No new finding minted (F-42.206–.215 unused).

5 · NEXT. The founder's look at /vendor/exchange after deploy (no walk card owed for a shell: nothing acts). Then 4c-3b, or 4c-2's venue shell when the fixture decision comes (R-42.14: shells first).
