# CE-42 · 4b-3b · R6 · THE SUNDAY BRIEF, WIRED (dreamos-pwa) — HANDOVER · seat R6

1. **Shipped (the flip).** `SUNDAY_PREVIEW = false` in the same edit that wires the door, exactly as 4b-3a's file said it would be (`lib/worklist/sunday.ts:4–11`). `sunday.ts` gains `SundayDoor` (the door's answer) and `SundayActions` (the real controls); `lib/solutions/routes.ts` gains three addresses — `postSunday`, `postSundayRefresh`, `igAuthorizeInsights` (the ig door's own with `?scope=insights`, ruling 13(b), returning to `/vendor/posts` through `pwaPaths`); `components/worklist/SundaySection.tsx` takes an optional `actions` prop; `app/vendor/(shell)/posts/page.tsx` gains `SundayLive`, which reads the door and hands the section its code, its brief and the actions.

2. **Zero chrome changed (R-42.14).** Same classes, same bytes, same order — 4b-3a's control inventory is the contract, and only the handlers moved. Connect Instagram is a **pre-minted `<a href>`** to the insights authorize (portfolio/screen.tsx:470–540's law: no `await` between her finger and the navigation, so iOS suppresses the Universal Link instead of claiming it), re-minted every 8 minutes **while the tab is visible** (the server state lives 10, and re-minting under a vendor sitting on Instagram's consent screen would make her callback look like a replay). The best-post row becomes an `<a>` to its `permalink`; Share this opens the card state; Share and Download are the cards section's own handlers over the signed brief card; Check again POSTs refresh. **Without `actions` every CTA still toasts `COPY.launchingSoon`** — which is how b76 still renders all eleven states from the fixture.

3. **The eleven states come from the door's codes.** `pending · notconnected · connect · expired · error · stale · empty · live` are codes; **under-100 and the arrows are payload shapes** (`new_followers: null`, `prev: null`), which is how the section already read them at 4b-3a; **share** is the glass's own state (her tap). The fixture is never handed to the live glass — `FIXTURE_BRIEF` appears twice on the page, the import and the `SUNDAY_PREVIEW` branch, and b76 asserts the count.

4. **Proven / not proven.** `b76_r6_sunday_shell_bench` **19/19** — §1.1–1.3 and §2.12 AMENDED BY LABEL (R-41.121) from the shell's meaning to the wired one (one flag, now false; the door declared and read by the PAGE, never by the section; every CTA real with `actions` and still a toast without; no fixture on the live glass), and §2.14 added for the minted anchor, the permalink and the signed card. `b74_r6_posts_room_bench` **12/12**, two cells amended by label: **C8** said "Sunday still draws its pending state only (4b-3 is its packet)" and this IS that packet, so it now asserts the address exists AND the page calls it; **C5** scanned everything after the page's first `return (` and read the arrow token `=>` as a JSX tag close, so sibling-component code (`=> setBusy(false)); }`) registered as literal text — arrows are blanked before the match and the cell's meaning is untouched. Both amendments proven both ways: C8 RED at the uncured tree, C5 RED under a byte typed into JSX. `tsc --noEmit` clean. b69 71/71, b73 69/69, b75 11/11, b76 (4c-3a) 14/14, b57 192/192, b59 census green; b40 and b42 exit as they do at the untouched tip. **NOT proven: `npm run build`** — this box cannot reach fonts.googleapis.com, so `next/font` fails and the run exits 1 with no error naming a touched file. **The founder's build is the gate.**

5. **Base.** dreamos-pwa `1327f4344a25eae1ef6eff6eea7dee418480cdcc` (R7's 4c-3b-1p exchange inbox). Collision derivation: R7 touched none of this packet's six paths — each is the same blob at `a9b5e0cd` and `1327f434`, verified by `git rev-parse <tip>:<path>` at the cut. The server half is dream-os `0b7dfc9` (4b-3b cut 2, pushed and floor-verified); **migration 0165 was run by the founder and verified `table 1 · column 1` before this flip was cut**, so the door has its table.
