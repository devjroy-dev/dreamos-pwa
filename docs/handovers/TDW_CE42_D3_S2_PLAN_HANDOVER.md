# CE-42 · SEAT D3 · `/plan` SITTING 2 · THE PUBLIC INTAKE (R-41.94)

**Cut at dreamos-pwa `e81d703c170c867d0306bbc59ef288aff8759e6e`, derived fetch-first at the moment of writing. pwa only; dream-os is zero-byte this sitting (its `/public` mount landed at D5 and is read-only at `6164472`).**

1. **`/plan` exists, and the sheet is one component with two callers.** `app/components/couple/AssistanceSheet.tsx` takes **palette, chrome, `signedIn`, `submit` and `copy`** as parameters (Fork A/5, ruled a); the bride page is now seventy-seven lines of Wine Night and `CanvasShell` and spells **not one vetoed byte**. All three §13 traps are dead **by construction**: the component imports neither `CanvasShell` nor `FrostCtx`, holds **zero colour literals**, and can name no route to send a stranger into the authenticated shell. `signedIn={false}` skips `fetchMyAssistance` and `/couple/me`, so the public page fires no authed fetch and cannot produce a 401 — and the F-41.41 quiet frame settles at mount, because a lane with no read would otherwise hold it open forever.
2. **FORK 1(a), and `lib/auth/otpSignup.ts` is zero bytes.** On this lane Send cannot post — the door is inside `requireCoupleAuth` — so the sheet hands the body back (`'held'`) and `/plan` keeps it. `useOtpSignup` is called with a **router shim whose `push` only records**, so the POST lands **between `persistSession` and the navigation** rather than racing it; `OtpSignupDeps.router` was already typed as nothing more than `{ push }`, so s1's sealed file is untouched and `b20_a4` is unmoved at 75/75. The shim is also the success witness: `verifyOtp` pushes on exactly one path and every refusal returns after `showToast`, so **a recorded href IS a verified session** — reading the token back out of storage would have been worse, because iOS can throw on that write and `persistSession` swallows it by design. Then Fork 2's beat, **1500 ms**, then whatever destination the mint chose (this page never names `/couple/onboarding` — Fork C(a) is re-ruled in one file or none).
3. **FORK 4(a) · the city guard is the sheet's and runs only where a caller asks for it.** `requireCity` defaults **off**, so the bride lane's behaviour is unchanged and R-41.25's profile coalesce still stands there; `/plan` sets it, because a stranger has no profile to coalesce from and F-42.58 says a cityless request cannot be forwarded to an outsider — she would file into silence. The vetoed byte `Add your wedding city.` sits under the field it names, holds until a field changes, and has one home. **FORK 6(a):** `markAssistRequested()` is called from the public path too, as the second caller of an existing write with one home — declared as the reuse it is, not authored as a new one. **FORK 3:** a `Suspense` boundary, `fallback={null}` — `force-dynamic` would have made the estate's most crawlable new page pay per hit. **MIRROR:** `app/layout.tsx` joins with **both terms**, `path==='/plan'` and the `/plan/` prefix, and the bench drives `/planet` to prove the second cannot stand alone. **SITEMAP:** `monthly`, `0.5`; `app/robots.ts` **byte-untouched**.
4. **Floor at the cut.** **`b20_a5_plan_pwa` 90/90 GREEN**, both-ways — it **REFUSES (exit 1)** at `e81d703c` where the two new files are absent; behavioural wherever a transpilable subject exists (the layout branch driven through the real pre-hydration script, the sitemap through the real module, the two doors through a recording `apiPost` stub, `readPrefill` lifted from the shipped file and bound), textual on comment-stripped source elsewhere; **§12 mutates production source eight ways with a no-op control**. **`b20_a3` 204/204 GREEN** — **AMENDED BY LABEL (R-41.121), never re-pinned**: the sheet is two files from this cut, so the cells that were written about "the sheet" now read the **union**, and had they been re-aimed at whichever half currently satisfies them the bench would have stopped guaranteeing the other half cannot grow a second copy. F-41.41's cell now **binds the file's own initialiser and drives it** rather than asserting a spelling. `b20_a3` is consequently **both-ways from this cut: 202/204 RED at base**, two cells, both naming the extraction that did not exist there. `b20_a4` 75/75, `b70`/`b71` FLOOR GREEN, `tsc --noEmit` clean. **INHERITED, NOT MINE:** `b40` (C50/C102 red, C80/C81/C84 refused for the absent sibling) and `b42` C7 — the red **SET** derived at base and at the cut and **compared, not counted**: identical. That comparison caught one thing that was mine — the new layout comment carried a **straight apostrophe inside the template literal**, which C102's lexer reads as a shipped byte, not a comment; cured to `\u2019` before the cut, and a `(+7 more)` that became `(+8 more)` was the only tell it ever gave.
5. **Findings filed from the assigned range.** **F-42.136** — the ratified frame's and D2's note's colour cites had rotted by eleven lines (`.pv-cta` is `:980–985`, `.pv-sealfacts` `:978`, `.pv-demo` `:986`); **every value verified present**, and the new page cites **by selector, never by line**. **F-42.137** — the sheet carried 22 keys plus two loose literals (`placeholder="City"`, the budget aria template); both now have a home in `SHEET_BYTES`. **F-42.138** — a walk on a browser profile holding a vendor session overwrites `access_token` in the other direction; **card precondition: fresh profile or incognito**, not a code change. **F-42.139** — the pwa tree carries no protocol and four of its docs cite one; a pointer file is one line, Block 09. **F-42.140 (uncured)** — a POST that fails *after* verify has **no retry**: she is verified, the row is not written, she gets #21 and #33 and `Verify` is withdrawn rather than offered against a spent code. **F-42.141 (cured here, declared as the one behaviour change this extraction makes to the bride lane)** — that sheet's refusal outlived the edit that answered it, F-42.74's class one surface over; every field now clears it through one wrapper. **`.142`–`.145` unspent.** **Declared dirt:** `npm run build` cannot run in the seat container — `fonts.googleapis.com` is outside its allowed domains and `app/layout.tsx`'s four `next/font` fetches fail. Run at **base** it fails with the identical four errors and exit 1, and **no file in this packet is named in the failure**: NAMED BASE, no delta. The build gate rides the founder's verify block.
