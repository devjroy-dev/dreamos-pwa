# TDW_15 · P3 · ZIP 4 (dreamos-pwa) — THE PULSE WITHDRAWN BY FOUNDER WORD

**Executor seat under CE-35, 2026-08-21.**
**CE-56 attestation — fresh `git fetch -q origin`, tree reset to origin, expected-clean, before any byte:**
`dreamos-pwa` **`6111bb9`** · `dream-os` **`2a4c320`**. Sibling-full.

**Serves:** F-15.22, minted at the chair and witnessed on the founder's device — the wordless hairline idiom **fails on-glass comprehension outside the envelope rows' context**. The addition is withdrawn whole and the masthead returns to its approved bytes net.

**This is not a failed delivery. It is a walked one.** The idiom's reasoning was sound — `spent` is an honest floor and a figure would have claimed precision it does not have — and it was still wrong on the glass, because a hairline is legible in the expenses bloom only where the rows around it say what it measures. Alone under a masthead it says nothing a reader can decode. **That is exactly what walking a surface is for, and it is why the veto came before the walk and the withdrawal after it.**

---

## 1 · WHAT SHIPPED — two files

| path | state | what |
|---|---|---|
| `app/(frost)/frost/canvas/sanctuary/page.tsx` | RESTORED | the pulse withdrawn whole — render block, state, effect, `PULSE_THRESHOLD` and its declared-duplication comment, and the import symbols added for it |
| `scripts/tdw15_p3_pulse.proof.mjs` | AMENDED, LABELLED | retires with its reader; 6 cells → 5, counts disclosed |

**Not touched:** `lib/frost/tokens.ts` and `app/coplanner/page.tsx` (the day-boundary cure is a separate limb and stands) · `components/frost/blooms/moments.tsx` and the extraction canary (ZIP 3, banked) · `components/frost/blooms/expenses.tsx` · every dream-os byte.

---

## 2 · THE WITHDRAWAL WAS A RESTORE, NOT A HAND-EDIT — and that is the point

A hand-unwind of five insertion sites would have been five chances to leave something behind or take something extra. **The file was restored from git to `94dd738`** — the tree the founder's masthead veto was given against — and then the restore was *proven* rather than the edit inspected.

**Two derivations make that restore exactly right, and both were run rather than assumed:**

```
$ git log --oneline 94dd738..6111bb9 -- "app/(frost)/frost/canvas/sanctuary/page.tsx"
cc80229 TDW_15 P3 ZIP2: … the budget pulse …
```

**Exactly one commit touched this file across the whole phase, and it was the pulse's.** So `94dd738` is not merely an old tree — it is *this file's approved bytes with nothing else of P3 in it*. Restoring loses nothing.

```
sha256 restored : 48652973745b1e5fc0288550275d5ab450ea618d059285154ae7f6712a80a396
sha256 94dd738  : 48652973745b1e5fc0288550275d5ab450ea618d059285154ae7f6712a80a396
sha256 tip      : 70700ef986c93f4760accc226b685cba34524ef9418ff4d68d7cebb0d57dc6df
```

The withdrawal diff is **85 lines removed, 1 added** — the one addition being the original import line returning without `fetchEnvelopes` and `BudgetEnvelope`. Nothing outside the pulse moved.

**The ε3 fence's surface is untouched again**, and §1 of the amended bench asserts that byte-for-byte on every run rather than by eye.

---

## 3 · CONSEQUENCES, EACH LABELLED

### · F-15.19 — CLOSES-DISSOLVED
The 0.9 threshold's second home left with the pulse. `HAIR_THRESHOLD` in `components/frost/blooms/expenses.tsx` is the one home again and **the duplication no longer exists** — it was not cured, it was dissolved, which is a different and cleaner ending. **§5 of the amended bench holds it**: the surviving constant must still be there *and* no bare `0.9` may survive in the conductor. That cell reddens on the pre-withdrawal tip, so the closure is proven and not merely declared.

### · THE BENCH — RETIRED WITH ITS READER, COUNTS DISCLOSED
**BEFORE 6 cells · AFTER 5.**

- **RETIRED — 4**, each reading a subject that no longer exists: §1.1 the pulse rides the existing envelopes reader (F-15.16) · §1.2 the absence rule (C-4) · §1.3 the fill branches on the named threshold · §1.4 wordless (C-3), no text child.
- **KEPT — 2**, whose subjects outlived the pulse: §1.5 → §2 the masthead's own elements · §1.6 → §3 THE WALL.
- **ADDED — 3**, and they are stronger than what they replace: §1 byte-identity to the approved bytes, **out of git rather than by eye** · §4 no pulse residue anywhere in the file · §5 F-15.19 dissolved.

**A retirement is not a deletion.** Each retired cell is named in the file's header with what it used to assert, so a later reader can see that four greens went away because their reader did — not because they had become inconvenient.

**CARRIED BY NAME.** The filename still says "pulse". It is kept rather than renamed: a rename drops one floor-set entry and adds another, and the estate's idiom is to carry a name and tell the truth in the header (`FROST_BLOOMS` is carried stale by name for the same reason). The header's first line says what the file now proves.

### · THE DREAM CENSUS — NO AMENDMENT WAS MADE, SO NONE IS REVERTED
The ruling asked for a second labelled amendment *if* ZIP 2's addition had moved the fence's figure. **It did not, and that was re-derived here rather than recalled** — `scripts/tdw13_d7_dream_design.proof.mjs` was run at the pre-pulse tree and at the pulse-bearing tip and printed the same census both times, class for class. It runs green after the withdrawal too: `3d. forty-one verb-sites across eleven classes, in live code` · `VERDICT: GREEN`. **The instrument was never amended, so there is nothing to revert and no second amendment is owed.** Stated with its derivation rather than skipped in silence.

### · THE MATRIX — the record's home, and a drift reported
The ruling directs a P3.1 row in the matrix. **There is no P3.1 row.** `docs/BRIDE_PARITY_MATRIX.md` is a Mira-capability × bloom contract, 21 rows off `src/agent/brideTools.js`; days-to-go, the pulse and image delivery are not bride tools, and TDW_15's acceptance items live in `docs/specs/TDW_15_ROOMS_FINAL.md §5`, dream-os. This is **c-35.14's class arriving once more** — the chair already owned the misdirection on this seat's derivation, and the instruction reproduces it. Reported, not adapted to (§0.2).

**The record therefore rides its committed home, as every other P3 record has** (CE-214 precedent), in this handover, in the chair's exact words:

> **P3.1 — the budget pulse: TRIED ON-GLASS, WITHDRAWN BY FOUNDER WORD 2026-08-21.** A walked outcome, not a failure. F-15.22 carries the reason.

**Zero matrix rows moved across the whole of P3**, declared here as they were declared in ZIPs 2 and 3.

---

## 4 · BENCHES — BOTH WAYS, REDS FROM THE RUN'S OWN OUTPUT (R-33.10)

| leg | result |
|---|---|
| **CURED** (withdrawn) | **5/0** |
| **UNCURED** — the pre-withdrawal tip `6111bb9`, amended bench copied in alone | **2 PASS / 3 FAIL** — §1 `not identical (1170 -> 1254 lines); first unmatched: "import { fetchEnvelopes, type BudgetEnvelope, …` · §4 `the threshold constant was left behind (PULSE_THRESHOLD)` · §5 `a bare 0.9 survives in the conductor` |
| **M1 · a PARTIAL withdrawal** (render block removed, constant left) | **2/3** — §1 `not identical (1170 -> 1171 lines); first unmatched: "const PULSE_THRESHOLD = 0.9;"` plus §4 and §5 |
| **M2 · instrument control** — the approved-bytes witness made unreachable | **4/1** — §1 `git show failed at 0000000 — without the approved tree this cell would be vacuous, so it FAILS rather than passing` |

**§2 and §3 stay GREEN on the uncured tree**, which is the control that they were never the pulse's cells and are not riding along.

**M1 is the mutation that matters.** A partial withdrawal is the realistic failure mode of an unwind like this — the visible block goes, a constant or an import stays — and three cells catch it, one of them by a single line of drift out of eleven hundred.

**M2 exists because a byte-identity cell is worthless if its witness silently vanishes.** The cell reads the approved tree out of git, so an unreachable ref must be a RED, never a pass. It is, and it says why in its own output.

**§6 GATE — cache cleared, as the ruling required, because the symbol count changed:** `rm -rf .next` then `npx --no-install tsc --noEmit` → **0 errors repo-wide**.

**SIBLING BENCHES, unaffected and re-run rather than assumed:** the day-boundary proof **20/0** · moments **6/0** · `tdw13_d4_extraction` **VERDICT: GREEN** (ZIP 3's ninth and tenth entries intact) · `tdw13_d7_dream_design` **VERDICT: GREEN**.

---

## 5 · FLOORS — WARM, SIBLING-FULL, BOTH REPOS

Results recorded in the delivery message.

**F-14.26 — DECLARED, NOT RESOLVED.** The pwa runner has no `--delivery`, so on a delivery tree it **measures but cannot gate**: it cannot verify a declared-dirt set, cannot hash the manifest's files before and after, and cannot distinguish `tdw_f0774_vacuity_probe`'s lawful refusal of a dirty tree from a real red without a hand-run control on a clean clone. That control was run at ZIP 2 and its verdict stands: rc 0, *"GREEN — the cure sees what the disease hid."* The port is a queued micro and is not this seat's.

---

## 6 · WHERE P3 STANDS AFTER THE WITHDRAWAL

- **P3.1** — the number she wakes to is cured in both repos and TZ-invariant (F-15.17, closed). **The pulse is withdrawn** (F-15.22). The morning line does not ship — F-15.18 open, acceptance 5.4's `briefing.js` limb waived by name, founder word "skip", 2026-08-20.
- **P3.2** — mood half shipped prior; photo half split by R-35.24. Ladder tip `0125`; `0126` noted, not reserved.
- **P3.3** — moments on the 07 image discipline, banked at `6111bb9` with the ninth and tenth allowlist entries granted by R-35.25.

**The masthead is the founder's approved drawn ink and nothing else, byte-for-byte** — which is where it started, and now with a cell that will say so on every run.

**Open by number:** F-15.18 · F-15.20 · F-15.21 · F-14.26 · R-35.24's split photo-attach arc.
**Closed this ZIP:** F-15.19 (dissolved).

---

*Sequencing beyond this delivery is the founder's.*
