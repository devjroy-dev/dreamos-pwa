# CE-42 · 4b-1 · R6 · THE "POSTS & ADS" ROOM (dreamos-pwa) — HANDOVER · seat R6
1. **Shipped:** `app/vendor/(shell)/posts/page.tsx` — one screen, three sections (ruling 1a): Cards live, Broadcast "Not switched on yet.", Sunday pending · copy home `lib/worklist/posts.ts` · `routes.ts` gains `POSTS_API_PATH`, `API.postCards`, `POSTS_HREF` · the hub's `posts` row gets its href (Coming → Open) · `b40` C31 amended by label to read `POSTS_HREF` · bench `b74` 8/8, 9 production mutations each red by name · the frame, cut 2, banked in `docs/mocks/posts-ads-mock.html` + 18 PNGs with the three frame vetoes applied.
2. **On the glass:** Business Solutions → Posts & ads → the card, Post/Status/Story, caption, Download (fetch → save; opens the image if the fetch refuses), Share (share sheet, else wa.me), Copy caption (no confirmation byte — none vetoed). Refusals render the door's own words for its three codes and the hub's generic byte for anything else. No switchboard key on vendor glass (F-42.193).
3. **Not proven:** the walk — Cloudinary's render, iOS download destination, the share sheet. `tsc --noEmit` exit 0; b40's two reds (C50, C102) byte-identical to the base. The pwa floor and `npm run build` are the founder's gates (R-40.66). Needs the dream-os ZIP deployed first, or the room reads the generic byte.
4. **Next:** 4b-2 replaces the Broadcast pending state with its door; 4b-3 the Sunday state (dark).
5. **Base:** dreamos-pwa `a96e2e23a73081070a9d2155e247a847938713f0`. **Cut 2** (chair-ruled (a)): cut 1's verify stopped on a 0-byte `app/admin/_components/tokens.css` written by ANOTHER SEAT'S bench, `scripts/tdw10_p3_deck.proof.mjs` — its §8 `mutate()` registered absent files as originals and `restore()` wrote them back, then threw ENOENT. Amended under R-41.121 with **F-42.173**: one `existsSync` line at the top of `mutate()`. Both ways: uncured → stray + crash; cured → none, 160 passed / 33 failed, red set unchanged (stays in the named base).
