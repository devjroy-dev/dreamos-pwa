CE-42 · 4c-1 · R7 G5.2 THE SHOOT BOARD (dreamos-pwa, base 59b23451f7e32fc99c14e3b7240ee04db1d6aa02 — re-pinned from 85f6f3c1 after SHELL; dream-os half at 728cd1a) · [CE-42 SEAT R7]

1 · SHIPPED. `components/vendor/CollabPostForm.tsx` — the one composer, `kind` collab|shoot (ruling 4(b)): the eleven off GET /collab/requirement-types with the founder-signed labels (F-42.184), the shell's standing sheet (StudioSheets `Sheet`, now exported, otherwise byte-unchanged), a note per requirement (collab_post_items.note, new control), `Shoot type` always on, the kept bytes restored (departure 3), `New Requirement` retired. `components/vendor/ShootsBlock.tsx` — the Shoots block INSIDE Referrals & partners (ruling F), reading the Collab doors through `?kind=shoot` (3(ii)), Interested/Pass on the respond door with her craft's `item_id`, rows into the post's own interior to cast. `lib/vendor/collabFormat.ts` (moved helpers; full-month dates R-42.13; the vetoed event-type labels), `lib/worklist/shoots.ts` (the seven vetoed strings), routes.ts `COLLAB_API_PATH` + five members, cityMatch.ts's header names its dream-os twin. The Collab screen reads all of it; its ten gold-at-alpha literals became existing tokens (F-42.186) and `b59` re-pins its ratchet 124 → 114 in this delivery. The vetoed frames land in docs/mocks as filed.

2 · ON THE GLASS. Referrals & partners gains a Shoots block (Post a shoot · Open to you · Your shoots); its empty forwards pair is left-set, bytes kept. The Collab room's `+ Post` opens the new sheet: the eleven crafts, sentence-case event types without the shoot pair, full-month dates everywhere on the room ("18 Sept 2026" retires). The roster sheet's crafts are the eleven. The gold tint leaves the Collab room's hairlines.

3 · PROVEN / NOT. `scripts/b75_4c1_shoot_board_pwa_bench.js` 10 GREEN, C4–C6 executing the shipped TS; ten production mutations each RED, reversed by edit. `tsc --noEmit` clean. pwa floor in the seat container at 59b23451 (SHELL's re-based floor, c-42.43), `--delivery … --check`: see the relay for the measured line; `b59`'s ratchet 124 → 114 is amended by label (F-42.186) in this delivery; `tdw_f0774_vacuity_probe` REFUSES only on a dirty tree (NEEDS_CLEAN), excluded by --check. NOT proven: `npm run build` (the founder's gate at apply), any device, any live post.

4 · OPEN. F-42.190 (the first-look line says 12 hours; no door returns admin_config's value) — filed, named on the card. F-42.189 (the demo collab page's sixteen) — Block 08. F-42.203 (the two short-date homes) — cured when each is next touched. F-04.112's named case stays named. c- (the seat's): the dream-os half's bench shipped as `b4c1`, a name rather than the derived ladder number; this half takes b75 by b69's rule.

5 · NEXT. The founder's card (after both deploys): first_look_hours → 0, MAKEUPBYSWATIROY posts a shoot, DEV440 sees and offers from Referrals & partners, Swati casts; the reverse to 12 is withheld until the walk reads. 4c-2 waits on a venue fixture; 4c-3 on 4b's key and F-42.177.
