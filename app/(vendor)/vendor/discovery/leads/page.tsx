'use client';
// app/(vendor)/vendor/discovery/leads/page.tsx
import { ComingSoon } from '../../../../../components/frost-vendor/atoms';
export default function VendorDiscoveryLeadsPage() {
  return (
    <ComingSoon
      title="Discovery Leads"
      blurb="Enquiries that came directly through the Discover feed — blind swipe reveals, Muse saves, and direct contacts."
      eta="Coming in the next build"
    />
  );
}
