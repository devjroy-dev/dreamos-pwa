'use client';
// app/(vendor)/vendor/discovery/images/page.tsx
import { ComingSoon } from '../../../../../components/frost-vendor/atoms';
export default function VendorDiscoveryImagesPage() {
  return (
    <ComingSoon
      title="Image Hub"
      blurb="Manage the photos that show in the Discovery feed. Control which images represent you in the blind card and the reveal."
      eta="Coming in the next build"
    />
  );
}
