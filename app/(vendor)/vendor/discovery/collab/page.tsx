'use client';
// app/(vendor)/vendor/discovery/collab/page.tsx
import { ComingSoon } from '../../../../../components/frost-vendor/atoms';
export default function VendorDiscoveryCollabPage() {
  return (
    <ComingSoon
      title="Collab Hub"
      blurb="Connect with other vendors — photographers with MUAs, decorators with caterers. Mutual referrals, packaged offers."
      eta="Post-launch"
    />
  );
}
